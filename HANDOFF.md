# Handoff — 文明长河（17-civilization-river）
更新时间：2026-08-22 11:56

## 这是什么

给 7 岁的 Jasper（在上海用 iPad）做的世界文明兴衰可视化，**单文件 PWA**：全部代码、数据、海岸线都在 `index.html` 里，无构建、无后端。三个视角——**按时间看**（泳道图）、**按地方看**（世界地图 + 城市面板）、**按列表看**（172 行的表）。

## 当前状态（接手先 `node tools/check.js` + `git status` 核实）

- `HEAD = 6277d59`，**已 push**（远端 `origin/main` 实测同步，未 push 数 = 0）
- 工作区只有 `HANDOFF.md` 未 commit（就是这份）
- `node tools/check.js` **全通过**（audit 16 条 warn、smoke 5 条 warn 都是既有基线）
- **9 泳道 · 172 文明 · 33 轨迹 · 342 张人物卡 · 84 张成就卡 · 392 段鼎盛区间 · 127 城 · 30 张数据表 · audit 规则至 82（含 79b）**
- depth 三项绝对缺口：鼎盛不足 **0**、无人物 **0**、大档无成就卡 **0**；中档无成就卡 **37**、小档 **77**

## 这一轮（2026-08-19 → 08-22）做完了什么

上一版 HANDOFF 留的唯一未闭环项是「v272–v275.2 那 60 段鼎盛区间没过独立核查」。已闭环，并顺势做了工具与内容两条线。

| 版本 | 做了什么 |
|---|---|
| v276 | 中档无成就卡补齐第 1 批 6 张（缺卡 43→37） |
| v277 | **对抗核查 9 条必修全部修完** |
| v278 | 建议档事实/hedge 类 12 条改完，**3 条驳回** |
| v279 | audit 规则 **79/80/81**（同带重复上架、跨带标题撞车、同一部书跨带） |
| v280 | audit 规则 **82**（跨层版图面积打架）+ 清掉 3 处真冲突 |
| v281–v284 | 正文薄补齐：唐、俄罗斯·苏联、孔雀王朝 |
| v283 | audit 规则 **79b**（同带内同一件事写两遍）+ 修掉它抓出的 6 处真重复 |
| v285 | **depth 正文密度改判据**：中文数字也算锚点 |
| v286–v287 | 正文薄补齐：大英帝国·英国、商 |

## 进行中 / 下一步

### 1. 正文薄清单（新口径，实测。做法见「写新内容的固定流程」）

```
孔雀王朝   大事记条数 10 / 六问 482 / 最短格 69 / 密度 2.2
罗马帝国   大事记条数 10 / 六问 372 / 最短格 49 / 鼎盛非短段 3   ← 下一个性价比最高
古希腊     六问 523 / 最短格 73 / 鼎盛段 3 / 非短段 2
西汉       大事记条数 9 / 非短段 2 / 密度 2.5
共和国     大事记描述率 0.47 / 鼎盛段 2 / 非短段 2
朱罗王朝   大事记条数 4 / 六问 438 / 鼎盛段 2 / 交往 1
明         六问 296 / 最短格 41
西周       六问 631 / 最短格 52 / 鼎盛段 2 / 交往 0
```

**罗马帝国是下一个**：六问 372 字、最短格 49，与大英帝国改前完全同一形态（概括句、零锚点），而大英那一版从 8% 拉到了 92%。

**商仍薄三项**（大事记 4 条、六问 408、最短格 57），但它是**层多料少**——甲骨/青铜/妇好已被 GL 段与人物卡分完，再往六问写就是复述。要补得先找新料（候选：车马坑、作坊分工、方国关系）。

### 2. 成就卡（中档 37 / 小档 77）

**建议小档 77 条明确不做**——影响力低、量大，做完也没人点。中档还剩 10 条「干净」的（不属于已核查那批）：

后倭马亚（科尔多瓦大清真寺）· 马穆鲁克（**只能选「驿传制度」**，另两个成就已被 GL 段与拜巴尔人物卡占掉）· 马拉塔（山地要塞体系）· 吐蕃（唐蕃会盟碑）· 南宋（市舶司海贸）· 阿尤布（经学院网络；注意「开罗城堡」与 `place_lore` 开罗|阿尤布 重合）· 朱罗（青铜舞王像）· 战国（变法运动）· 西晋（**键是「再统一」**，站内原文带括号，`stripParen` 后才匹配）· 特奥蒂瓦坎（网格都市）

### 3. 城市断层（`node tools/gap.js`，实测 9 城 12 处）

摩亨佐达罗（前1300—前550 断 750 年）· 拉合尔（560—1206 断 646 年）· 加尔各答 · 拉萨 · 特拉布宗 · 大津巴布韦 · 罗马 · 卡霍基亚 · 胡志明市

### 4. 等 Ray 定的两个体例问题（我有意没动）

- **帕提亚「安息回马箭」归 `k:'art'`** —— 一项骑射战术挂在「艺术」域，条上直接显示「艺术」两个字给孩子看。像是为凑齐四域
- **四处「GL 逐句复述 q」**：赫梯两段、帖木儿、德里苏丹、古巴比伦数学段。要重写而非改一句，牵涉「GL 该讲什么」的口径

### 5. 只有 Ray 能做的

- **唐朝实验收口** —— 六页材料 8/9–8/10 就做完了。`IDEAS 001`（搜索→故事级颗粒）与 `003`（探索模式）的触发条件**都写着「唐朝实验出结论之后」**，`TANG-EXPERIMENT.md` 又写死「结果出来前不加功能、不做第七页」。**这一件堵住整条产品线**。观察清单已推到飞书群
- `whviz-sq` 搜索词导出（零成本探针，只要在他 iPad 上导一次）
- **域名备案** —— `DEPLOY-CN.md`：EdgeOne 已部署但国内仍打不开
- `VIDEO-CANDIDATES.md` 32 条核 BV 号（文档写死要 Ray 亲自看）
- iPad 实机验收

## 试过但失败的路

1. **「跑了工具」不等于「没问题」。** 规则 78 用最长公共子串 ≥14 字，而 v272–v275.2 那批的复述全是**同义改写**——一条都没触发。上一版 CHANGELOG 却把「扩了规则 78」当成兜底。

2. **自核会把对的改成错的。** 商甲骨那条是铁证：核查员四次被 API 529 打断后改用自核，把正确的「十几万片」改成了错的「一万三千件」，还在 CHANGELOG 里记成「修正」。**自核不是独立核查的替代品，只是拖延。**

3. **规则会漏，而且是同一个病灶换形态。** 79 只抓「k 与区间全同」→ 商换个 k 就漏；80 只抓「跨带标题逐字相同」→ 俄苏改写标题就漏；补了 79b（年代重叠 + 标题去通用词后仍有 ≥2 字公共子串）之后，**孔雀的「阿育王护法」与「石柱与摩崖」还是漏了**——两个标题一个公共字都没有，正文却都在讲石柱诏令。**标题完全不同、正文讲同一件事，只能人读。**

4. **停用表是这类规则能不能用的分水岭。** 不去掉「世纪」「时代」，西葡的「白银世纪 vs 黄金世纪」、罗马的「工程时代 vs 法学家时代」就会误报。实测：字集 Dice 版 14 处精度约三成不可用，LCS+停用表版 9 处可用。

5. **指标会系统性冤枉某类写法。** depth 的密度原来只认 `\d{2,4}`，看不见中文数字，而中国带与古代带普遍用中文数字写——秦的「四十字诏」「广五十步」一个都没被看见。v285 改了。**但要注意**：改完秦在组内仍是 0%，因为同组的唐清明也一起涨了。**相对指标里，统一的修正不改变同质组内的排名。**

6. **hedge 只改正文、不改标题等于没改。** 蒙古《史集》那段我自己犯过：正文改成「已知最早」，英文标题 `the first world history` 忘了跟。是 `lint-content` 抓出来的。

7. **英文串里的撇号必须写 `\'`。** `soldiers' pay` / `ASEAN's` / `Tuvalu's` / `robbers' tunnel` 都当场把语法步打红。Python 补丁脚本里要写 `\\'`，写成 `\\\\'` 会变成「转义反斜杠 + 终止引号」。

8. **批量替换的 all-or-nothing 是逐文件的，不是全局事务。** `civs.js` 回滚了而同批的 `gl.js` 已写盘。每次都要看 `git diff --stat` 确认哪个文件真改了。

9. **改数据前必须 `grep -ln` 确认它住在哪个文件。** 鼎盛段散在 `data/gl.js` / `data/gl_x.js` / `data/civs.js` 三处，**同一对重复段还可以跨文件分居**（隋的 econ 在 gl.js、tech 在 gl_x.js）。我连着猜错三次。

10. **注入反例会污染规则 75 的 `.audit-stats.json`**（加过段再撤掉就算「缩水」），测完 `rm` 一次。

11. **写长不等于写厚。** 唐和孔雀第一版都是加了散文、密度反而下降——因为加的是句子不是锚点。判断标准是「每百字有几个可查的具体物」。

12. **手滑会把别的语言打进正文。** 大英 legacy 里混进过一个俄语词 `первый`。收尾时值得全站扫一遍非中英文字符（站内合法的只有西辽卡里解释词源的 `Китай`）。

## 关键上下文

### 改完必跑
```
node tools/build.js      # 改 data/*.js 之后必跑
node tools/check.js      # = build 一致性 → audit → 语法 → lint-content → smoke
node tools/lint-content.js <本批第一个 commit>~1   # 批量写完之后,逐条销账
```
⚠ `node tools/audit.js | tail` 会让 `$?` 变成 tail 的退出码——要看真实退出码就别接管道。

### 写新内容的固定流程
`node tools/context.js <带名|城市名>` → 写（中国带以中文一手为准；**西方带先用英文材料打底再译中文**，Ray 2026-08-18 定）→ `node tools/check.js` → commit → **派 adversarial-verifier**（给它 commit 号 + 判定依据 + 存疑位置清单；**不给完成报告、不给改动理由**）→ 逐条裁决（站不住的要驳回，这轮驳了 3 条）→ 修正 commit → `lint-content <sha>~1` 销账

### 加成就卡的两个硬约束
- **键必须与该文明 `f['成就']` 里的原文整词一致**，渲染端做 `NSPLIT` 拆分 + `stripParen` 去括号后**精确匹配**，对不上就是纯文本、点不开而且不报错
- 自查可达性：照渲染端做一遍 `split(/[、,，;；]/)` + 去括号，再比对 ACHV 的键

### 基线文件
`tools/.crossband-baseline.json`（**入库，非 gitignore**），四个键：
`titles`（规则 80）· `books`（81）· `areas`（82）· `samebandPairs`（79b）
每条带 `why` / `when`，**不自动写入**——新增命中就是红。
> 没有理由的基线三个月后没人敢动，只会一直加不会减。规则 36 的 71 城基线就是这么变成黑箱的。

### 核查员反复挑到的模式（下一批写之前先看）
1. 中英 hedge 不齐（含标题与正文不齐）
2. 卡片/鼎盛段抄母条目——规则 78 只查逐字，同义改写查不出
3. 同一件事在同卡讲第三遍
4. **跨色带互相拆台**（同一件事在两条带上年代/口径不一致）——目前无工具，只能人读
5. 最高级要么给出处要么写「之一」「已知最早」
6. 现在时会过期（「至今还」「再没有」）

### 本机环境
- **`git push` 现在能推了**（2026-08-22 实测成功，`007d6d7..6277d59`）。上一版 HANDOFF 记的「被权限拦」已过期
- `curl` 出网被拦；`timeout` 不存在。**Python `urllib` 与 WebFetch 出网可以**
- smoke 需要 `playwright-core`（仓库根 `npm install`，只装这一个 devDependency，用本机 Chrome）
- adversarial-verifier 这轮一次跑通，耗时约 21 分钟。连续遇到 API 529 时别硬重试——**也不要拿自核当替代品交付**（见「试过但失败的路」第 2 条）

### 关键文件
| 文件 | 要点 |
|---|---|
| `docs/CHECKS.md` | 规则体系设计文档 |
| `docs/CHANGELOG.md` | 每版详录，**判断依据都在这里**；v277–v287 是这一轮 |
| `docs/DATA.md` | 数据模型；ACHV 键匹配规则、`y` 是「这东西起作用的年代」 |
| `docs/DESIGN.md` | 「卡片是博物馆，不是词条」在「信息层级」一节 |
| `docs/IDEAS.md` | 001/003 待讨论，**触发条件都是「唐朝实验出结论之后」** |
| `docs/TANG-EXPERIMENT.md` | 观察清单三条 + 「结果出来前不加功能」的纪律 |
| `tools/audit.js` | 82 条规则；78 的判据在 1802–1806 行（grep `复述母条目`）；79–82 的头注释里写着**为什么某些做法行不通**，动手前先看 |
| `tools/depth.js` | 密度判据在文件头注释，v285 起含中文数字 |
| `data/gl.js` / `gl_x.js` / `civs.js` 内联 `gl:[` | **鼎盛段住在三个地方**，改之前先 `grep -ln` |
