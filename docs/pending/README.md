# 待核查的六问批次（v41 合并，尚未经 adversarial-verifier）

这里的 JSON 是撰写 agent 的产出，**已经合并进 index.html 但还没核查**。
核查完、按意见修正后，这个目录连同本文件一起删掉。

| 批 | 范围 | 条数 | 状态 |
|---|---|---|---|
| A | 东亚 | 5/22（session limit 中断，只写到战国） | 已合并，**待核查** |
| C | 欧洲+美洲 | 15/15 | 已合并，**待核查** |
| D | 南亚+东南亚 | 19/19 | 已合并，**待核查** |
| B | 中东北非+撒南非洲 | 0/23 | **未撰写** |
| E | 草原·中亚 | 0/17 | **未撰写** |

## 接手要做的三件事

1. **核查已合并的 39 条**：派 adversarial-verifier，判定依据见 `q-style-guide.md`
   的三条铁律与「已被抓过的错型」清单。核查对象给 JSON 文件，不要给撰写者的自查报告。
2. **补写 A 剩余 17 条**（秦、西汉、东汉、三国、西晋、东晋·十六国、南北朝、隋、
   五代十国、北宋、南宋、辽、西夏、金、元、朝鲜半岛、民国）+ B 23 条 + E 17 条。
   批次数据在会话 scratchpad 的 `q-batch-{A,B,E}.json`；**那是临时目录，多半已经没了**，
   用 `node tools/refs.js` 或直接读 index.html 重新导出即可（导出脚本见 v39 commit 记录）。
3. 合并脚本 `q-merge.js` 也在临时目录，同样可能已丢失——它做的事就是校验六键齐全+
   双语，然后把 `q:{...}` 插到每个条目的 `d:` 行之前，二十行左右，重写不难。

## 为什么先合并再核查

撰写成本很高（一批约 10 万 token），session limit 随时可能砍断。先落盘保住成果、
本地 commit 但**不 push**，比留在临时目录里等着被清理安全。**未核查的内容不上线**
是硬线——上一批 60 段核出 4 处硬错，这不是可以省的步骤。
