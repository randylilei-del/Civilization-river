# Handoff — 文明长河（17-civilization-river）
更新时间：2026-08-15 00:20

## 任务目标

给 7 岁的 Jasper（在上海用 iPad）做的世界文明兴衰可视化单文件 PWA。
本轮做了四件事：**六问表达返修 → 共同体层 → 两个图形缺陷 → EdgeOne 国内部署排查**。

---

## 已完成（v134–v148 已 push 至 `065da59`；**v149–v152 已 commit 未 push**——2026-08-15 UX 批，push 被权限拦，等 Ray 放行）

接手时先 `git status` / `git log --oneline -20` 核实。

| 版本 | 内容 |
|---|---|
| v134 | 六问标题改口语问句（「怎么起来的?/怎么管的?/钱从哪儿来?/强在哪儿?/后来怎么不行了?/留下了什么?」）+ 钩子句样板（唐、古埃及）+ **audit 规则 49** |
| v135 | 「国家还在但色带结束了」统一排查，4 条改名：荷兰→**荷兰共和国**、埃塞俄比亚→**埃塞俄比亚帝国**、西班牙·葡萄牙→**西班牙·葡萄牙帝国**、朝鲜半岛→**朝鲜半岛诸王朝** |
| — | `docs/IDEAS.md` 002「共同体」写入 |
| v136–v142 | **六问钩子句铺完全部 140 个文明（840 条）** + **audit 规则 50**（钩子不得与正文首句重复） |
| v143–v144 | **共同体层上线**：欧盟 / 东盟 / 非盟三条，新表 `data/unions.js` + **audit 规则 51** |
| v145 | 色带标签不再被画布边切掉（12 个视图全部归零） |
| v146 | 台湾相关表述统一口径（按语法角色分类，非全局替换） |
| v147 | 地图白色长条（跨日界线）+ 标签跟随横向滚动 |
| v148 | 按实测重写 `docs/DEPLOY-CN.md` |
| v149–v152 | **UX 批(2026-08-15,Ray 拍板六条一起做,并解除实验期功能冻结)**:卡片重排+六问平铺(v149)/细色带命中补偿选单(v150)/开场条·今日文明+首访提示(v151)/**搜索上线=IDEAS 001 层一**+新表 `data/search_alias.js`+audit 规则 52+搜索词探针 `whviz-sq`(v152)。细节见 CHANGELOG 四条 |

**当前规模**：140 文明 · 大事记 1000 · GL 265 段（140/140 覆盖）· 事件 22 · 轨迹 10 · 共同体 3 · 搜索别名 10 · audit **52 条规则**(52=搜索别名,v152)。

---

## 进行中 / 下一步

### Ray 手上的（按性价比排序）

1. **微信发 `index.html` 给上海** —— 2026-08-14 已实测：单独拷进空目录（无 `sw.js`/`manifest`/`icons`）
   功能完整（色带 140、标签 126、共同体 3、事件 22、卡片含地图与钩子）。**不用等备案，今天就能用。**
   六个故事页 `tang/qin/han/song/ming/qing.html` 同理（各 31–37KB）。
2. **提腾讯云工单**：「全球可用区（含中国大陆）项目，使用平台默认 `.edgeone.cool` 域名，
   中国大陆访问返回 401，是否必须绑定已备案域名？」—— 答案回来后改 `docs/DEPLOY-CN.md` 第三节。
3. **开压缩**：EdgeOne **站点加速 → 智能压缩**（不在 Pages 项目里）。实测国内传输 2362KB
   vs 本地文件 2365KB，**没有 gzip/brotli**；开了约 500KB。
4. **看 24 条 B 站候选视频**（`docs/VIDEO-CANDIDATES.md`），报「文明 + BV + 标题」给 Claude 入 `data/video.js`。
   ⚠ 铁律：VIDEO 只能填人工核实过的链接，**绝不可凭印象生成 BV 号**。
5. **细看 840 条钩子句的语气**（Ray 说「回头再细看检查」）。发现哪条别扭报编号，改单条几分钟。

### 代码侧待办（等 Ray 拍板才动）

- **维基链接国内打不开**（`docs/DEPLOY-CN.md` 第四节）：A. 中文改百度百科（`index.html` 的
  `wiki()` 一个函数、一行）／ B. 不改／ C. 中文去掉外链。**建议 A，等 Ray 选。**
- **东帝汶入盟待核**：`data/unions.js` 东盟条目按 10 国计，`nt` 里已写明「这一条待核」。
- **故事页离线**：`sw.js` 缓存清单只有 `index.html`，六页既不在清单也不注册 SW。
  **有意押后**（属「实验通过后的最小整合」，见 `docs/TANG-EXPERIMENT.md`）。
- **唐朝实验结论**：六页发出去后等 Jasper 反馈；**结果出来前不做第七页、不加功能**。
  实验一收口，`docs/IDEAS.md` 001（搜索/别名/故事级颗粒）要一起拿出来讨论。

---

## 试过但失败的路（本轮最值钱的部分）

1. **EdgeOne 排查连续改口三次**：加速区域选错 → 备案 → 域名未关联环境 → **又回到备案**。
   根因是**先下结论再找证据**。最后靠一条不对称定案：
   **干净 URL 国内 401（123ms）／海外同一 URL 不带任何凭证 200（2362KB）**——
   这条同时否掉「墙」（不会 123ms 应答）、「区域选错」（带 token 就通）、
   「预览未发布」（那样海外也会拦）。**教训：先量再写。**
2. **想用全局字符串替换做「台湾→中国台湾」**：会造病句（「清朝 1683 年取中国台湾」
   「马关条约把中国台湾割给了日本」）。改为按**语法角色**分类——归属语境改 3 处，历史叙述保留 7 处。
3. **想用「横跨色带的括号」画欧盟**：①全景下 1993 起算只有 **45px**，标签放不下（改 1957 起算得 95px）
   ②1957—2025 欧洲泳道四条色带里**只有法德是成员**（英国 2020 退出、俄非成员），
   括号四条错两条。→ 改成泳道底部横杠 + ◎ 图标 + 成员名单卡。
4. **对地图白条的第一次推断是错的**：看到主环顶点纬度从 75.9 跳到 16.9 就以为压缩掉了顶点。
   其实环绕欧亚一圈本来就会两次经过同一条经线。真因是**跨日界线**——
   `[-180,65]→[180,65]` 与 `[178.6,69.4]→[-180,69]` 两条横扫全图的边，
   中间填充被缠绕规则抵消，**缝的上下边界正好是 65 与 69**。
5. **选择器写错造成两次假警报**：`span.pn[data-fig]`（实为 `data-person`）报「人物 0」；
   `panel.querySelector('svg')` 拿到火花线报「陆地 0 条」。
   **教训：报「找不到」时先证明选择器是对的。**
6. **反向注入自己出错**：bash 向 python 传了字面 `\n`，注入成 JS 语法错误，
   而我只 `grep 共同体` 把真实报错滤掉了，看着像规则没生效。
   **教训：注入测试要看清报的是哪一种错。**
7. **`curl` 出网被本机权限拦**（CHANGELOG v125 已记）。改用
   `node -e "https.request({host: <IP>, servername: <域名>, headers:{Host:...}})"` 直连边缘节点。

---

## 阻塞项 / 等用户决定

- **维基链接 A/B/C**（见上）——一行改动，只差拍板。
- **备案**：买域名 + 个人备案 **1–3 周**，Claude 不碰（涉及身份证）。这是国内正式上线的唯一路径。
- **钩子句语气 review**：840 条，Ray 说回头细看。

---

## 关键上下文

### 验证命令（改完必跑）

```
node tools/build.js                # 改 data/*.js 之后必跑
node tools/audit.js                # 52 条规则，退出码非 0 即有问题
sed -n '/<script>/,/<\/script>/p' index.html | sed '1d;$d' > /tmp/c.js && node --check /tmp/c.js
```

浏览器验证：`python3 -m http.server 8777 --bind 127.0.0.1 --directory <项目路径>`（后台任务方式起，
`nohup &` 会被权限拦），再用 claude-in-chrome；`file://` 会被扩展拒绝。

### 本机环境的坑

- **Shadowrocket 以 TUN 方式在跑**（系统代理显示 Off，但 DNS 返回 fake-ip `198.18.x.x`）。
  所以**浏览器测不出「手机不挂梯子」的情况**；测国内可达性必须
  `dig @223.5.5.5` 拿真实 IP + node 直连 IP 并带 SNI。
- `curl` 出网被权限拦；`timeout` 命令不存在。

### 关键文件

| 文件 | 作用 |
|---|---|
| `data/unions.js` | 共同体表（本轮新增）。字段与纪律见 `docs/DATA.md` |
| `tools/audit.js` | 52 条规则；49/50/51 本轮新增（钩子句 ×2、共同体 ×1）,52 随 v152 搜索别名表加 |
| `docs/DEPLOY-CN.md` | **已按实测重写**。第七节记了「这份文档改过什么」，加内容前先读 |
| `docs/DATA.md` | 新增「台湾相关表述的口径」一节 + `q`/`qh` 子表 + `UNIONS` |
| `docs/DESIGN.md` | 新增：色带标签定位（v145/v147）、共同体层、跨日界线 |
| `docs/IDEAS.md` | 001 搜索/故事颗粒（**层一已上线 v152**,层二三仍待实验结论）；002 共同体（**已收口**） |
| `docs/TANG-EXPERIMENT.md` | 六页实验的判据与边界——**结果出来前不加功能** |

### 长期约定（不在本文，去这些地方找）

- 仓库 `CLAUDE.md`：数据改动工作流、铁律（双语／颜色／不引入外部资源／VIDEO 人工核实）
- `docs/ARCH.md`：数据迁出架构（Phase 1–4 已完成）
