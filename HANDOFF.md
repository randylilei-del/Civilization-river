# Handoff — 文明长河 / civilization-river
更新时间：2026-08-07（v54 已 commit，**未 push**）

> 给下一个 Claude 会话：先读本文件 + CLAUDE.md 即可继续，无需原对话记录。
> 上线地址 https://civilization-river.vercel.app ；仓库实名是大写 C 的 `Civilization-river`。

## 任务目标

给 7 岁儿子做的世界文明兴衰交互可视化（单文件 PWA，已上线）。本轮四条主线：
① 并存政权 `co` 铺完 ② 补缺失的同期强邻色带 ③ 时间轴缩放的输入方式与可发现性 ④ 卡片地图取景。

## 当前状态

**127 文明 · 281 人物卡 · 六问 106/127 · co 14 张卡 102 条目 · 大事记 934 · audit 28 条规则 · index.html 1.43 MB**

**接手时先 `git status` / `git log` 核实。** 写这份文件时：工作区干净，本地领先 origin/main **4 个 commit**。

### ⚠️ 待办第一件：push

本机 settings 拦 Claude 的 push，**必须 Ray 自己跑**：

`cd "/Users/ray/cc projects/17-civilization-river" && git push`

推完 Vercel 自动部署。未推的四个 commit：

| commit | 内容 |
|---|---|
| `4f5a714` v51 | 并存政权 co 铺完 8 条候选（7→14 张卡、102 条目）|
| `91c2e30` v52 | 补 8 条缺失色带（119→127）+ 两轮核查修正 8 硬错 49 口径 |
| `515da8a` v53 | 时间轴缩放补三种输入 + 可发现性 |
| `e104742` v54 | 卡片地图自适应取景 |

## 本轮做完的事

**① co 铺完**（v51）。8 条候选全做，印度河文明有意不做（哈拉帕是否算「政权」学界无定论，卡片正文已写「文字至今未解读」，再标 co 会自相矛盾）。

**② 补 8 条色带**（v52）。起因是 Ray 指出唐同期的吐蕃没有体现，并判断这是代表性问题——**判断成立且比一条吐蕃严重**。

**③ 缩放**（v53）：`zoomTo()` 本来就有，缺的是输入方式和提示。

**④ 地图取景**（v54）：查文文化那张认不出在南美洲哪里，是系统性问题。

## 进行中 / 下一步

**没有半成品。** 下面是待办，按 Ray 关心的「功能进度」组织：

| 功能 | 进度 | 卡在哪 |
|---|---|---|
| 补人物 | 完成 | — |
| 并存政权 co | **完成** | — |
| 兴衰六问 | 83%（106/127）| **剩 13 条在世文明等 Ray 定第⑤问口径；另新增的 8 条色带六问已写全** |
| 补缺失色带 | 第一批+第三批完成（8 条）| **第二批 3 条与高句丽未做，见下** |
| 补 PEAK 年份 | 80% | 11 条脱离 Taagepera 序列，等 Ray 定原则 |
| 拆分粗颗粒色带 | 未动 | 日本 1425 年一条、朝鲜半岛 1597 年一条，见「机制二」 |
| 成就弹出卡 | 0% | **Ray 提的需求，动手前必须先过设计** |

### 缺失政权清单（完整版在 `/private/tmp/.../scratchpad/missing-polities.md`，会被清理，要点抄在这里）

盘查结论：**八条泳道都没有 >80 年的时间空洞，缺的不是时间而是泳道内的地理**。泳道是区域筐，站里讲每个筐只跟一条主线走，筐里另一个子区域可以黑几百年而时间轴看着是满的。

选条目的判据（比「存续多久」更硬）：**站里已经在讲这件事，但主角不在图上**。

**已做（v52）**：吐蕃、渤海国、马穆鲁克、阿散蒂、南诏·大理、法蒂玛王朝、维查耶纳伽尔、马拉塔

**未做，建议顺序**：
1. 第二批（补最大地理空白，各填一千年/五百年/八百年）：**埃塞俄比亚所罗门王朝**（1270–1974，非洲之角从阿克苏姆 940 年结束到 1957 年空了一千年）、**奇穆**（900–1470，站里瓦里·蒂瓦纳科 1000 年结束后直接跳印加 1438）、**密西西比文化·卡霍基亚**（800–1600，**北美在「美国 1776」之前全站为零**）
2. **高句丽**（前37–668）——⚠️ 它压在「朝鲜半岛 313–1910」里，加色带等于动机制二，**要先决定那条拆不拆**
3. 再往后：波罗王朝、后倭马亚、哈布斯堡、波兰-立陶宛、瑞典帝国、蒲甘、卡涅姆-博尔努、阿尤布、莫切

**加一条新色带的成本**：`CIVS`（含六问双语 6 键，主要工作量）+ `EN.civ` + `CHRONO`/`EN.chrono` + `GEO` + `PLACE`。照西夏（`n:'西夏'`）抄结构。**加完先跑 audit，它会逐条告诉你还缺哪张附表**——这是本轮最省事的一招。

### 机制二：一条色带包上千年（本轮没动）

跨度 >600 年的单条色带 30 条。与中国史（28 条覆盖 4000 年）最不对称的：日本 600–2025（1425 年）、朝鲜半岛 313–1910（1597 年）、大越 938–1884（946 年）、中世纪诸王朝 540–1206（666 年）。

**不建议轻易动**：拆分要改既有条目的 `n` 键，而 `GEO`/`PEAK`/`CHRONO`/`GL`/`VIDEO`/`PEOPLE` 全按名索引，牵连面远大于新增。另见坑 ⑧。

## 阻塞项 / 等 Ray 决定

1. **push**（见上，最优先）
2. **吐蕃、渤海国挂在「草原游牧」文明圈**——两个核查员都点了。图例字面写「草原游牧」，而吐蕃卡自己说「河谷青稞农业加高原畜牧」、渤海卡说「制度几乎照搬唐朝」。站里 `金`/`元` 也这么挂（该字段实际被当「非汉族政权」用），但 7 岁孩子只会照字面理解。改文明圈会动配色与图例语义，**没自作主张**。可选：改 `s` / 改图例文案 / 维持现状
3. 六问最后 13 条在世文明的第⑤问口径（「为什么衰落」对它们不成立）
4. PEAK 11 条脱离 Taagepera 序列的处理原则（三选一）
5. 成就弹出卡的设计（与 GL 鼎盛区间的内容重复问题）
6. **320px 老手机上 9 张 co 卡溢出**（其中 7 张是既有老卡）。根因是 `.co-i` 的 `white-space:nowrap`，属 DESIGN.md 设计决策。目标设备 iPad 竖屏 panel 有 599px，不受影响。要不要为 320px 改设计是 Ray 的决定
7. **吐蕃 × 渤海国时间轴标签重叠**——Ray 已选择接受；v53 的滚轮缩放能解开它，实际影响已变小

## 试过但失败的路（本轮新增）

**① 长字符串当锚点做替换，必失配**
改核查意见时用整段中英文本做 python 替换锚点，因破折号、转义、全半角反复匹配失败。**改用行号定位或短锚点。** 这是 HANDOFF 坑 ⑥ 的同类，又犯了一次。

**② python 脚本 assert 失败会整批不落盘**
一批 15 处替换，第 12 处锚点失配抛异常，脚本在 `write()` 之前中断，**前 11 处全部丢失**，而终端已打印 11 个 ✓。差点据此认为改完了。**对策：失配改成打印跳过而非 assert，或把 write 放进 try/finally。**

**③ 测试脚本自己过期，制造假警报（两次）**
- 测「吐蕃在时间轴上有没有标签」得 false，以为踩了坑 ⑧。实际是脚本停在英文模式却搜中文名。
- 测滚轮缩放得「失效」。实际是先取了 `rect`、又跑 254 次 `openCiv` 导致页面滚动，坐标过期。
**改渲染/交互的验证脚本，每次都要重新取 rect、并显式设定语言。**

**④ 窄屏测量漏了滚动条，结论偏乐观**
先前报「co 卡 360px 溢出 0」，实际面板有 17px 竖滚动条、可用宽度是 287px 不是 302px，法兰克那条 296px 一直是超的。**量 flex 容器宽度时必须确认滚动条状态。**

**⑤ 修一个溢出撞出另一个**
给西哥特补「图卢兹→托莱多」后英文项 322px > 容器 302px。换连接词没省下宽度（问题在名字本身）。最终把迁都事实放进组 `x`（`.co-x` 是块级会折行，无宽度风险），`c` 保持「托莱多」。

**⑥ Service Worker 缓存让我测到旧数据**
改完刷新，测出的仍是改前的值。**验证前先清 SW + caches 或加 query 参数。**

**⑦ 上一轮的坑仍然有效**：`color-mix` 陷阱（v48，全站已无，别再引入）· 改渲染必须量化验证不能看截图 · 测颜色用 canvas 取像素不用 `getComputedStyle` · 模板字符串里别写含 `${}` 的正则 · 英文名里的撇号必须转义 · 拆分色带前先算宽度（坑 ⑧）· 手工 grep 清点引用会漏，用 `tools/refs.js`

## 内容质量的硬教训（本轮两轮核查共核出 8 硬错 + 49 口径）

- **「最早/第一/唯一」类断言最危险**：马拉塔「印度本土政权最后一次到达阿托克」被 1813 年锡克帝国一个反例直接推翻。这类话写得像知识点彩蛋，最容易被孩子记住。
- **精确数字挂错锚点**：渤海「围上京十余日」——《辽史》丙寅围城、己巳请降，实为三日，方向相反。
- **两处出处拼成一句**：伊本·赫勒敦「从蒙古兵锋下逃来」——他 1382 年自突尼斯赴开罗，差了八十余年。
- **一个字段写对、另一个写反**：维查耶纳伽尔的 `born`/`fall` 都写「北方/德干诸苏丹国」，只有 `b` 写成「德里苏丹国」。**改一处内容要顺带看同卡其它字段。**
- **译名不是风格问题是功能问题**：`f['人物']` 会渲染成维基搜索链接，「格图兹」这种编造译名 = 坏链。同一战役站内两种译法（艾因贾鲁特/阿音扎鲁特）也会导致搜不到。
- **敏感题目里一个字母的差别**：渤海英文写 `king of Goryeo` 会指向 918 年才建立的高丽王朝，在讲族属争议的卡上等于倒向一方。应为 `Goguryeo`。
- **热门说法可能已被推翻**：长白山大喷发已测定在 946 年、晚于渤海亡国 20 年，是被时间线**排除**，不是「证据不足」。

## 关键上下文

**验证流程（每次改完必跑，顺序固定）**
```
sed -n '/<script>/,/<\/script>/p' index.html | sed '1d;$d' > /tmp/c.js && node --check /tmp/c.js
node tools/audit.js          # 28 条规则；新增色带时它会逐条报还缺哪张附表
node tools/refs.js <文明名>   # 拆分/改名前必跑
python3 -m http.server 8777 --bind 127.0.0.1 --directory "<项目路径>"   # 后台任务方式起
# 浏览器全量扫描(claude-in-chrome;本机无 Playwright,file:// 被拒):
#   127 × 中英 = 254 张,查 undefined/NaN/[object/null/空括号
#   co 卡还要在 375/360px 宽度下查 .co-i 溢出(注意扣掉 17px 滚动条)
#   改渲染时要量化:canvas 取 1×1 像素算对比度,别信 getComputedStyle
```

**数据结构速查**
- `q` 兴衰六问：`{born,rule,money,power,fall,legacy}` 每键 `[zh,en]`，六键必须齐全
- `co` 并存政权 / `ct` 标题覆盖：规范已写进 `docs/DATA.md`（v51 补的）。要点：**先后接力而非并存时必须换 `ct`**；**组里只有一个成员时不要给 `m`**；接力关系里不要给 `m`（南诏那次就是错的）
- `PEOPLE`：`yk:'r'`=在位、`yk:'a'`=活跃。非君主一律 `'a'`
- 颜色：`--c-<圈>` / `-a`（浅档）/ `-b`（深档），深浅两套写死十六进制。**不要用 color-mix**
- 新色带的影响力标定参照：唐 1.0 · 阿拉伯哈里发 1.0 · 奥斯曼 .9 · 莫卧儿 .9 · 北宋 .75 · 突厥 .6 · 吐蕃 .55 · 马拉塔 .55 · 马穆鲁克 .55 · 伊尔汗国 .5 · 辽 .4 · 回鹘 .35 · 阿散蒂 .35 · 渤海国 .3 · 西夏 .25

**缩放相关（v53 新增，改交互前必读）**
- 标签位置由渲染器取影响力最大点（`index.html:5702-5705`），标签重叠是数据决定的，改不掉除非动峰值年
- 全景横轴是**非线性分段**（`BP` 断点表），已缩放后是线性。跨这两种状态做坐标换算必踩坑
- 滚轮只在时间轴条内响应，是有意的（见 commit 说明）

**内容撰写与核查流程（本轮验证有效）**
1. 导出批次数据（含现有卡片内容，防与既有内容矛盾）
2. 写规范文件，把前几批被抓过的错型写成自查清单
3. 派撰写 agent（多批并行），产出 JSON
4. 合并脚本校验后插入
5. **派 adversarial-verifier 独立核查——只给成品和判定依据，绝不给撰写者的自我报告**
6. **量大时拆成两个核查员**（本轮 8 条拆两组，它们各自补上了对方的盲区；单个 agent 会摊薄）
7. 逐条核实核查意见，站不住的反驳，修正
8. 全量渲染扫描 → commit

**Ray 的口径偏好（累积确认）**
- 客观事实 > 因果推论；因果内容必须「主流共识 + 多因 + 争议标注」
- 人物卡 `s` 段只写可核查的成就与地位，不写「他改变了历史」这类
- 新补人物：帝王将相不超过一半，有实质事迹的女性主动补
- 拿不准写区间，查不到就留空，**不编**
- **汇报只说「哪个功能、走到几成、有没有卡住他」**——中间的核查细节自己消化
- spec 他不细看，评审点放在「做出来的实际卡片」上
- 数据量大的批次必然有错，主动核查并说出来，比假装没问题有用
