# Handoff — 文明长河（17-civilization-river）
更新时间：2026-08-18 20:30

## 这是什么

给 7 岁的 Jasper（在上海用 iPad）做的世界文明兴衰可视化，**单文件 PWA**：全部代码、数据、海岸线都在 `index.html` 里，无构建、无后端。三个视角——**按时间看**（泳道图）、**按地方看**（世界地图 + 城市面板）、**按列表看**（172 行 × 9 列的表）。

## 当前状态（2026-08-18；接手先 `node tools/check.js` + `git log origin/main..HEAD` 核实）

- `HEAD` 见 `git log -1`（v261 + 若干核查修正）。工作区干净，`check.js` 全通过（audit 7 条 warn 是既有小段重合，不影响退出码；smoke 标签重叠 5 处基线冻结在 `OVERLAP_BASELINE`）
- **9 泳道 · 172 文明 · 33 条轨迹 · 51 张成就卡 · 127 个城市点 · 30 张数据表 · audit 规则至 72**
- 城市断层：9/127 座仍有 >150 年空档（断层总数 12）——起点是 32 座 / 41 处
- **未 push**：v249 起的全部 commit 在 Ray 手里，不要反复试

### 2026-08-18 这一天做了什么（v249 → v261，另一条线）

**Ray 提出「能不能把检查规则提炼出来并自动化，别每次靠人点」→ 一整套工具链落地：**

| 工具 | 干什么 | 一句话用法 |
|---|---|---|
| `tools/check.js` | 改完一条命令 = build 一致性 → audit → 语法 → lint-content（只打印）→ smoke；pre-commit 跑 `--quick` | `node tools/check.js` |
| `tools/smoke.js` | headless 渲染层烟测（playwright-core + 本机 Chrome，首次 `npm install`）：四态零 pageerror/零站外请求、引导层真消失、344 卡零脏值 + 中英 class 直方图、254 城×行有正文、色带非黑、标签中心在带内、轨迹进视口、列表字号、iPad/手机入口 | 加断言先注入反例：`SMOKE_INDEX=<反例html> node tools/smoke.js` |
| `tools/depth.js` | **内容厚度体检**：172 带 × 12 维 / 127 城 × 8 维同组相对排名；核心层判薄，人物 0 / 成就卡 0 / 鼎盛非短段 <2 走绝对缺口，照片视频走素材缺口；期望随分量走（大档后 35% / 中 25% / 小 15%） | `node tools/depth.js` / `depth.js 唐` |
| `tools/context.js` | **写新内容前必跑**：一条命令摊开同卡 b/d/f/q/qh/GL/chrono/人物/成就卡 + 版图内各城小段 + 同城别带首句 + SETTLED + depth | `node tools/context.js 秦` / `context.js 香港` |
| `tools/lint-content.js` | 只扫 HEAD..工作区 diff 的 data 行，列最高级/现在时/大数字/因果词四类可疑句；check.js 顺带打印不计退出码 | `node tools/lint-content.js HEAD~1` |
| audit 规则 72 | 「中心」地名换算坐标做 PIP，招牌不能在自己版图外（上线第一跑抓到欧洲殖民非洲的阿尔及尔） | — |
| audit 规则 36 warn | 版图**新增命中**也报 warn（71 城基线一次性并入） | — |

**Ray 定的内容标准（写进 DESIGN.md「信息层级」）：卡片是博物馆，不是词条——每张卡要还原这段历史里值得记住的东西：有实物、有人、有一两个说清「为什么重要」的数字或场景。补的是展品不是字数。**

**「厚度第一批」13 条按此做完**（每条：context → 英文/中文材料打底 → 写 → check → commit → adversarial-verifier → 修正 commit）：秦 v249 / 罗马共和国 v250 / 波斯 v251 / 清 v252 / 蒙古帝国 v253 / 西周 v254 / 西葡 v255 / 奥斯曼 v256 / 特奥蒂瓦坎 v257 / 莫卧儿 v258 / 吠陀 v259 / 西晋 v260 / 香港 v261。十一轮核查约 25 条必修全采纳，其中挖出的**既有 bug**：秦大事记英文「七万/七十万」差十倍、清「1790 四大徽班」、四库种数两处打架、蒙古「各汗国都发纸币」、上都「元亡后毁」时序反、马略「将军发饷」等。**西方带先用英文材料打底再译中文**（Ray 定），中国带以中文一手为准。

**核查员反复挑到的模式（下一批写之前先看）**：①中英 hedge 不齐（「十来万」vs well over；「几十年」vs years）②GL 段抄 q（v250.1 起纪律：GL 讲「什么时候最好 / 什么时候失灵」，制度细节留给 q）③同一件事在同卡第三次讲（何尊「宅兹中国」、1453 巨炮）④译名两套并存（鲁布鲁克/鲁不鲁乞、巴耶济德/巴耶塞特、沙贾汉/沙贾汗、摩哈奇/莫哈奇——已统一）⑤最高级要么给出处要么「之一」。

### 这一轮做了什么（v234 → v248.1）

**A 类版图修正（改多边形）已枯竭**（v234/v235 收尾），此后是 **B 类新增色带**，一条带全套双语：

| 版 | 新带 | 清零的城 |
|---|---|---|
| v236 | 印度-希腊诸王 | 喀布尔 |
| v237 | 北元·蒙古诸部 | 乌兰巴托、哈拉和林 |
| v238 | 东察合台·叶尔羌 | 喀什、敦煌 |
| v239 | 加喜特巴比伦 | 巴格达、巴比伦 |
| v240 | 阿瓦·勃固 | 蒲甘、仰光 |
| v241 | 托尔特克与谷地诸邦 | 墨西哥城、特奥蒂瓦坎 |
| v242 | 伐卡塔卡与德干诸国 | 孟买、汉皮 |
| v243 | 巽伽与恒河诸国 | 德里、瓦拉纳西、巴特那 |
| v244 | 帕查卡马克与中部海岸 | **利马**（全站最大的洞，1615 年）|
| v245 | 努比亚诸王国 | **麦罗埃**（1300 年）|
| v246 | 弗里吉亚 | 安卡拉 |
| v247 | 勃兰登堡·普鲁士 | 柏林 |
| v248 | 扎格维王朝 | 阿克苏姆 |

五轮对抗核查，共 **31 条必修全部采纳**（v235.1 / v236.1 / v237.1+238.1+239.1 / v240.1~243.1 / v244.1~247.1）。

## ⚠ 下一条带开工前，先过这张自查表

**这张表是 2026-08-17 加的，理由：五轮核查里有一半必修是「上一轮判过 → 采纳 → 下一批原样复发」。再写一遍教训没用，要在动手前逐条对。**

| # | 复发过几次 | 开工前问自己 |
|---|---|---|
| 1 | **4 次**（坎大哈 / 别失八里 / 法拉斯 / 西里西亚） | **这条带的 `f['成就']`、`f['中心']`、`q` 里点名的每一个地方，是不是都落在我画的多边形里？** 招牌不能在自己边界外 |
| 2 | **3 次**（乌兰巴托 / 仰光+墨西哥城 / 利马+麦罗埃+柏林+安卡拉） | **`place_lore` 是不是在讲「那个朝代这个地方是什么样」，而不是把色带卡的话再说一遍？** 现在有 audit 规则 71 报 warn（`node tools/audit.js` 输出末尾的 ⚠ 段），写完跑一次 |
| 3 | **8 次** | 数据层有没有 `**markdown**`？——`node tools/newband.js <band.js>` 会在插入前拦，别跳过它 |
| 4 | **2 次**（v237 / v239） | 插 `civs.js` 的锚点，取的是**条目开头**还是条目中间？取中间会把既有条目劈成两半 |
| 5 | **2 次**（v237 / v241） | 注入反例之后，**先确认探针点真的落进了多边形**，再看 audit 响不响。幅度不够 = 探针没验证过 |
| 6 | **2 次**（v240.1 判过、v247 复发） | 这条带的头号成就有没有现成照片挂在**别的带**下面？（`civ_photo.js`） |
| 7 | **3 次**（v236.1 / v239.1 / v244.1） | 有没有写「最早的」「最大的」这类最高级？站内同型的对冲写法是「之一」「同类更早就有」 |
| 8 | **2 次**（v241 / v246） | CHANGELOG 里写的「清零」「剩 N 年」，**是不是按 k>0.02 口径真算过**？量化自述没人查，而它是下一轮决策的依据 |
| 9 | 1 次（v237.1 指出） | 新写的专名，站内是不是已有另一种译法？（阿马尔那/阿马尔纳、戈尔迪翁/戈尔迪乌姆） |
| 10 | 1 次（v244.1） | PEAK 给数字之前先问：**这个政权有没有公开统计？**有就别估（普鲁士那次是「猜了一个不需要猜的数字」）；真查不到才给示意值 + `as` 标注 |

**新带的固定流程**（工具都已进仓库，不在 scratchpad 里）：`node tools/newband.js <band.js>` 预检 → 插 civs/geo/chrono/gl（锚点取条目开头）→ `node tools/check.js` → 补 PLACE / PEAK 或 PEAK_NA → 写每城小段（写完跑规则 71）→ 补基线与 NOT_IN 探针 → **注入反例并确认探针点真落进去了** → CHANGELOG（量化自述要真算）→ commit → 派 adversarial-verifier。

## 下一步

### 立刻能做的（不需要 Ray）

**厚度线（`node tools/depth.js` 出清单，按边界清楚→模糊）**：
0. 先看这两处待统一的既有数字：清 乾隆卡「约一千三百万平方公里」vs q.power「约 1470 万」；蒙古 成吉思汗卡「3300 万」vs 成就卡「2400 万」；traces.js 十二表法「刻在铜表上」断言语气 + 年份 -450 vs chrono -451
1. **24 条无人物**（每条 1–3 张人物卡；FIGURES_NA 留空名单在 audit.js）
2. **11 条大档无成就卡**（西葡、俄罗斯·苏联、波斯、奥斯曼、秦、西汉、元、明、清、共和国、美国；每张要查证，一轮 5–10 张，schema 见 achv.js）
3. **35 条鼎盛不足**（大/中档非短段 <2；写前 `context.js <带>` 看同组参照）
4. 再按正文薄清单：孔雀王朝、俄罗斯·苏联…（`depth.js 25`）

**断层线**：


**先跑 `node tools/gap.js`** 拿最新名单（补完再跑一次复核，别凭印象写"清零"——那个错犯过两次）；查单城用 `node tools/gap.js 150 <城市名>`，它会顺带按「时间重叠 + 中心点距离」列出近处候选带，**不用外接框**（原因见该文件头）。

剩余 9 座城 12 个断层，**全是单城带**：摩亨佐达罗（前1300—前550 / 375—622）、拉合尔 560—1206、加尔各答（前185—320 / 550—750）、拉萨（842—1206 / 1400—1636）、特拉布宗 前330—前27、大津巴布韦 1450—1650、罗马 1620—1815、卡霍基亚 1600—1776、胡志明市 627—802。

**两处查出来但没做的 A 类缺口**（记在 CHANGELOG 里）：
- **`印加` 多边形是一条沿安第斯山脊的窄带，整条太平洋海岸都在外面** —— 不只利马，昌昌与纳斯卡也不在内，而印加恰恰以「四方之地」统治了从厄瓜多尔到智利的整条海岸。**处理时要按整条海岸做，不是补一个利马。**
- 卡霍基亚 1600—1776 史实上是法属路易斯安那，而 `法兰西` 带只画欧洲本土 —— 要不要给它加北美殖民块，牵动整条带口径。

### band 文件长什么样（`tools/newband.js` 吃的就是这个）

```js
module.exports = {
  name: '带名',
  civ: `{ l:'泳道', s:'文明圈', v:色深, n:'带名', k:[[起年,0],…,[止年,0]], b:'一句话。',
  d:'中文叙述段',        // 缺了会静默回退到 b —— audit 规则 69 专门拦这个
  e:{ n:'English name', w:'维基消歧', b:…, d:…, f:{ 'Peak':…, 'Center':…, 'Figures':…, 'Legacy':… } },
  f:{ '鼎盛':…, '中心':…, '人物':…, '成就':… },   // 「成就」「人物」两格中英项数必须相等（按位置替换）
  qh:{ born:[中,英], rule:…, money:…, power:…, fall:…, legacy:… },   // 中 ≤30 字 / 英 ≤12 词
  q:{ born:[中,英], rule:…, money:…, power:…, fall:…, legacy:… } },`,
  geo:    `/* 注释：不画什么、为什么、断言清单 */\n'带名': { p:[[…]], c:[…] },`,
  chrono: `'带名': [[年,[中,英],[中,英]],…],`,
  gl:     `'带名': [\n  { a:, b:, k:'art|thought|tech|econ', t:[中,英], d:[中,英] },\n],`,
};
```

**插入时锚点取条目开头**（`{ l:'xx', s:'yy', v:n, n:'邻带名'`）——取条目中间会把既有条目劈成两半，build 当场语法错。这个错犯过两次。

### 有意不做的

- **A 类版图修正**已枯竭；剩下的候选（贵霜→瓦拉纳西/巴特那、嚈哒→摩亨佐达罗、中世纪诸王朝→拉合尔/加尔各答）**基线里的缺口经查是有意的分工**（加尔各答/达卡归波罗、金奈归朱罗），别再"修"。
- audit 规则 71 现有的 7 条 warn 全是既有小段（德黑兰|现代中东 56% 最高），按「清不完的旧问题用 warn」的纪律不强制清，但**新增的小段不许再进这张名单**。
- 文明照片仍空 15 条（v209 有意留空）；GL 描述 ≤20 字 83 段（v229 不机械追长度）；钩子提炼型 29 条（v232 判定这是钩子该有的样子）。

### Ray 手上的

- **iPad 实机验收**：五步引导、欢迎屏、卡片小地图国界、泳道名加重；顺带看这 13 条新带在 iPad 上读不读得动
- **`docs/TANG-EXPERIMENT.md` 那个实验** —— 卡着 IDEAS 001 层三 **和 IDEAS 003「探索模式」的落地页设计**两条线
- **导出 iPad 上的 `whviz-sq` 搜索记录**；**看 Jasper 点不点 `gvRand`**（零成本探针，见 IDEAS 003）
- **IDEAS 003 里要 Ray 判的**：把 `gvRand` 改成加权 + 提到全站常驻，算不算「加功能」
- 视频单子：`docs/VIDEO-CANDIDATES.md` 32 条候选待人工核 BV 号

## 试过但失败的路（新 session 请先读这节）

0. **【2026-08-16 晚间 · 加新色带这一轮的四条】**
   - **「读了原文却读错口径」在新带上又复发了三次**，audit 70 条规则一条都拦不住:巴布尔（父系帖木儿、只有母系是蒙兀儿，我写成「从这一带往南走」，而多边形明写不吞他的家乡——**图和文互相否定**）、达延汗（1480 是即位那年他八岁，统一在 1510，**而我自己的 k 曲线拐点就画在 1510**）、1516 那句（废的是罕东左卫不是沙州卫、闭关是 1528 不是 1516）。**对策：凡带年份或人物关系的断言，写之前回一手记载确认，不能从站内别处或印象里推演；写完回头看自己这条带的 k 曲线/多边形有没有跟正文打架。**
   - **重复的三种形态，我三条带各犯一种**:①**同城不同带**（苏萨故事讲到第四遍，我数漏了 `achv.js`）②**自己的 place_lore 抄自己的 q**（乌兰巴托那段前三分之二是 `q.legacy` 的逐字改写）③**图文不符**（别失八里写进速览「中心」却落在多边形外，坎大哈同型复发）。**扫重复要扫四个地方：place_lore / civs 的 q 与 qh / gl / achv，而且要把自己刚写的那份也算进去。**
   - **静默覆盖比报错危险**:往 `place.js` 加 `'哈拉和林'` 时那个键早就有了，JS 后写胜出，**「蒙古帝国」卡片的显示被无声改掉**，build/audit/语法/smoke 全绿。已加 **audit 规则 70** 查 data 表键唯一性。同类的还有 v236.1 的缺中文 `d`（渲染 `c.d || c.b` 静默回退）——**凡是「缺了会退回一个合法值」的地方，都不会被"零 undefined"那类自检抓到。**
   - **注入反例的幅度要先验证**:v241 我把东界拉到 -88 就以为够了，而奇琴伊察在 -88.57，压根没进多边形，探针当然不响——**而我已经把「实测报警」写进了 CHANGELOG**。写了没验证过的结论，比不写更糟。**注入之后先确认探针点真的落进去了，再看 audit 响不响。**

1. **【2026-08-16 下半场，最贵的三条】**
   - **「读了原文却框错了」是这个项目最顽固的错误类型，audit 69 条一条都拦不住。** 三个实例：《晋书》「在南三十年」是陶璜在南方任官的**总数**（跨吴晋两朝），我引用了后半句「举州号哭」却把前半句口径读反，凭空造出二十年；凉州会盟「蒙古不派驻军」是我从 `蒙古帝国 q.rule` **推演**出来的条款、查无依据，而实际议定的含括户、驿站、达鲁花赤，方向正好相反（核查员判词：**不是数字错，是给 7 岁孩子的历史模型错了**）；「亚历山大打到印度河就折返」——他最东到旁遮普的比阿斯河，远在印度河**以东**，而站内 `拉合尔|孔雀王朝` 早就写对了。**对策：凡带具体数字或具体条款的断言，写之前必须回到一手记载确认口径，不能从站内别处推演。**
   - **绕过自己设的约束。** v236 的 commit 写着「三段小段有意避开犍陀罗」，而**文明卡的 `qh.legacy` 抖得比小段还响**，直接断言「佛像的第一张脸在这一带想出来」——站内贵霜卡明写「谁先谁后至今没有定论」。**避让必须覆盖整张卡（q/qh/GL/chrono），不是只覆盖 place_lore。** 这是 v235.1「对策扩展为也读同卡 q/f」的反向复发。
   - **静默回退比报错更危险。** 新带只写了 `e.d` 没写中文 `d`，而渲染是 `c.d || c.b`——不报错、不留白、不产生 undefined，所以「N 卡零 undefined/NaN」这条自检**结构上就抓不到**，结果是中文读者比英文读者整整少一段。同一漏洞在下一条带当场复发 → 已加 **audit 规则 69**。**一次是手滑，两次是模板缺陷，该加规则而不是改数据。**

1. **写新内容前必须先读同卡/同城的既有字段** —— 这条现在要扩展成**三层**：同城其他年代的 `place_lore`、**同卡自己的 `q`/`f`/`GL`/`chrono`**、以及**同城不同带**的段落（「亚历山大」那条正确表述就在 `拉合尔|孔雀王朝`，属于第三层，前两层兜不住）。

X. **（原第 1 条）写新内容前必须先读同卡/同城的既有字段** —— **这一天犯了四次**，是本 session 最贵的教训。核查员原话：12 条问题里 7 条是「站内既有字段本来写对了，新文案写错或把对冲删掉了」。具体：奥斯曼 `q.money` 没说「最大」而 GL 写了「国库最大的一笔」；印加 `q.power` 写「一日数百里」而 GL 写「一天八百公里」；美国 `q.power` 写「工业产出」而 GL 写成总产出；高棉 `b` 有「一般被认为是」而 GL 删掉；大英 `chrono` 写「加冕礼」而 GL 写成「称呼就是那时来的」；**长船成就卡把 v212.1 已经裁决并改对的「维京船博物馆闭馆中」改回了「今天在奥斯陆能看到实物」**。这类错 audit 一条都拦不住。**动笔前把该 civ 的 `q` / `chrono` / 该城的 `place_lore` 打印出来放在手边，成本极低。**
2. **`display` 会盖掉 `[hidden]`** —— 给元素写了 `display:flex/inline-flex/grid` 之后，`hidden` 属性与 `el.hidden = true` 全部失效，而 JS 读到的 `el.hidden` **仍是 true**，任何「查属性」的测试都会通过。v151 的 `#firstHint` 从做出来那天起点了 × 就关不掉；v226 的欢迎屏同样中招，是 Ray 在 iPad 上看出来的。已加 audit 规则 67。**验可见性要用 `getBoundingClientRect` + `getComputedStyle`，不能只读属性。**
3. **「先清零再加规则，清不完就不加」** —— 这一天用了四次（`DRATIO_TODO` 14 条、GL 描述 51 段、规则 68 的 22 条、B 批钩子 43 条）。加了修不完的阈值规则只会变成长期红灯，红灯久了就没人看。B 批只改了 100% 重合的 14 条、**没有为剩下 29 条加规则**，是有意的。
4. **写断言比画坐标更容易错** —— 版图断言这两轮写错五次（北京属燕、成都属蜀、西安是秦核心、马德里本身是穆斯林建的堡垒、罗马属拉丁西欧），全部由脚本当场纠正。**「写断言 → 脚本迭代」这套流程拦的不只是坐标画歪，也拦写断言的人记错。**
5. **补画版图会顺带吞掉不该吞的** —— v230 为纳入伊比利亚把格拉纳达也吞了（纳斯里德到 1492 才亡，而站内非斯段自己写着这一年），为纳入拉萨把和田也吞了。核查原话：**探针「写在了没出问题的方向上」**。补画之后要专门给「刚扩到的边缘」加反向探针。
6. **手抄原文去匹配替换，必错** —— 改 `data/*.js` 时两次手抄英文原文都因为记错而失败。改用**中文定位 + 程序化扫描到未转义的收尾引号 + 整段替换**。但注意：整段替换意味着中英是分别新写的，**收尾要把中英并排读一遍**——本轮 3 处中英不同义全出在这个环节。
7. **中英混排是高频手滑** —— 一天犯四次（`rise持续ly`、`it was那 that`、`no外 hand`、`天然珍珠business`）。audit 有规则拦，但写完自己先扫一遍更省事。
8. **千位分隔符是分项字段里的禁用字符** —— 拉帕努伊的英文成就写 `a settlement 3,500 km`，而 `NSPLIT` 认逗号，这一项在英文界面上被劈成两半。已加进规则 68。
9. **audit 全绿不是内容质量证据** —— 68 条规则全是结构性的。史实错误、中英不同义、跨条目矛盾、渲染层键名失配、图文不一致，这几类结构上都拦不住。「N 卡零 undefined/NaN」同样查不出「少渲染一行」——少一行不产生 undefined。
10. **别用改数据去解决渲染问题** —— v219 为让色带拿到标签把 `k` 曲线末帧从 0 改成 .26，违反 DATA.md 明文约定。核查定性：**等同于为了让测试通过而放宽断言**。后来发现真正起作用的是另一处有依据的改动，末帧改动纯属多余。
11. **合成事件测不出事件顺序类 bug**（v199.1）：交互改动要用真点击再走一遍。
12. **Commons 缩略图对站外只出标准档**：960/1280 有，800/640/320 全 404。
13. **维基条目首图不能盲用**：每批都要 contact sheet 人眼过一遍。
14. **rule 36 基线可能在维护正确的有意之举**（v208）：**改版图前先看那座城|那条带有没有既有小段——有小段 = 很可能是有意的。**
15. **横向核查最省力的切口是「时间断层」**，但**外接框筛出的候选大半是误报**（汤加帝国的多边形横跨太平洋、大英帝国横跨全球、西汉框住印度四城），必须人工筛。
16. **大百科搜索词规则的坑**：多字优先、只去标点。

## 关键上下文

### 改完必跑（顺序）
```
node tools/build.js      # 改 data/*.js 之后必跑
node tools/check.js      # 2026-08-16 起一条命令 = build 一致性 → audit → 语法 → headless smoke;--quick 跳过 smoke
```
smoke（`tools/smoke.js`，playwright-core + 本机 Chrome，首次 `npm install`）已把下面「浏览器全量验证」那些手粘脚本固化成断言，pre-commit 会自动跑 `--quick`。人眼仍要看的：iPad 手感、史实、中英同义、图片内容。
⚠ `node tools/audit.js | tail` 会让 `$?` 变成 tail 的退出码——要看真实退出码就别接管道。

### 浏览器全量验证
起服务：`python3 -m http.server 8777 --bind 127.0.0.1 --directory <项目路径>`（后台任务方式），claude-in-chrome 访问 `http://127.0.0.1:8777/index.html`。
- **文明卡**：159 × 中英 = 318 卡查 `undefined/NaN/[object`
- **城市面板逐行扫空行**：127 城 × `gvQuery(lon,lat,ci)` + `gvDetail(i)`，正文 <20 字即空行
- **可见性必须量真实盒子**：`getBoundingClientRect()` + `getComputedStyle().display`，别读 `el.hidden`
- ⚠ 后台标签页里 rAF 不跑、smooth scroll 不动——用 `behavior:'auto'`

### ⚠ 断层诊断脚本的两个漏洞（2026-08-16 踩出来，复用前必读）

1. **候选脚本只找「外接框框住这座城、多边形没盖到」的带，「外接框根本没框住」的真画歪它一条都报不出来。** 敦煌 420—581 因此被误判成「站内根本没这段的带」，而南北朝那条带明明存在、只是西界没画到河西走廊。**换判据**：按「时间重叠 + 该带中心点距这座城 < 2000km」找候选，别用外接框。
2. **audit 规则 36 的城市基线只报「丢失」、不报「新增」** —— 而新增恰恰是「补画顺带吞掉不该吞的」那个病灶所在。**补画后必须自己把 230 城基线的全部新增列出来逐条确认**，audit 不会替你查。v234 靠这一步逮住了西宁（吐谷浑）和昆明（爨氏）两处误吞。
3. **注入反例时幅度不够，探针不会响。** v237 第一次注入没触发，加大幅度把探针点真正圈进去才报警。**探针没验证过等于没加。**

### 断层诊断流程（本轮用的，可复用）
1. 对每座 `GEO_CITY` 逐城求覆盖空档 >150 年 → 待办城市名单
2. 对每个空档，找「时间上能填、外接框又框住这座城、却没被多边形盖到」的带 → 候选
3. **人工筛**（外接框误报占大半）→ 逐条查史实 → 查 `place_lore` 有没有既有小段
4. 写断言 → 脚本迭代坐标到全过 → 落 `data/geo.js`（**顶上写注释：不画什么、为什么、断言清单**）→ 补 audit 正反探针 → **注入反例实测探针会响** → 127 城逐行扫新出现的空行 → 补小段

### 改版图的固定流程（v188 立，v227/v230 各用一遍）
写断言（在内 / 在外的城市清单）→ 脚本迭代 → 落 `data/geo.js` + 注释 → **补正向 LANDMARKS + 反向 NOT_IN 探针** → **反向注入实测探针真的会响** → 检查规则 36 基线 → **看那座城|那条带有没有既有小段** → 新出现的空行补小段

### 给孩子的新内容的固定流程
**先读同卡/同城既有字段** → 写作 → build/audit/语法 → 浏览器验证 → commit → **派 adversarial-verifier**（给它：commit 号 + schema + 判定依据 + 我自己的存疑点清单；**不给完成报告**）→ 逐条裁决（**包括核它给的改法**）→ 修正 commit

### 本机环境的坑
- **`git push` 被权限拦**（Ray 手动推）；`curl` 出网被拦；`timeout` 不存在。**Python `urllib` 出网可以**。
- B 站视频核实：`api.bilibili.com/x/web-interface/view?bvid=BV...`（带 UA / Referer，1s 节流）。
- CDP 超时不等于页面死了：长脚本会 45s 超时，拆小 + 同步写法即可。

### 城市面板的六层内容（别写串了）
| 层 | 表 | 回答什么 |
|---|---|---|
| 总结段 | `city_lore.js` | 这座城**为什么在这里** |
| 古称 | `city_names.js` | 带年代 |
| 古迹 | `city_sites.js` | 今天还看得见的实物 |
| 照片 | `city_photo.js` | 点了才加载；每城 ≤3 |
| 地点小段 | `place_lore.js` | 键「城市\|文明」 |
| 视频 | `city_video.js` | 全是搬运稿件非官方号 |

### 新 session 开工前的三件事
1. `node tools/check.js` + `git log origin/main..HEAD` 核实上面的状态（**这一步别省**：本文件的数字是 2026-08-17 的快照，仓库可能已经动过）
2. `node tools/gap.js` 拿最新的断层名单——本文件写的「9 座城 12 处」也是快照
3. **读本文件的「⚠ 下一条带开工前，先过这张自查表」那一节。** 五轮核查里有一半必修是「上一轮判过 → 采纳 → 下一批原样复发」，那张表就是为这个加的
4. 想看细节再去 `docs/CHANGELOG.md`：v226.1（`display` 盖 `hidden`）、v229.1+v233.1（新内容覆盖站内既有正确表述，一天犯四次）、v244.1~v247.1（招牌落在自己边界外 + 猜了一个不需要猜的数字）
5. 每做完一条带就派 adversarial-verifier（给它 commit 号 + 判定依据 + 我的存疑点，**不给我自己的完成报告**）

### 关键文件
| 文件 | 要点 |
|---|---|
| `index.html` | 三个视角；`linkAchv`/`linkNames` **按位置**把英文换回中文键；`tourStart`/`twStart` 是引导层；`.tour-welcome[hidden]` 那条 CSS 别删 |
| `tools/audit.js` | 68 条规则；64=中心键渲染一致、65=中英长度比、66=版图面积数量级、67=display 盖 hidden、68=分项字段中英项数 |
| `data/gl.js` | 鼎盛区间；**注意 `gl` 可能写在 `civs.js` 的条目里**（战国就是），改的时候两个文件都要找 |
| `docs/CHANGELOG.md` | 每版详录，**判断依据都在这里** |
| `docs/IDEAS.md` | 001 的层二层三等着 `tang.html` 的结论 |
