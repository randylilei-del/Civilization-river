# Handoff — 文明长河（17-civilization-river）
更新时间：2026-08-16 01:10

## 这是什么

给 7 岁的 Jasper（在上海用 iPad）做的世界文明兴衰可视化，**单文件 PWA**：全部代码、数据、海岸线都在 `index.html` 里，无构建、无后端。三个视角——**按时间看**（泳道图）、**按地方看**（世界地图 + 城市面板）、**列表**（v207 新加，150 行 × 9 列的表）。

## 当前状态（2026-08-16 01:10 实测；接手先 `git status` / `git log origin/main..HEAD` 核实）

- `HEAD` 见 `git log -1`（v214 起）。**本地领先远端一批 commit（v208–v214），Ray 说统一 push**；push 在 Ray 手里，不要反复试。
- 工作区仅 HANDOFF.md 未提交
- **9 泳道 · 155 文明 · 29 条轨迹 · 127 个城市点 · 30 张数据表 · audit 规则至 63**
- 照片层：城市 126/127 座 226 张（巴特那空）· 文明 145/155 条 288 张（11 条空）
- 视频：文明 32 条 · 城市 11 座
- 地点小段：127 城 × 全部文明行 **零空行**（v203 起）

### 2026-08-15 全天（v199–v212）做了什么

| 版 | 内容 |
|---|---|
| v199–199.1 | 地图视角：选中即收成窄带（内容拿到 77% 屏）+ 选中标记醒目；修「点了收不住」（click 反弹） |
| v200–201.1 | 南马都尔 + 汤加王国两条大洋洲色带；古迹表补齐 81→124 座；两轮对抗核查修正（含 `peakHTML` a<1 万 km² 渲染成 0 的边界 bug） |
| v202–202.2 | **城市照片**（Wikimedia Commons，点了才加载；点图放大；点空白收回） |
| v203–203.1 | 119 行空白小段全部补上；辽南界 / 北宋北缘两处版图错 |
| v204 / v209 | **文明照片**两批（速览四格之下） |
| v205 | 选传播轨迹时竖向也滚到站点所在泳道 |
| v206 | 大百科搜索词「压成词条」（法显访印→法显） |
| v207–207.2 | **列表视角** + 「中心」列城市链接到地图（列表与文明卡两处） |
| v208 | 横向核查：7 处版图错（东晋·十六国不盖北方等）+ 13 段小段 |
| v210 | Ray 第二批文明视频 13 条（BV 全部接口核实） |
| v211 | **现代欧洲诸国**色带（第 151 条）——七座欧洲名城近两百年不再空白 |
| v212 | **维京·北欧**色带 + 「维京扩张」轨迹 + 奥斯陆 / 哥本哈根城市点 |
| v213 | **斯里兰卡诸王国**色带 + 锡兰块补进英属印度 |
| v214 | **蒙古国**、**朝鲜**两条现代带 |
| v211.1 / 212.1 / 213.1 | 三批对抗核查修正（欧元 21 国、维京船博物馆闭馆改建、波罗迦罗摩海堤长 …） |

## 下一步

### 正在跑 / 刚跑完的
- v208+v211、v212、v213 三批核查已修完（v211.1 / v212.1 / v213.1）。**v214（蒙古国 + 朝鲜）的核查员在跑**；若这个 session 没接上，下一个 session 先 `git show 88e4f8a -- data/` 自己过一遍年份与断言（现代题材数字尤其看「现在还成立吗」）。

### 横向核查（v208 前做的）剩下的缺口，按性价比排
1. ~~斯里兰卡~~（v213 已做）；~~蒙古现代 / 朝鲜~~（v214 已做；平壤 1910—1945 仍空，日本带只画本土是有意的）。
3. **阿富汗**（喀布尔 1738—2025：杜兰尼 / 现代阿富汗）、**摩洛哥**（非斯 1258—1918）、**海湾**（迪拜 1258—1918）。
4. 奥斯陆 1066—1815 空（没有卡尔马联盟 / 丹麦-挪威带）。
5. 人口流轨迹还欠：腓尼基与希腊的地中海殖民、日耳曼大迁徙、南岛语族向西到马达加斯加。
6. 文明照片仍空 10 条（匈奴 / 柔然 / 匈人 / 嚈哒 / 准噶尔 / 俄国治下中亚 / 西晋 / 渤海国 / 卡涅姆 / 加纳 / 拉皮塔）——要么真没实物，要么条目首图是地图；巴特那城市照片空。
7. 成就卡 119/152 条没有；文明视频 120/152 没有；城市视频 116/127 没有（`docs/VIDEO-CANDIDATES.md` 24 条候选待核）。

### Ray 手上的
- **iPad 实机验收**：今天他验了地图窄带、照片、列表、传播轨迹；还没验的是深色模式、英文界面、时光穿梭 playbar、共同体卡。
- 列表视角竖屏 9 列要横滑——他还没反馈是否要收起「一句话」列。

## 试过但失败的路（新 session 请先读这节）

1. **合成事件测不出事件顺序类 bug**（v199.1）：真手指点城市 = pointerdown → pointerup → click；「点窄带展开」用 click 监听会被同一次点击的 click 反弹回去。交互改动要用真点击再走一遍。
2. **Commons 缩略图对站外只出标准档**：960 / 1280 有，800 / 640 / 320 全 404；`loading="lazy"` 加在「点了才插进刚 unhide 的容器」的图上 Chrome 不触发加载。
3. **文明照片第一路（古迹表映射）对大朝代反而吃亏**——唐只拿到 2 张（怀圣寺光塔 / 大雁塔），因为只能拿到 127 城古迹里恰好标了那个朝代的；第二批（v209）靠手工点名补。
4. **维基条目首图不能盲用**：搜索会落到无关条目（「月亮金字塔」搜出 2026 年一起枪击案）；直取 `titles=` 也会拿到地图 / logo / 拼贴 / 人像——每批都要 contact sheet 人眼过一遍（做法：`_contact*.html` 7 列缩略图截屏）。
5. **rule 36 基线可能在维护错误现状，但也可能在维护正确的有意之举**：v208 我把「德意志盖住华沙 / 布拉格」当错改了，audit 报「基线覆盖丢失 + 两段小段永远展示不出来」才发现那是有意的（鼎盛版图取 1942 最大范围，小段写的就是 1939 占领）。**改版图前先看那座城|那条带有没有小段。**
6. **横向核查最省力的切口是「时间断层」**：按城市算从最早色带到 2025 的空档，一眼分出「多边形画歪」（一批城同一段空）和「缺色带」（一片地区同一段空）。脚本在 CHANGELOG v208 说明里，思路：`GEO_CITY × CIVS` 点在多边形内 → 排年份 → 找 >60 年空档。
7. **核查员的改法也要核**（v191 起的老规矩，本轮又中一次）：v203.1 它建议「1275 元军攻破潭州」——正确；但 v191 它建议的「比任何有人岛都离大陆远」是假断言。逐条核。
8. **写英文钩子先数词**（≤12）；写中文钩子别抄正文首句（audit 会拦，但拦到再改多一轮）。
9. **加城市点的连锁清单**：GEO_CITY → city_lore 总结段 → city_sites → city_photo → 每条命中文明的 place_lore → audit 基线（可选）。漏一环那座城点开就缺一层。
10. **大百科搜索词规则里踩过的坑**：单字动词「入」把「字母传入希腊」切成「字母传」；去尾连词把「郑和」切成「郑」；TRAIL 写成字符类把「亚历山大」削成「亚历山」——多字优先、只去标点。

## 关键上下文

### 改完必跑（顺序）
```
node tools/build.js      # 改 data/*.js 之后必跑(改完表头注释也要跑,v204 漏过一次)
node tools/audit.js      # 63 条规则,退出码非 0 即有问题
sed -n '/<script>/,/<\/script>/p' index.html | sed '1d;$d' > /tmp/c.js && node --check /tmp/c.js
```
⚠ `node tools/audit.js | tail` 会让 `$?` 变成 tail 的退出码——要看真实退出码就别接管道。

### 浏览器全量验证
起服务：`python3 -m http.server 8777 --bind 127.0.0.1 --directory <项目路径>`（后台任务方式），claude-in-chrome 访问 `http://127.0.0.1:8777/index.html`。
- **文明卡**：152 × 中英 = 304 卡查 `undefined/NaN/[object`
- **城市面板**：127 城 × 中英，每城每行 `gvDetail(ri)` 查 undefined + 空行（脚本见 CHANGELOG v203）
- 照片：`CIV_PHOTO` / `CITY_PHOTO` 全量 `Image()` 加载测试（v209 276/276）
- 收 `pageerror`
- ⚠ 后台标签页里 rAF 不跑、smooth scroll 不动——测滚动类改动要么临时改 `behavior:'auto'`，要么把标签页切到前台。

### 从浏览器往本机传数据
`python3 http.server` 不收 POST；scratchpad 里有个 `recv.js`（node，8778 端口，POST 什么存什么），本轮所有 Commons 元数据都是这么落盘的。下个 session 要用就再起一次。

### 给孩子的新内容的固定流程
写作 → **先查站内既有口径** → build/audit/语法 → 浏览器验证 → commit → **派 adversarial-verifier**（给它：commit 号 + schema + 判定依据 + 我自己的存疑点清单；**不给完成报告**）→ 逐条裁决（**包括核它给的改法**）→ 修正 commit。

### 改版图的固定流程（v188 立，v208 又用了一遍）
写断言（必须在内 / 在外的城市清单）→ 脚本迭代坐标到全过 → 落 `data/geo.js`（顶上写断言注释）→ **补 audit 探针（正向 LANDMARKS + 反向 NOT_IN）** → **反向注入实测探针真的会响** → 检查规则 36 基线 → **看那座城|那条带有没有既有小段（有 = 可能是有意的）** → 新出现的空行补小段。

### 加照片的固定流程（v202/v204/v209）
候选标题（古迹表映射 / 手工点名）→ en.wikipedia `titles=`+`pageimages`（**别用 search**）→ Commons `imageinfo` 查存在 / 许可 / mime → 剔 svg / tiff → contact sheet 人眼过 → 存 `{f, h(md5 前缀), t, by, lic, s?}` → 960px 全量加载测试。API 有 429 限速，串行 650ms 一次、429 睡 8 秒。

### 本机环境的坑
- **`git push` 被权限拦**（Ray 手动推）；`curl` 出网被拦；`timeout` 不存在。**Python `urllib` 出网可以**（B 站 API 核 BV 号就是这么跑的）。
- B 站视频核实：`api.bilibili.com/x/web-interface/view?bvid=BV...`（带 UA / Referer，1s 节流）。
- CDP 超时不等于页面死了：长脚本会 45s 超时，拆小 + 同步写法即可。

### 城市面板的六层内容（别写串了）
| 层 | 表 | 回答什么 |
|---|---|---|
| 总结段 | `city_lore.js` | 这座城**为什么在这里** |
| 古称 | `city_names.js` | 带年代，渲染在对应文明行上 |
| 古迹 | `city_sites.js` | 今天还看得见的实物；「(现建筑)/(始建)/(动工)」三种标注（v201.1 定） |
| 照片 | `city_photo.js` | 点了才加载；每城 ≤3 |
| 地点小段 | `place_lore.js` | 键「城市\|文明」，那个朝代这地方什么样 |
| 视频 | `city_video.js` | 全是搬运稿件非官方号 |

### 关键文件
| 文件 | 要点 |
|---|---|
| `index.html` | 三个视角：`#geoview`（地图，`GV.mini` 窄带态）、`#tableview`（列表）、时间图；`bkTerm()` 大百科搜索词；`photoStripHTML()` 照片条共用；`centreLinks()` 中心列/格城市链接 |
| `data/civ_photo.js` / `city_photo.js` | Commons 图；URL 拼法与 960 档限制写在表头 |
| `tools/civ_photo_dynasty_map.json` / `civ_photo_curated_titles*.json` | 文明照片两路来源的可复现清单 |
| `data/traces.js` | 29 条；头注写着人口流的四条入选判据 |
| `tools/audit.js` | 63 条规则；本轮新增 62/63 照片表校验；LANDMARKS / NOT_IN 探针 |
| `docs/CHANGELOG.md` | 每版详录，**判断依据都在这里，接手先读 v199 以来条目** |
