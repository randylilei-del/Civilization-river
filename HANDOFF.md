# Handoff — 文明长河 / civilization-river
更新时间：2026-08-07（v50 已上线）

> 给下一个 Claude 会话：先读本文件 + CLAUDE.md 即可继续，无需原对话记录。
> 上线地址 https://civilization-river.vercel.app ；仓库实名是大写 C 的 `Civilization-river`。

## 任务目标

给 7 岁儿子做的世界文明兴衰交互可视化（单文件 PWA，已上线）。本轮三条主线：
① 颗粒度对称（把中国史的细分程度推到全世界）② 给卡片加机制分析层（兴衰六问）③ 补人物、纠正「历史就是帝王打来打去」的结构性偏食。

## 当前状态（全部已 commit 并 push，线上已验证）

**119 文明 · 281 人物卡 · 六问 106/119 · co 并存政权 7 条目 · GL 224 段 · 大事记 900 · audit 28 条规则 · index.html 1.39 MB**

人物卡类别：king 97 / state 52 / war 36 / sci 33 / mind 23 / art 22 / faith 10 / sail 8。
帝王将相占比 66%（本轮开始前 77%），king 单项 35%（开始前 44%），女性 6 → 26 人。

**接手时先 `git status` / `git log` 核实。** 写这份文件时：工作区与远端同步，只有 `.DS_Store` 未跟踪（应加进 .gitignore，本轮没做）。

## 本轮 commit 列表（v32 → v50）

| commit | 内容 |
|---|---|
| `b93d3df` v32 | 补 14 条 PEAK 版图峰值年 + 蒙古 ay 1270→1309 + showAy 去重显示 + audit 第 23 条 |
| `5459f82` v33 | 东周拆成春秋/战国（八处结构） |
| `f0a311c` | co 字段设计 spec |
| `18fdda0` v34 | 六个分裂期加「同时存在的政权」co 字段（38 政权）+ audit 24–27 条 |
| `28e7085` v35 | co 数据按核查修正（纪年口径统一/都城补迁都/英文消歧/主线重定） |
| `b003e1d` v36 | 罗马拆成共和国/帝国（十一处结构） |
| `f949600` v37 | 亚述·巴比伦拆成古巴比伦/亚述/新巴比伦 + 新增 `tools/refs.js` |
| `1474241` v38 | 古埃及用 co 分期不拆色带（ct 标题覆盖）+ 迁都表述 8 条改「→」 |
| `c26ce38` v39 | 兴衰六问试点 10 条（q 字段，details 默认收起）+ audit q 校验 |
| `ea79c28` v40 | 六问试点按核查修正（4 硬错 + 7 口径） |
| `5fa029d` v41 | 六问铺开首轮合并 39 条（**当时未核查，标注了不要 push**） |
| `4b61ce8` v42 | 那 39 条按核查修正（11 硬错 + 8 口径 + 6 处卡片同错） |
| `3e3fd08` v43 | 六问铺到 106/119，东亚批核查修正（7 硬错 + 8 口径 + 3 处模板句） |
| `ce84612` v44 | 草原中亚 + 中东非洲两批核查修正（15 + 30 处） |
| `3ad36cd` v45 | 副标题注明「同色深浅只为区分相邻色带、没有含义」 |
| `8694b75` v46 | 补人物试点 23 张 |
| `7eb8097` v47 | 那 23 张按核查修正（5 硬错 + 13 口径 + 八思巴改挂元） |
| `a596036` v48 | **修 iPad 上「配色全黑」——彻底移除 color-mix 依赖**（详见下方坑） |
| `a66f938` v49 | 第二批人物卡 40 张合并 |
| `93ec39d` v50 | 那 40 张按核查修正（12 硬错 + 16 口径，反驳 2 条） |

## 进行中 / 下一步

**没有半成品**——所有改动都已 commit 且经过核查与全量渲染验证。下面是待办，按 Ray 关心的「功能进度」组织：

| 功能 | 进度 | 卡在哪 |
|---|---|---|
| 补人物 | **完成** | — |
| 兴衰六问 | 89%（106/119） | **剩 13 条在世文明，等 Ray 定第⑤问口径** |
| 补 PEAK 年份 | 80% | **11 条脱离 Taagepera 序列，等 Ray 定原则** |
| 拆分粗颗粒条目 | 80% | 剩米诺斯·迈锡尼、帕提亚·萨珊，可做可不做 |
| 并存政权 co | 60% | 非中国 9 条（古希腊/腓尼基/斯瓦希里/布哈拉三汗国/玛雅/中世纪西欧/印度河/文艺复兴意大利/现代诸国），随时可做 |
| **成就弹出卡** | 0% | **Ray 提的需求，动手前必须先过设计** |

**1. 六问最后 13 条（在世文明）**
法兰西、德意志、俄罗斯·苏联、日本、韩国、共和国、美国、现代中东、现代中亚、现代东南亚、现代非洲、印度·南亚诸国、拉丁美洲诸国。
「⑤ 为什么衰落」对它们不成立，需要 Ray 定新口径（候选：换成「现在面临什么挑战」，但给在世大国写挑战同样要定分寸）。

**2. 成就弹出卡（Ray 的原话：「成就这边也应该做一些卡片，类似于人物的弹出格式」）**
动手前要想清楚一件事：**很多成就（科举、罗马法、金字塔、造纸术）在「鼎盛区间」（GL）里已经有一份内容了**，两边会重复。是合并、还是各管各的，必须先定再写代码。参考人物卡的成功路径：先做 spec → 试点十几张 → Ray 过口径 → 铺开。

**3. PEAK 11 条脱离序列（旧账）**
法兰西 250 万在 Taagepera 的 France 序列里根本不存在 · 印度河 130 万是文化范围口径 · 笈多/拜占庭/贵霜用的是 Turchin 的值 · 桑海/准噶尔/萨法维/英属印度/德意志他未收录。三个选项：严格统一 / 混合来源但标出处 / 维持现状。**改这些要动 Ray 逐条核过的数值，必须他先选。**

**4. 其他旧账**：唐面积口径（540 vs 1237 万）· 纪录片 14/119 · `.DS_Store` 该进 .gitignore。

## 试过但失败的路（本轮新增，全部是实战踩出来的）

**① `color-mix()` 是个陷阱 —— 本轮最严重的线上 bug（v48）**
Ray 在 iPad 上报「点开中世纪西欧，配色都是黑的」。根因：`fill()` 与地图陆地都用 `color-mix(in oklab, …)` 现算颜色，而 **color-mix 要 Safari 16.2（2022 年底）才支持；SVG 的 `fill` 拿到无效值会回退成纯黑**。全站 119 条色带里 71 条（v=1/v=2）+ 所有陆地在旧 iPad 上全黑。
**同一个函数里还藏着第二个 bug**：混合基准取 `--surface`/`--ink`，而这两个变量在深浅模式下互换 → 深色模式里 v1/v2 明暗整个反转。
**修法**：三档颜色预先算成十六进制写进 CSS 变量（8 圈 × 2 档 × 深浅两套 = 32 个值），`fill()` 只查表。**现在全站代码里没有 color-mix，别再引入。**

**② 修 bug 时我自己造了同一个 bug，靠验证抓住**
插入 `-a/-b` 变量定义的脚本失败没写文件，而 `fill()` 已改成引用它们 → 119 条色带全部引用未定义变量、**全变纯黑**，等于把要修的 bug 放大到全站。是 canvas 逐像素测量发现的，不是肉眼。**改渲染必须量化验证，看截图不够。**

**③ 测颜色不能用正则解析 `getComputedStyle`**
现代浏览器返回 `oklab(0.71 -0.02 -0.10)`，正则提数字会丢负号，得出全是 0 的假亮度 —— 我据此得出过「71 条低对比」的错误结论。**正确做法：canvas 画 1×1 像素再 `getImageData`。**

**④ 模板字符串里不要写含 `${}` 的正则字面量**
`ppl-merge.js` 里 `` `...${civ.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}...` `` —— 模板插值与正则字面量语法打架，导致「波斯(阿契美尼德)」这类带括号的名字转义失效、匹配不到。**改成先算好转义再字符串拼接。**

**⑤ 英文名里的撇号必须转义**（`Zubaida bint Ja'far`、`Chang'an`）
批量插入时漏了会直接写坏 JS 语法。本轮栽过一次，回滚重来。

**⑥ 凭 node 输出反推源文本，锚点必失配**
`JSON.stringify` 把 `.5` 显示成 `0.5`、中文标点全角/半角、EN 文案与记忆不符。**改数据前必须直接 sed/Read 源文件原文做锚点。** 本轮因此重跑过四五次脚本。

**⑦ 核查员建议的 audit 新规则做出来后被我推翻**
它建议「六问里的数字与卡片 b/f/peak 不一致就报错」。实现后 57 组候选几乎全是误报 —— 同一张卡合法地存在多个同量词数字（印加「疆域南北四千公里」与「驿道三万公里」是两件事），机械比对区分不了「矛盾」与「不同事实」。**留着会让 audit 天天误报、把真错误淹掉，已删除。这类矛盾只能靠语义理解发现，对策是「每批数据都过核查员」而不是加规则。**

**⑧ 拆分色带不是免费的（v37 实测）**
时间轴对早期年代做了压缩：亚述·巴比伦拆三条后，古巴比伦（299 年）45px、新巴比伦（87 年）39px，都低于标签阈值 52px —— **时间轴上没了名字**。所以古埃及改用「拆信息不拆色带」（co 分期 + ct 标题）。**越古老的条目越不要拆色带，先算宽度再动手。**

**⑨ 手工 grep 清点引用会漏 —— 已工具化**
拆罗马时漏了 `GL_X`（追加表，键失配就静默变孤儿）。**拆分/改名前必跑 `node tools/refs.js <文明名>`**。鼎盛区间有**三个**来源：`CIVS.gl`（内联）、`GL` 表、`GL_X` 追加表。

**⑩ 上一轮的坑仍然有效**：3D 沙盘教训（视觉复杂度要为信息服务）· B 站不能页内嵌 · `#panel .fact > span` 要收窄 · `[hidden]` 会被 flex 盖掉 · 万→亿≠billion · 字段末尾可能没逗号

## 内容质量的硬教训（六问 + 人物卡共核出 60 余处错，这几类反复出现）

- **带数字的断言让人停止追问**。王贞仪卡上「编号 43259 的小行星以她命名」—— 那颗小行星纪念的是 1924 年生的血液学家**王振义**，同名不同人；而同一句里金星环形山是**真的**，真假各一半最难发现。
- **时代 × 数字错配**：「康雍乾…人口涨到四亿」（四亿是道光年间的事）。
- **精确数字挂错锚点**：亚述「三年拆完帝国」、辛那赫里布「150 公里的渠」（那是历次开渠总和）。
- **「最早/第一/唯一」必查**：「史上第一家股份公司」（莫斯科公司 1553 更早）· 屠呦呦「首位科学类诺奖中国籍科学家」（杨振宁李政道 1957 年持中国国籍）· 王选「世界第一份激光照排中文日报」（查无来源）。
- **伪引文**：西周「天命靡常，惟德是辅」是《诗经》与《尚书》两句拼接 —— **这条在卡片里躺了很久，不是本轮写的**。突厥卡片带引号的「听信唐朝甘言而亡国」同类。
- **热门知识已被推翻**：侯奈因「在智慧宫主持翻译」（学界认为智慧宫是藏书处非翻译院）· 维特鲁威《建筑十书》「1414 年被找到」（该书从未失传）· 莎士比亚「首创 1700 个英文词」。
- **六问写对了、卡片写错了**：草原批核查发现 12 处，六问在替卡片纠错而卡片没跟上，形成站内自相矛盾（花剌子模最典型）。**改六问时要顺带看卡片。**

## 阻塞项 / 等用户决定

1. 六问最后 13 条在世文明的第⑤问口径
2. PEAK 11 条脱离序列的处理原则（三选一）
3. 成就弹出卡的设计（与 GL 鼎盛区间的关系）
4. push 一律由 Ray 自己跑（本机 settings 拦 Claude 的 push）

## 关键上下文

**验证流程（每次改完必跑，顺序固定）**
```
sed -n '/<script>/,/<\/script>/p' index.html | sed '1d;$d' > /tmp/c.js && node --check /tmp/c.js
node tools/audit.js          # 28 条规则,新规则必须反向注错(注错版 exit=1、对照组 exit=0)
node tools/refs.js <文明名>   # 拆分/改名前必跑
python3 -m http.server 8777 --bind 127.0.0.1 --directory "<项目路径>"   # 后台任务方式起
# 浏览器全量扫描(claude-in-chrome;本机无 Playwright,file:// 被拒):
#   119 × 中英 × 深浅色 = 476 张,查 undefined/NaN/[object/null/空括号 /\(\s*\)/
#   改渲染时还要量化:canvas 取 1×1 像素算对比度,别信 getComputedStyle 的字符串
```

**数据结构速查（本轮新增）**
- `q` 兴衰六问：`{born,rule,money,power,fall,legacy}` 每键 `[zh,en]`，六键必须齐全（缺键那一问静默消失）。渲染成 `<details>` 默认收起。
- `co` 并存政权：`{n,y,c,g,m,x}` 全双语；`y[0]` 可为 null（老国只写灭亡年）；`ct` 换区块标题（古埃及用「先后的王国时期」）。
- `PEOPLE`：`yk:'r'`=在位、`yk:'a'`=活跃（与 `r` 互斥）。**非君主一律用 `'a'`**，否则渲染成「在位 X—Y」。**在世人物**用 `yk:'a'` + 活跃年（全站仅屠呦呦一例）。
- 颜色：`--c-<圈>` / `-a`（浅档）/ `-b`（深档），深浅两套写死十六进制。**不要用 color-mix。**

**内容撰写与核查流程（本轮验证有效，六问 96 条 + 人物 63 张都走的这条）**
1. 导出批次数据（含现有卡片内容，防止与既有内容矛盾）
2. 写规范文件，**把前几批被核查员抓过的错型写成自查清单**
3. 派撰写 agent（多批并行），产出 JSON
4. 合并脚本校验后插入（六键齐全/双语/不重复/条目存在）
5. **派 adversarial-verifier 独立核查 —— 只给成品和判定依据，绝不给撰写者的自查报告**
6. 逐条核实核查意见（本轮反驳过 5 条，其中 2 条是核查员缺站上代码/数据所致），修正
7. 全量渲染扫描 → commit

**Ray 的口径偏好（累积确认）**
- 客观事实 > 因果推论；因果内容必须「主流共识 + 多因 + 争议标注」。
- 人物卡的 `s` 段只写可核查的成就与地位，**不写「他改变了历史」这类**（v26.1 定的铁律）。
- 新补人物：帝王将相不超过一半，有实质事迹的女性主动补。
- 数据量大的批次必然有错，主动核查并说出来，比假装没问题有用。
- 拿不准写区间，查不到就留空，**不编**。
- **汇报只说「哪个功能、走到几成、有没有卡住他」**——他明确说过，中间的核查细节自己消化，别当成进度汇报。
- spec 他不细看，评审点放在「做出来的实际卡片」上。
