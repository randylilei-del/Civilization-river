# Handoff — 文明长河 / civilization-river
更新时间：2026-08-06 07:17

> 给下一个 Claude 会话：先读本文件 + CLAUDE.md 即可继续，无需原对话记录。
> 上线地址 https://civilization-river.vercel.app ；仓库实名是大写 C 的 `Civilization-river`。

## 任务目标

给 7 岁儿子做的世界文明兴衰交互可视化（单文件 PWA，已上线）。本轮主线是**给 PEAK（鼎盛硬数字）补齐年份，让跨文明比较真的成立**——结果发现要补的不只是年份，全表「统一用 Taagepera 序列所以可比」这个前提本身是破的（见「下一步」第 1 条）。

## 已完成（v32 / v33，均已 commit）

| commit | 内容 |
|---|---|
| `b93d3df` | v32 补 14 条版图峰值年 + 蒙古 ay 1270→1309 + 年份与鼎盛期重复时不显示 + audit 第 23 条规则 |
| `5459f82` | v33 东周拆成春秋与战国（八处结构同步） |

**接手时先 `git status` / `git log` 核实。** 写这份文件时：工作区干净，**本地领先远端 3 个 commit（v31/v32/v33 都没 push）**。

当前数据量：**116 文明** · 217 人物卡 · PEAK 57 条（有版图 56、其中带年份 31；有人口 34、其中带年份 18）· GL 219 段 · 900 条大事记 · 纪录片 14 个文明 · index.html 765 KB。

### v32 补的 14 条 ay（全部落到 Taagepera 论文原文行，非二手转引）

苏美尔·阿卡德 前2250 · 古埃及 前1450 · 亚述·巴比伦 前670 · 波斯 前500 · 孔雀 前261 · 哈里发 720 · 塞尔柱 1080 · 花剌子模 1218 · 德里 1312 · 马里 1380 · 帖木儿 1405 · 阿兹特克 1520 · 印加 1527 · 奥斯曼 1683

### v32 的显示规则（`showAy` / `peakSpan`，在 peakHTML 上方）

「鼎盛」那格已经写了年代，再并排标版图峰值年多数时候是重复。只在相差 **30 年以上**才显示——31 条里只剩 8 条出年份。其中**隋、金是因为「开皇之治」「世宗大定之治」根本没写年代**，那时 ay 是卡片上唯一的时间线索。

## 进行中 / 下一步

**1. PEAK 有 11 条根本不在 Taagepera 序列内（优先级最高，需要 Ray 先定原则再动手）**

这是本轮最重要的发现。改这些要动数值本身，而数值是 Ray 逐条核过的，不能替他决定。

| 文明 | 现值 | 实际情况 |
|---|---|---|
| 法兰西 | 250 万 | Taagepera 的 France 序列里**没有任何一行是 2.5**，最接近 1813 年 2.1 |
| 印度河文明 | 130 万 | Taagepera 是 30 万。现值是「文化分布范围」，他记的是「政治实体」，差 4 倍 |
| 笈多 | 350 万 | Turchin 2006 的值；Taagepera 1979 是 170 万 |
| 拜占庭 | 270 万 | Turchin 2006 的值；Taagepera 1979 是 280 万 |
| 贵霜 | 200 万 | Turchin 的值 + Taagepera 的年 |
| 桑海 | 140 万 | Taagepera 是 80 万且自己打了问号；140 万来自 Orum 2019 城市研究百科 |
| 准噶尔 | 400 万 | **查不到出处**。两个可查来源都是 360 万（Adle 2003 给 1717、Scheidel 2020 给 1650） |
| 萨法维 | 290 万 | Taagepera 全表无此条目（1997 年五张表逐行读过，1500 年后西亚直接跳到奥斯曼） |
| 英属印度 | 490 万 | Taagepera 未收录 |
| 德意志 | 54 万 | Taagepera 未收录。54 万是 1871 帝国本土口径，但存续区间写的是 1866–2025，现代德国只有 35.7 万 |

三个选项：① 严格 Taagepera，不在序列内的改成他的值或标注「非序列」 ② 接受混合来源但每条标出处，放弃「跨文明可比」的强主张 ③ 维持现状，只在 docs 里说明。

**2. 三条标签与数字错位（改标签比改数字容易）**

- **希腊化王朝**：520 万是**亚历山大本人的帝国**（前323 他去世当年，当年即分裂），标签却写「希腊化王朝 前331—前30」。真正的希腊化王朝（塞琉古）峰值只有 390–400 万。
- **中世纪西欧**：120 万是**加洛林帝国**，能定位到 Taagepera 1997 附录原文行 `814 1.2 KH Charlemagne's death`。标签却是个地理区（西欧陆地约 300–350 万）。
- **西班牙·葡萄牙**：1370 万是**西班牙一家**，葡萄牙另有 550 万（1820）。

**3. 两条占比算错（改数值即可，无争议）**

- **奥斯曼 5.5%**：分子取 1683 年的 3000 万、分母取 M&J 的 1600 年 5.45 亿。按 1683 年（世界约 6.0 亿）应为 **5.0%**。
- **阿拉伯哈里发 16.0%**：Taagepera 原文 34M/220M = **15.5%**。

**4. 英属印度整条要重做**

现在的「3 亿 / 18%」是 **1900 年快照，不是峰值**。峰值是 1941 年普查 388,997,955（约 3.89 亿），该年世界约 23 亿，占比 **16.9%**——注意绝对人口涨了、占世界比反而降了。ay 用 **1911** 最稳（1886 并上缅甸之后、1937 缅甸与亚丁分出之前）。

**5. py 还有 15 条空着，但不要无脑填**

16 条是**结构性无解**：M&J 的分国序列起点是**前 400 年**，之前的没有。苏美尔、亚述巴比伦、希腊化、帕提亚萨珊、塞尔柱、萨法维、匈奴、突厥、花剌子模、帖木儿、马里、桑海、印度河、贵霜、西葡、法兰西——**留空是正确状态**，跟那 7 个「真的没有人物可写」的文明一个道理。

现代 6 条（美国 / 拉美 / 现代非洲 / 现代东南亚 / 现代中亚 / 印度·南亚诸国）我建议 `ay` 保持缺省、只补 `py:2024`——现代国家版图基本不变，标「版图年」等于暗示它某年达峰后衰落。**Ray 未表态。**

**6. 唐的面积口径，Ray 仍未表态**（上一轮就挂着）

现值 `a:540`（Taagepera 实控口径），中文通行值 1237 万 km²（含羁縻州）。**动这条要连带考虑其他文明是否同步换口径。**

**7. 纪录片只有 14/116**

硬规则不变：**链接必须人工核实后才能填**。核实命令 `curl "https://api.bilibili.com/x/web-interface/view?bvid=BVxxx"`。

## 试过但失败的路

**① 上一轮的 `scratchpad/` 目录整个消失了（本轮开头就撞上）**
上一份 HANDOFF 让「改 `scratchpad/cnpeak.js` 导出对照表」，但那个目录在磁盘上已不存在（项目没有 .gitignore，那些脚本从未入库）。本轮重写了导出脚本。
**教训：写 HANDOFF 时如果引用了脚本，要么把脚本入库，要么写清楚它是一次性的、下次得重写。**本轮的脚本同样在会话临时目录 `/private/tmp/claude-501/.../scratchpad/`，**下次大概率也没了**——`peakdump.js`（按泳道导出 PEAK 对照表）和 `addyear.js`（批量给 PEAK 插字段）如果还要用，要么重写要么这次就入库。`checkyear.js` 的逻辑已经固化进 `tools/audit.js` 第 23 条，不用再重写。

**② `vm.runInContext` 里 `const` 拿不到（重写脚本时第一次就栽了）**
从 index.html 抽出 `const CIVS = [...]` 丢进 vm 跑，然后读 `ctx.CIVS` —— 拿到 `undefined`。`const` 是词法声明，不挂到 context 对象上。
**修法：拼接的代码末尾加一行 `;globalThis.__out = {CIVS, PEAK, LANES};`，再读 `ctx.__out`。**

**③ 把「补年份」当成机械活，判断错了**
原计划是「查 Taagepera 序列填 36 个数字」，派了三个 source-researcher 后才发现 11 条根本不在那个序列里。**任务性质从「补全」变成了「口径审计」**，而口径要 Ray 拍板，我不能代劳。所以只落了 14 条零争议的，其余全部转成待决策清单。
**教训：「统一用了某个数据源」这句话本身要先验证，不能当前提。**

**④ py 原打算和 ay 一起补，全部放弃**
每一条 py 都拖着一个「要连带改数值」的尾巴（英属印度要改人口和占比、奥斯曼要在两个口径里选一个、拜占庭那个 26M 连出处都查不到），另有 16 条结构性无解。**混在一起改会把「补年份」和「改数值」两件事搅在一起，出了错分不清是谁的锅。**

**⑤ 世纪修饰词想细分，验证后砍掉**
`peakSpan` 原本打算把「14世纪初」「11世纪中」「12世纪后期」按比例细分成子区间。逐条推演发现：这三条按整世纪算和按细分算，`showAy` 的结果**完全一样**（都是不显示）。30 年这个粒度下修饰词不影响判断，砍掉。

**⑥ 英文标签 `Spring and Autumn` 与 `Warring States` 重叠**
拆完东周后深色+英文模式下看到两个标签挤在一起。实测像素：全称右边缘 380 > `Warring States` 左边缘 386（重叠）。改用 `Spring & Autumn`（EN 大事记里本来就是这个简写），间隔 6px。**维基跳转名 `w` 保持全称，不影响搜索。**

**⑦ 拆分后 audit 报了 4 条错，都是「中心」字段引起的**（已修，记下来避免重犯）
`f.中心` 里的地名不是随便写的：要按 `/[、,;；→]/` 分割后**每个都能在 PLACE 表里查到**，且**中英分割后数量必须相等**。我一开始写「洛邑与齐晋楚秦都城」配英文 `Luoyi and the capitals of Qi, Jin, Chu, Qin`——英文里的逗号被当成分隔符，zh=1 en=4 直接报错。**修法：春秋复用已有的整串键「洛邑与列国都城」，战国用三个具体都城并给 PLACE 补了「临淄」「邯郸」。**

**⑧ 以下是上一轮记的坑，仍然有效**
- 地图沙盘 3D（v25 做完又撤）：图层视差不是高度，**要做高度得先有海拔数据**。
- 人物卡「所以」口径（v26 全部重写）：写可核查的事实，不写因果推论。
- 人物头像：约 15 个现代人物照片大概率仍在版权期，改用八类字形。
- B 站页内嵌播不通：`b` 一律新标签页跳原片，只有 YouTube 走页内嵌。
- `#panel .fact span` 是后代选择器，会把人名片变 block，要收窄成 `> span`。
- `display:flex` 会盖掉 `[hidden]`，必须显式写 `#pcard[hidden]{display:none}`。
- **GL 合并只往 `GL` 里塞会让 `GL[c.n] || c.gl` 短路，把写在 CIVS 条目里的 gl 整个吞掉。两处都要认。**（本轮拆东周正好撞上，两处都改了）
- 单位换算：万→亿**不能直接映射成 billion**；小数值取整会失真。
- 插入字段的脚本：条目可能**末尾没有逗号**；英文名里的**撇号必须转义**。

## 阻塞项 / 等用户决定

1. **v31/v32/v33 三个 commit 都没 push** —— `git push` 由 Ray 自己执行（本机 settings 限制，Claude 跑会被拦）。
2. **11 条脱离 Taagepera 序列，要先定原则**（下一步第 1 条的三个选项）。
3. **现代 6 条要不要只补 py:2024、ay 留空**（我的建议是留空）。
4. **唐的面积口径**（旧账）。

## 关键上下文

**验证四步（CLAUDE.md 的工作流，每次改完都要跑）**
```
sed -n '/<script>/,/<\/script>/p' index.html | sed '1d;$d' > /tmp/c.js && node --check /tmp/c.js
node tools/audit.js                    # 现在是 23 条规则,退出码非 0 即有问题
python3 -m http.server 8777 --bind 127.0.0.1 --directory "<项目路径>"   # 用后台任务方式起,nohup 写法会被权限拦
# 浏览器全量扫描(claude-in-chrome;本机没装 Playwright,file:// 会被扩展拒绝):
#   for (const lang of ['zh','en']) { 切语言; for (const c of CIVS) openCiv(c) }
#   每次查 panel.innerHTML 是否出现 undefined / NaN / [object / 空括号 /(\s*\)/
```

**audit 第 23 条（v32 新加）**：`ay`/`py` 必须落在该文明自己的存续区间（`c.k` 首尾）内。年份填到区间外**肉眼极难发现**——卡片照常渲染，只是悄悄写着一个这个文明还不存在的年份。加规则时做了反向注错：注错版 exit=1、对照组 exit=0。

**反向注错测试（重要纪律）**
每加一条 audit 规则都要故意写错一次确认真能抓到，外加一个未注错的对照组。注意**改数据格式会让测试锚点失效**（报「锚点没找到」而非「漏了」，需同步更新锚点）。

**Taagepera 原文 PDF 已下载**（复用可避免重抓，都在 `~/.claude/projects/-Users-ray/<session>/tool-results/`）：1978 Systematics、1997 Expansion and Contraction、Turchin 2006、McEvedy & Jones（**无文字层，读不出来**）。
- **Taagepera 1979（600 BC–600 AD）三个 agent 都没拿到**，Cambridge Core 付费墙。拜占庭/孔雀/贵霜/笈多的值只能经二手确认归属。想彻底钉死口径必须找到这篇。
- **Wikipedia 的 M&J 世界人口列本身有错**（1700 年标 1010，M&J 实为 610，明显串列）。**1600 年之前的值两次抓取一致、可用；1700 年之后的必须回原书核。**

**数据口径（完整版在 docs/DATA.md，这里只留要点）**
- PEAK：版图取历史最大疆域、人口取历史峰值，**二者通常不在同一年**；`pc:1`=官方户口数；拿不准写 `[下限,上限]` 区间，**不要取单一高值**。
- 版图统一用 Taagepera 序列（**但见「下一步」第 1 条，这个统一是破的**），w 的分母按 py 年取 McEvedy & Jones 世界人口。
- **k 关键帧首尾约定**：已消亡的文明首尾帧都写 0（色带两端收窄成尖）；存续至今的末帧保持非 0。拆分朝代同样遵守——春秋/战国在前 453 年各自收窄成腰。
- 7 个文明**真的没有人物可写**（印度河、二里头·夏、米诺斯·迈锡尼、奥尔梅克、查文、瓦里·蒂瓦纳科、大津巴布韦），留空是正确状态，**不要为了凑数编人名**。
- 拆分色带要同步八处：CIVS（含内联 `gl`）· GEO · CHRONO · EN.civ · EN.chrono · PEOPLE 的 `c` 字段 · 外部 GL 表 · WIKI_NAME。

**Ray 的口径偏好（反复确认过的）**
- 有争议人物**不下自己的判断**，只陈述可核查的事实并注明评价来源。
- 客观事实 > 因果推论。
- **数据量大的批次必然有错，主动说出来并给出校对入口**，比假装没问题有用。
- 版图/影响力本来就是「主观示意」，价值在相对形状——但 PEAK 是为了可比才存在的，口径断裂要当真问题处理。
