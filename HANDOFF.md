# Handoff — 文明长河 / civilization-river
更新时间：2026-08-06（v40 已上线）

> 给下一个 Claude 会话：先读本文件 + CLAUDE.md 即可继续，无需原对话记录。
> 上线地址 https://civilization-river.vercel.app ；仓库实名是大写 C 的 `Civilization-river`。

## 任务目标

给 7 岁儿子做的世界文明兴衰交互可视化（单文件 PWA，已上线）。本轮两条主线：**① 把中国史的颗粒度对称到全世界**（拆分粗条目 + 并存政权 + 分期）；**② 给卡片加机制分析层**（兴衰六问）。

## 已完成（v32 → v40，全部已 commit 并 push，线上已验证）

| commit | 内容 |
|---|---|
| `b93d3df` v32 | 补 14 条 PEAK 版图峰值年（Taagepera 原文行）+ 蒙古 ay 1270→1309 + showAy 去重显示 + audit 第 23 条 |
| `5459f82` v33 | 东周拆成春秋/战国（八处结构） |
| `18fdda0` v34 | 六个分裂期加「同时存在的政权」co 字段（38 政权）+ audit 24–27 条 |
| `28e7085` v35 | co 数据按独立核查修正（纪年口径统一/都城补迁都/英文消歧/主线重定） |
| `b003e1d` v36 | 罗马拆成共和国/帝国（十一处结构） |
| `f949600` v37 | 亚述·巴比伦拆成古巴比伦/亚述/新巴比伦 + 新增 `tools/refs.js` |
| `1474241` v38 | 古埃及用 co 分期不拆色带（ct 标题覆盖）+ 迁都表述 8 条改「→」 |
| `c26ce38` v39 | 兴衰六问试点 10 条（q 字段，details 默认收起）+ audit q 校验 |
| `ea79c28` v40 | 六问按独立核查修正：4 硬错 + 7 口径，中英同步 |

当前数据量：**119 文明** · 218 人物卡（+亚述巴尼拔）· PEAK 58 条（带 ay 33）· GL 224 段 · co 并存政权 6 条目 38 政权 + 古埃及分期 3 · q 六问 10 条目 60 段 · 大事记 900 · audit 28+ 条规则 · index.html 806 KB。

**接手时先 `git status` / `git log` 核实。** 写这份文件时：工作区只有本文件未 commit，本地与远端同步（v40 已 push）。

## 进行中 / 下一步

**1. 兴衰六问：等 Ray 和孩子看完试点，口径确认后铺开（头号事项）**
试点 10 条：罗马共和国/帝国、亚述、新巴比伦、唐、明、清、蒙古、奥斯曼、大英。Ray 要判断的是**深度和长度**对孩子合不合适。确认后按同流程（写→adversarial-verifier 核→修）铺其余约 80 个已谢幕文明。
- 口径纪律在 `docs/superpowers/specs/2026-08-06-six-questions-design.md`：主流共识、不写单因论、争议标「主流看法」、每段 2–4 句。
- **在世文明（美国/共和国/现代诸国）不做**，⑤ 对它们既是猜测也敏感——将来单独定口径。
- 核查实绩（说明这一步不能省）：首批 60 段核出 4 硬错——最疼的是「康雍乾人口涨到四亿」（四亿是道光年间的事）。

**2. 拆分批次剩两条：米诺斯·迈锡尼、帕提亚·萨珊**
流程照 v36 罗马：`node tools/refs.js <名>` 盘点 → 逐处改 → audit → 全量扫描。色带宽度都够出标签（已验算），不会重演亚述的掉标签问题。

**3. co 第二批（并存政权铺到非中国条目）**
候选：古希腊、文艺复兴意大利、腓尼基、斯瓦希里城邦、布哈拉三汗国、玛雅、中世纪西欧、印度河文明、现代诸国。spec 在 `docs/superpowers/specs/2026-08-06-coexisting-polities-design.md`（含核查修正记录）。

**4. PEAK 11 条脱离 Taagepera 序列（旧账，Ray 未定原则）**
清单和三个选项见 spec 与 v37 之前的对话记录：法兰西 250 万在 Taagepera 序列里不存在、印度河 130 万是文化范围口径、笈多/拜占庭/贵霜是 Turchin 值、桑海/准噶尔/萨法维/英属印度/德意志他未收录。**动它们要 Ray 先选：严格统一 / 混合标出处 / 维持现状。**

**5. 其他旧账**：唐面积口径（540 vs 1237 万）Ray 未表态；纪录片 14/119；py 16 条结构性无解（M&J 分国序列起点前 400 年）留空是正确状态。

## 试过但失败的路

**① 拆分不是免费的：时间轴对早期年代做了压缩（v37 实测）**
亚述·巴比伦拆三条后，古巴比伦(299年) 45px、新巴比伦(87年) 39px，都低于标签阈值 52px——**时间轴上没了名字**。因此古埃及改用「拆信息不拆色带」（co 分期 + ct 标题）。**越古老的条目越不要拆色带**，先算宽度再动手。

**② `co` 的语义边界（差点教错）**
co 区块标题是「同时存在的政权」，古埃及三个王国是**先后接力**——硬套就是教错的。加了 `ct` 字段换标题（「先后的王国时期」）。「前后相继」型合并条目（米诺斯·迈锡尼、俄罗斯·苏联、帕提亚·萨珊）**不适用 co**，它们该拆就拆。

**③ 因果内容必然藏错，且错得「看起来很对」（v40 实证）**
六问首批 60 段，自检后仍被核查员核出 4 硬错 + 7 口径问题。特征：**表面精确最危险**（亚述「三年」挂错锚点）、**盛世配大数最易错**（康雍乾×四亿）。铺开剩余 80 条时核查步骤绝对不能省。

**④ 凭输出反推源文本，锚点必失配（v37 踩了两次）**
`JSON.stringify` 把 `.5` 显示成 `0.5`、EN 文案与记忆不符。**改数据前必须直接 sed/Read 源文件原文做锚点**，不要用工具输出的美化形态。

**⑤ 手工 grep 清点引用会漏（v36 实证）**
拆罗马漏了 `GL_X`（追加表，键失配就静默变孤儿）。已工具化：**拆分/改名前必跑 `node tools/refs.js <文明名>`**。写这个工具时又踩一坑：第一版所有表返回同一个错行号（全文件首匹配），比不给行号更误导，已修（按表限定搜索范围）。

**⑥ 鼎盛区间有三个来源不是两个**
`CIVS.gl`（内联）、`GL` 表、`GL_X` 追加表。glHTML 用 `GL[c.n] || c.gl` 短路，`GL_X` 合并时挂上去。三处都要认，refs.js 已覆盖。

**⑦ 上一轮的坑仍有效**：3D 沙盘教训（视觉复杂度为信息服务）、B 站不能页内嵌、`#panel .fact > span` 收窄、`[hidden]` 被 flex 盖、万→亿≠billion、字段末尾可能没逗号、撇号要转义。

## 阻塞项 / 等用户决定

1. **六问口径确认**（深度/长度）——确认即可铺开 80 条。
2. **PEAK 11 条口径原则**（三选一）。
3. 唐面积口径（旧账）。
4. push 一律由 Ray 自己跑（本机 settings 拦 Claude 的 push）。

## 关键上下文

**验证流程（每次改完必跑，顺序固定）**
```
sed -n '/<script>/,/<\/script>/p' index.html | sed '1d;$d' > /tmp/c.js && node --check /tmp/c.js
node tools/audit.js          # 28+ 条规则,新规则必须反向注错(注错版 exit=1,对照组 exit=0)
node tools/refs.js <文明名>   # 拆分/改名前必跑
python3 -m http.server 8777 --bind 127.0.0.1 --directory "<项目路径>"  # 后台任务方式起
# 浏览器全量扫描(claude-in-chrome;本机无 Playwright,file:// 被拒):
#   两语言 × 全部 CIVS openCiv,查 undefined/NaN/[object/null/空括号 /\(\s*\)/
```

**数据结构速查（本轮新增的）**
- `co`：并存政权 `{n,y,c,g,m,x}` 全双语；`y[0]` 可为 null（老国只写灭亡年）；`ct` 换区块标题。audit 24–27。
- `q`：兴衰六问 `{born,rule,money,power,fall,legacy}` 每键 `[zh,en]`，六键必须齐全（缺键那一问静默消失，audit 抓）。
- `showAy`/`peakSpan`：版图峰值年与「鼎盛」文字重复（±30 年）时不显示。
- 拆分条目必查清单以 `tools/refs.js` 输出为准（CIVS/GEO/CHRONO/CHRONO_X/GL/GL_X/VIDEO/PEAK/WIKI_NAME/EN.civ/EN.chrono/PEOPLE/EVENTS）。

**独立核查流程（大批数据/内容必走）**
导出成品到 scratchpad → 派 adversarial-verifier（只给成品+判定依据，不给完成报告）→ 逐条核实其意见（v35 反驳过 3 条、v40 全收，都对）→ 修 → commit 里记「接受几条、反驳几条、为什么」。

**Ray 的口径偏好（累积确认）**
- 客观事实 > 因果推论；因果内容必须「主流共识 + 多因 + 争议标注」。
- 数据量大的批次必然有错，主动核查并说出来，比假装没问题有用。
- 拿不准写区间不取单一高值；查不到就留空，**不编**。
- spec 他不细看，评审点放在「做出来的实际卡片」上——给他看效果，不要给他看文档。
