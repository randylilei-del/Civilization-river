# Handoff — 文明长河 / civilization-river
更新时间：2026-08-09（v111–v118 已由 Ray push；**v119–v123（Phase 4 全部）已 commit 未 push**）

> 给下一个 Claude 会话：先读本文件 + CLAUDE.md 即可继续，无需原对话记录。
> 上线地址 https://civilization-river.vercel.app ；仓库实名是大写 C 的 `Civilization-river`。

## 开新 session 先看这里

**story 页系列(v124-v132)**:六朝各一页(tang/qin/han/song/ming/qing.html),独立单文件、
微信可直接发文件给孩子看;实验判据在 docs/TANG-EXPERIMENT.md,**结果出来前不加功能、
不推更多朝代**。改文案直接改各页 ACTS 数组(变量名 TANG_P 是模板残留,装的是各页自己的版图)。

**这一轮（v93→v108）是收口的一轮，没有半成品，可以从任何地方重新起步。**

接手动作，按顺序：

1. `git log --oneline -3` + `git ls-remote origin main` —— 确认本地与远端状态，
   **不要信本文件里写死的 commit 号**
2. `node tools/audit.js` —— 应当 exit 0、46 条规则全过（45 = 正本一致性守卫，46 = config 英文完整性）
3. `node tools/coverage.js` —— 应当是「条目 637 · 城市 101 · 全满 101 · 总缺口 0」
4. **数据迁出重构已全部完成（v111–v118，Phase 1–3）**：23 张内容表的正本全在 `data/*.js`，
   **改数据 → 改 `data/<表>.js` → `node tools/build.js`**；改代码/config → 直接改 index.html。
   audit/coverage/peakgap/refs 四工具统一走 `tools/load.js`（注意两点：①它镜像了 GL_X 合并，
   index.html 那边改合并逻辑两处要同步 ②它返回的是**合并后视图**，做字节级比对要直接 eval 文件）。
   **Phase 4 也已完成（v119–v123）**：EN 字典退役，中英同条目成对（EVENTS/CHRONO 的 [zh,en]、
   CIVS 的 e 块、config 的 en 字段），CHRONO_X 已并回 CHRONO。**改内容时中英一起写在同一条里**。
   渲染层改动的验证用 `tools/snapshot.browser.js`（DOM 快照 326 条哈希）。设计与决策全在 `docs/ARCH.md`

三条都对，就说明站是干净的。**如果 Ray 直接说「继续」而没指方向**，
按「进行中 / 下一步」里那张推荐排序表提议，让他挑——不要自己选一个就开干，
本轮的主线就是他一句实机反馈定下来的，方向由他给最省事。

**最要紧的一件事不在代码里**：他儿子在上海用 iPad，而 `vercel.app` 要 VPN 才打得开
（阻塞项第 2 条）。**这是「整个项目给孩子做的，而他打不开」的问题**，
优先级高于任何内容补充，但需要 Ray 自己完成注册与实名。

## 任务目标

给 7 岁儿子做的世界文明兴衰交互可视化（单文件 PWA，已上线）。

**本轮（v93 → v108）由 Ray 的一句实机反馈驱动**：他点开地理视角里的成都，说
「**里边很多信息太粗糙了……基本上所有的朝代都只是说他在核心区域的边缘，
现在这个点进去看我觉得就没什么信息含量**」。

这句话指向的是一个真实缺陷：地点小段（`PLACE_LORE`）当时只写了 189 组，
**最该点的城反而最空**——洛阳 22 个朝代只有 1 条手写内容、开封 1/22、成都 1/19，
其余全靠一句自动生成的兜底句（「在核心区域的边缘」就是它）。
本轮把它从 189 组写到 **637 组，101 座城 × 全部命中文明，兜底句剩余 0 格**。

## 当前状态

**140 文明 · 302 人物卡 · 36 成就卡 · 六问 140/140 · GL 140/140 · 大事记 999 ·
地点小段 637 组（101 城全满）· PEAK 92/140（**其余 48 条已判定为「有意留空」，见 audit 规则 44，
不要再去查一遍**）· VIDEO 14/140 · audit 44 条规则 · index.html 2.2 MB**

**接手时先 `git status` / `git log` 核实，并用 `git ls-remote origin main` 与本地 HEAD 对比**
——不要信本文件里写死的 commit 数，它一定会过期。

### 本轮结束时没有待办的 push

v51–v108 全部已推。Ray 的本机 settings 拦 Claude 的 push，**任何时候要推都必须他自己跑**：

`cd "/Users/ray/cc projects/17-civilization-river" && git push`

## 本轮做完的事（v93 → v108）

| commit | 内容 |
|---|---|
| `bb5f06e` v93 | 修 Ray 实机反馈的三条地图问题（白条 / 压扁 / 台湾没轮廓）|
| `48a4e25` v94 | 深度补完成都/洛阳/西安/开封 + **新增第 140 条色带「三星堆·古蜀」** + audit 规则 41 |
| `bc6f1da` v95 | 洛阳/西安两轮核查修正（15 处硬错，含 5 处站内互斥）|
| `11e392b` v96 | 中国十城 + 欧洲十四首都补到满格（189→316 组）+ 四轮核查（约 40 处硬错）|
| `69d6803` v97 | 敦煌补满 + 四轮核查修正 87 处 + **修掉核心/边缘显示层 bug** + audit 规则 42 |
| `b8f6b66` v98 | 中东七城 83 组（316→399）+ audit 规则 43 + 规则 39 修 bug + `tools/coverage.js` |
| `609f0ff` v99 | 南亚三城 + 波斯两城 43 组（399→442）|
| `6a3b0b3` v100 | 中东七城四轮核查修正 60 余处（**含「假共识」那条**）|
| `0aeacb0` v101 | 中亚/东南亚四城 28 组 + **修掉一串版图错误**（河内被圈进中华人民共和国）|
| `9049a69` v102 | 内亚五城 30 组（470→500）|
| `0dd53c9` v103 | 中亚/地中海五城 25 组（500→525）|
| `8ff6353` v104 | 四轮核查修正 70 余处（**含「记载搬错两百年」那条**）|
| `a401197` v105 | 南亚/东南亚/地中海七城 30 组（525→555）|
| `4d97efd` v106 | **地点小段全部写完 —— 637 组，101 城全满，兜底句 0 格** |
| `a8988ed` v107 | 最后两批的**自查**修正 10 处（核查员当时全部因额度中断）|
| `e216b5e` v108 | 最后 112 组的独立核查修正约 60 处 —— **637 组全部核过** |

### 这一轮真正学到的东西

内容是量，**下面这几条才是这轮的产出**——它们全部已写进本文件的「硬教训」一节：

1. **写 lore 会反过来暴露数据错误。** 写河内时发现它被圈进了中华人民共和国的版图，
   根因是那两块多边形为了兜住海南岛多加了个 `[108,18]` 顶点。顺着修下去，
   又发现**十个文明的大陆边界都从海南岛上穿过**（岛被切成两半），
   以及**北宋本来就不该盖住河内**（越南 938 年就独立了）。
2. **audit 规则 36（城市覆盖基线）在这一轮真正发挥了作用**——改 GEO 时它当场报出
   「河内丢了共和国、民国」「海口丢了七个文明」，两次都拦住了我的错误批量改动。
3. **同一批新内容内部的矛盾，比新旧内容之间的矛盾更难发现。** 德黑兰那批里我写
   「两地两百年后完全对调」，而同一批我自己写的另外三张卡逐条否掉它。
4. **自查查不出「我不知道该怀疑的」。** v107 自查改了 10 处，全是我本来就知道
   属于高危形状的断言；v108 独立核查抓到的绝大多数，我一处都想不到。


## 进行中 / 下一步

**没有半成品。** 待办按 Ray 关心的「功能进度」组织：

| 功能 | 进度 | 卡在哪 |
|---|---|---|
| **地点小段（本轮主线）** | **完成 637/637 —— 101 城 × 全部命中文明，兜底句 0 格，全部经过独立核查** | — |
| 并存政权 co | **完成** | — |
| 兴衰六问 | **完成 140/140** | — |
| GL 领域鼎盛区间 | **完成 140/140** | — |
| 补缺失色带 | **完成**（11 + A 档 3 + B 线 6 + v94 三星堆），各批都过了独立核查 | — |
| 核查员遗留条目 | **已清完** | — |
| CHANGELOG 补录 | **完成，v0–v108 全覆盖** | — |
| audit 规则 | **44 条** | 见下方专列 |
| 补人物卡 | **彻底完成（v130+v131）**：9 条留空为正确终态（audit 规则 48 `FIGURES_NA` 双向守卫，**别再重判**）；莫切已补「西潘王」（Ray 拍板，独立核查 APPROVED）。303 张卡覆盖 131/140，**这条待办关闭**。遗留一个小待查：莫切 rule 文本里「金面具」的措辞（v131 CHANGELOG 有记） |
| 成就弹出卡 | 36 张，已过四轮独立核查 | 268 个成就词里可点 36。**没铺满是有意的**——很多词是描述性的（「盛唐气象」「行省与常备军制」），硬写会注水 |
| **PEAK 硬数字** | **完成 92/140** | **这件事做完了，别再开工**。56 条缺口查证下来只有 8 条能填、48 条应留空，名单与四类理由已内联在 `tools/audit.js` 的 `PEAK_NA` 里，规则 44 双向校验（缺数字又不在名单 / 在名单里却有数字了，两头都报）。孔雀王朝的尾巴已于 v129 收口：改为 350@前250（Taagepera 1978a Table 2，一手核过；Turchin 的 5.0 按全站口径不取） |
| **VIDEO 纪录片** | **14/140** | **必须人工核实链接**，CLAUDE.md 明令「绝不可凭印象生成 ID」。这是目前覆盖率最低的一项，也是**唯一一项 Claude 不能自己推进的** |
| 国内访问 | 链接侧已做完，部署卡在 Ray 手上 | 部署步骤见 `docs/DEPLOY-CN.md`（代码零改动已逐项确认）。**注册 + 实名只有 Ray 能做，别选成国际站**（海外节点，选错等于白做）。链接侧 v110 已解决：中文模式在文明卡标题与人物卡底部各加一条 B 站搜索出口，维基保留。**待 Ray 实机验一件事**：冷门文明（赫梯 / 桑海 / 卡涅姆·博尔努）的 B 站搜索结果像不像话——境外测不了，不行就收窄成「只在 `VIDEO` 表有人工核过的源时才给链接」 |

### 下一轮可以做什么（按我的推荐排序，Ray 未拍板）

0. **国内访问的注册实名**（Claude 推不动，但价值最高，见上表「国内访问」一行）。
1. ~~PEAK~~ —— v109 已完成。~~维基链接~~ —— v110 已做（Ray 拍板双链）。
2. **地点小段的第二层深度**：637 组现在每格都有内容，但深浅不一。
   中国十城与欧洲首都写得最透，非洲/美洲那批多是三四句。
   若 Ray 觉得某几座城还是「点进去没东西」，用 `tools/coverage.js` 定位、按本轮同样的
   「写 → 派核查 → 逐条评估 → 改」流程推进即可。
3. **人物卡剩下的 11 条**——先判断哪些是「留空才对」。
4. **VIDEO** 需要 Ray 自己核链接，Claude 只能准备候选清单。

### audit 44 条规则里，近两轮新增/修改的六条

| 规则 | 内容 | 由来 |
|---|---|---|
| 36（升级） | 城市覆盖基线改存**文明名单**而非计数 | 「丢一个又加一个」计数不变，逃得掉 |
| 41 | 英文词漏进中文串 | 写成都时手滑打出「唐玄宗west 奔蜀」，纯靠肉眼发现 |
| 42 | 手写正文说「边」而系统判「核心」 | 核查员发现正文与自动定位句当场打架 |
| 43 | 非拉丁字母混进英文单词 | 手滑打出西里尔的 `двух-headed`，**现有检查没有一条能看见它** |
| 39（修 bug） | 年份对齐支持「1980 年代 / the 1980s」 | `\b1980\b` 匹配不上 `1980s`；修完立刻逮到一处一直漏的真漂移 |
| **44**（v109） | 版图留空必须是「有意留空」不能是「还没查」 | 48 条名单按四类理由内联在 `audit.js` 的 `PEAK_NA`，**双向校验**：缺 `a` 又不在名单（新色带没人判断过）、在名单里却已有 `a`（名单过期），两头都报。顺带补了 `as` 的双语校验——原先只查 `an`，漏一半等于没查 |

**规则 42、43 都经过反向注入验证**（注入 exit=1、干净数据 exit=0）。
规则 42 的裸版本会误报开封|商（英文「opening the frontier」是地名「启封」的来历），
收窄成「中英两侧都要出现边疆词」才干净——**会误报正确文本的规则比没有规则更糟**。


### 缺失政权 B 线 —— 全部做完（v78/v79）

原 9 条：3 条升到 A 档由 v69 做掉（阿尤布 · 莫切 · 后倭马亚），剩 6 条 v78 补完。

**下次再排新色带候选，先做这一步**：`grep` 政权名，看站里既有卡片是不是已经在讲它。
「读到了却点不着」是硬缺口，「地图上有块空白」只是锦上添花——v69 那次分档分错，
就是因为只按「地理/时间空缺」判、没回站里 grep。

**⚠️ 欧洲泳道现在基本没有余量了，再加必须先算堆叠**：1680 峰值已达 3.09（阈值 2.35，
压缩 scale 0.76）。压缩本身按比例、相对形状不变，**真正的判据是「有没有色带在自己
峰值处掉标签」**（DESIGN.md：厚度 <13px 不画名字）。现状 17 条全部保有标签，
最薄的瑞典 14px——再加一条就可能有人掉下去。量法见 v78 的 commit。

**高句丽已用另一种方式解决**：不加色带，而是在「朝鲜半岛」卡里用 `ct`+`co` 分期显示（Ray 拍板 2C）。日本那条同样处理。这套「拆信息不拆色带」的做法风险远低于拆分色带，**下次遇到「一条色带包上千年」优先用它**。

### 核查员留下的条目 —— 已全部清完（v66 / v68）

上一版列的 11 条 + 2 条 audit 建议，**一条不剩**（明细见 CHANGELOG v66/v67/v68）。
最后那条「成就卡 `y` 四种语义」由 Ray 拍板 A、v68 落地：**`y` = 这东西起作用的年代，
不是所属文明的存续期**；只有一个时点就给单值 + `yk:'m'` + `ca:1`。
**断代的不确定范围绝不能写成区间**——会被读成「这东西只存在了六十年」。

**反驳掉一条（核实后不成立，别再改回来）**：「共和国人均寿命 67 与毛泽东卡 65 打架」——
两处讲的是**不同年份**：毛卡是执政期至 1976，共和国卡是至 1980。世界银行序列 1976 约 65、
1980 约 67，两个数都对。

**audit 两条建议只采纳了一条半**：「年份与大事记一致」按建议做成规则 32。
但「`y` 必须落在 `c` 所指文明的 `k` 区间内」**没做**——罗马法挂在罗马帝国（`k` 从 -27 起）
而 `y[0]` 是前 450 年的《十二铜表法》，造纸术 `y[1]`=1400 也远超东汉。**成就卡的 `y` 讲的是
这件事本身的跨度，不是文明的存续期**，那条规则会把设计意图当错误报。改为做了更值钱的
规则 31：**键必须能在 `f['成就']` 原文里被整词点出来**——这正是坑⑭（四张卡有两张点不开）
的机器化版本。

## 阻塞项 / 等 Ray 决定

1. ~~push~~ —— v109 已由 Ray 推；**v110 已 commit 未 push**（**每轮结束都要重新确认，别信写死的值**）
2. **国内访问**——Ray 的儿子在上海用 iPad，`vercel.app` 需要 VPN 才能开，**这是「整个项目给孩子做的，而他打不开」的问题**。查证结论：Gitee Pages 已停服；阿里云 OSS / 腾讯云 COS 要备案域名；**腾讯云 EdgeOne Pages 是最接近 Vercel 的替代**（免费、5GB 上限、默认域名 `xxx.edgeone.app` 无需备案、国内 CDN 加速、连 GitHub 自动部署）。建议**双份部署**，Vercel 保留不动。**注册与实名必须 Ray 自己做**，Claude 不碰身份证类信息。未实测（本机不让绕代理发请求）
3. ~~成就卡口径~~ —— Ray 已认可四张卡的写法（「四张卡片的方式我也 ok 继续推进吧」），v74 铺到 36 张、v76 核完
3b. **维京·北欧色带仍然缺失**——站内没有维京/丹麦-挪威这条带，全站 140 条色带里
   「维京」只作为别人卡片里的一句话出现（布哈拉卡讲银币流向北欧）。
   **注意症状已经消失但问题还在**：奥斯陆与哥本哈根现在不在 `GEO_CITY` 里，
   所以「有城市命中 0 个文明」这个可检测的信号没有了（本轮实测 101 城全部至少命中 1 条），
   但内容空白本身还在。等 Ray 决定是否补。
   **加之前先按 v78 那条纪律算欧洲泳道堆叠**：1680 峰值已达 3.09、最薄的瑞典 14px，
   再加一条就可能有色带在自己峰值处掉标签
4. **320px 老手机上 9 张 co 卡溢出**——Ray 已定**先不改**。根因是 `.co-i` 的 `white-space:nowrap`，属 DESIGN.md 设计决策；目标设备 iPad 竖屏 panel 有 599px，不受影响
5. **吐蕃 × 渤海国时间轴标签重叠**——Ray 已定**接受**。v53 的滚轮缩放能解开它
6. ~~GL 正文不渲染~~ —— Ray 拍板「点一下展开」，v73 已做。**现在 `t` 与 `d` 都会被读到**
7. ~~32 张新成就卡还没核查~~ —— v76 已派四个核查员核完，报出 60+ 处，改完

## 试过但失败的路（本轮新增，全部是实战踩出来的）

**① python 批量替换脚本 assert 失败会整批不落盘 —— 最危险的一个**
一批 15 处替换，第 12 处锚点失配抛异常，脚本在 `write()` 之前中断，**前 11 处全部丢失**，而终端已经打印了 11 个 ✓。差点据此认为改完了。
**对策：失配改成 `print` 跳过而不是 assert，或把 write 放进 try/finally。本轮后面的脚本都改成了「跳过并汇报」。**

**② 测试脚本自己过期，制造假警报 —— 本轮发生四次**
- 测「吐蕃在时间轴上有没有标签」得 false → 实际是脚本停在英文模式却搜中文名
- 测滚轮缩放得「失效」→ 先取了 `rect`、又跑 254 次 `openCiv` 导致页面滚动，坐标过期
- 测卡片内容得 false → 六问在 `<details>` 里默认折叠，`innerText` 取不到隐藏内容
- 线上验证时又栽了一次语言模式
**对策：验证脚本每次都要显式设定语言、重新取 rect；查内容优先直接读 `CIVS` 数据而不是 `innerText`。**

**③ 窄屏测量漏算滚动条，结论偏乐观**
先前报「co 卡 360px 溢出 0」，实际面板有 **17px 竖滚动条**，可用宽度是 287px 不是 302px，法兰克那条 296px 一直是超的。
**对策：量 flex 容器宽度时先确认 `panel.offsetWidth - panel.clientWidth`。**

**④′ 反向注入要先确认锚点真的落在目标区间（v112）**
测规则 45 时用「青铜时代大崩溃」做注入串，`replace` 改到的第一处在 650 行的 CIVS——
未迁移区，本来就不归规则 45 管，于是「注入」了个寂寞，audit exit=0 差点被读成守卫失效。
**对策：注入前先取标记区间的切片、assert 目标串在区间内，再改。**

**④ 管道会吞掉退出码，我据此报过一次「四条规则全过」（v109）**
测 audit 新规则时写的是 `node tools/audit.js | grep PEAK; echo exit=$?`，四条注入全打印 `exit=0`——
那是 **`grep` 的退出码**（它找到了匹配所以返回 0），audit 自己的退出码被管道盖掉了。
报错文字对，不等于退出码对；而 audit 的契约恰恰是退出码。
**对策：测退出码就别接管道——`node tools/audit.js > /dev/null 2>&1; echo $?`，分两步测。**

**⑤ 稀疏的数据表不等于「该值就是峰值」（v109，核查员抓到）**
Taagepera 1978b Table 5「China」在前1300 有一行 `0.7 Shang`，下一个点直接跳到前1122。
我据此判定「0.7 是商的峰值」并写进了代码注释——**错**。同作者 1978a Table 2 的五十年网格
把这段逐格标了 Shang：前1250 = .9、前1200 = 1.0、**前1150 = 1.1**。
Table 5 只是没有这几个点。
**对策：一个数字是不是峰值，要在「有连续网格的那张表」里确认，不能拿稀疏序列的相邻行反推。
同一作者的不同论文粒度不同，查一张表就下结论是这类错误的通用形状。**

**④ 修一个溢出撞出另一个**
给西哥特补「图卢兹→托莱多」后英文项 322px > 容器 302px。换连接词（later→then）**没省下宽度**，问题在名字本身。最终把迁都事实放进组 `x`（`.co-x` 是块级会折行，无宽度风险）。
**另一次相反的经验**：日本武士时代原本合成两条、英文过长，**拆成四条反而两全**——镰仓与室町本就是两个幕府，分开更准也更短。**遇到宽度问题先问「是不是合并得不对」。**

**⑤ Service Worker 缓存让我测到旧数据**
改完刷新，测出的仍是改前的值。
**对策：验证前先清 SW + caches，或加 query 参数（本轮一直用 `?v=xxx`）。**

**⑥ 长字符串当锚点必失配（坑 ⑥ 重犯）**
用整段中英文本做 python 替换锚点，因破折号、转义、全半角反复失败。
**对策：改用行号定位，或取短而唯一的锚点。**另注意 `PEAK` 表是**紧凑格式无空格**（`'法兰西':{a:250}`），按印象写带空格的锚点匹配不到。

**⑦ 撇号没转义搞坏语法（坑 ⑤ 重犯）**
`the capital's water` 写进单引号字符串直接把 `node --check` 搞挂。

**⑧ 三条色带被 `git add -A` 扫进了 audit 规则那个 commit**
埃塞俄比亚/奇穆/卡霍基亚本该独立成 commit。**新增内容与工具改动要分开暂存。**

**⑨ SVG 元素没有 `.click()` 方法**（不继承 HTMLElement），测点击要 `dispatchEvent(new MouseEvent('click',{bubbles:true}))`。

**⑩ 缩放锚点漂移，两次才修对**
第一版用年份差算比例——但全景横轴是**非线性分段**（`BP` 断点表），760 年在屏幕上占 48%、在年份区间里占 77%，漂了 636 年。改用像素比例后仍漂：**「全景→首次放大」是投影切换（非线性→线性），对应关系必然断**，再叠加右端 2025 的夹取，光标下从 760 跳到 101。最终解法：**首次放大以光标那一年居中、一步到位**，之后每步精确。

**⑪ 提示文字画进 SVG 会随横向滚动跑掉**（实测滚到 x=−79）。改挂 `.chart-card` 的绝对定位层——站里 `lane-tags` 与「全景」按钮本来就是这么做的。

**⑫ audit 新规则第一版漏掉了它要防的主要场景**
规则 29 只认 3–4 位数字，结果「9世纪初」「14世纪前期」这类写法**整条被跳过**——而核查员手工抓到的渤海、马穆鲁克恰恰是这种写法。反向注错才发现。修完又发现「前7—前4世纪」破折号后还有个「前」，跨世纪正则漏匹配后退化成「前4世纪」，**反而制造新误报**。

**⑬ 反向注错本身也会设计错**
测「同组多个 m:1」时只注入了**一个** `m:1`，而规则允许每组一个，自然不报错。要注入两个才是有效的注错。

**⑭ 成就卡的键必须与 `f['成就']` 里的原文整词一致**（与 PEOPLE 同一约束）。按概念命名写了「科举」「青铜礼器」，而站里原文是「科举制」（挂在隋不是唐）与「司母戊鼎」（挂在商），四张里两张点不出来。

**⑮ 验证脚本查错了容器，`ok:true` 是假的（v67，坑②的新变种）**
测四张成就卡能不能点开，脚本点完去 `panel` 里找 `.pc-head`，取到空字符串却因为
「span 找到了、click 没抛错」判成功。实际成就卡渲染进 **`#pcard .pc-box`**（`openAchv`
里写着），跟人物卡同一层、与 `panel` 完全无关。**教训：断言要落在被改的那个容器上，
「没报错」不等于「渲染了」**；改法是先 `pcBox.innerHTML=''` 归零再点，然后断言
`h3` / `.pc-sub` / `.pc-a li` 三项都取到值。

**⑯ audit 新规则可能在空数据上假通过（v67）**
`ACHV` 声明在 `TRACES` **之后**，而 audit 抽数据只抽到 `TRACES` 结束——新规则第一版
写完就"全通过"，其实是 `Object.entries(ACHV || {})` 在空对象上空转。**加规则前先打印
一次它要遍历的键**，确认真读到了（`ACHV keys = [科举制, 造纸术, 罗马法, 司母戊鼎]`），
再谈反向注错。同理：`node tools/audit.js | head -3` 的 `$?` 是 `head` 的退出码不是
audit 的，**验退出码必须单独 `out=$(...); echo $?`**。

**⑰ 本机 `rm` 会被权限拦**
`rm -f tools/_probe.js` 单独一条能过，但拼进 `&&` 长命令就被拒。临时脚本要放在
`tools/` 下（audit 用 `__dirname/..` 定位 index.html，扔 /tmp 会找错根目录），
**跑完记得单独一条命令删掉**，别留在仓库里。

**⑱ 数据里写 markdown 会原样显示给读者（v69）**
六问渲染是 `<p>${c.q[k][li]}</p>` 直插，**不处理 markdown**。我在新写的条目里用了
`**强调**`，星号会一个不少地显示在页面上——而读者是 7 岁小孩。查的时候顺带发现
**站里早就有一处**（埃塞俄比亚六问的「它是一份政治文献，不是史料」）。
这类错在源码里看着极自然，因为周围的 JS 注释全是带 `**` 的。
**已做成 audit 规则 33**，往后不用靠眼睛防。

**⑲ 我自己又踩了一次坑⑯（管道退出码）**
写完规则 33 测反向注错，用的是 `node tools/_probe.js | head -5`，然后读 `$?`——
那是 `head` 的退出码，报了个 `EXIT=0` 看着像"注错没生效"。
**上一轮刚把这条写进本文件，这一轮又犯。** 验退出码只能 `cmd >/dev/null 2>&1; echo $?`。

**⑳ 验证脚本的选择器要去源码里查，不要照猜（坑②/⑮ 的第三次变种）**
测新色带有没有画到主图上，用了 `[data-civ]`，结果三条全报「主图上找不到」。
实际属性是 **`data-ci`**（CIVS 数组下标，`path.band[data-ci="N"]`），
卡片地图是 panel 里**最后一个** svg（viewBox `0 0 332 231`）不是第一个。
换对以后三条都在，还都有标签。**「找不到」先怀疑选择器，再怀疑功能。**

**㉑ 加色带时 audit 会逐条报缺哪张表——照着补就行**
实测流程：写完 CIVS 跑 audit → 它报 `[缺英文 EN.civ]`/`[缺版图 GEO]`/`[PLACE 缺古地名]`/
`[缺英文 Figures]`，补一张跑一次，直到全过。**这是加色带最省事的一招，别凭记忆列清单。**
另有一个容易漏的：`f['中心']` 中英**条数必须相等**，英文写成 "Moche valley, Lambayeque valley"
（逗号分隔两项）才行，合成一句 "the Moche and Lambayeque valleys" 会被判 zh=2 en=1。

**㉒ 验证脚本选择器错了第四次（v71，坑②/⑮/⑳ 的第四次变种）**
测「GL 正文有没有改对」用 `panel.innerHTML.includes(...)` 得 false，差点以为改丢了。
实际是 **GL 的 `d` 根本不渲染**（见阻塞项第 6 条）。
**教训升级为通则：验证脚本报「找不到」时，先证明这个东西本来能被找到**——
用哨兵字符串（把数据改成一个不可能出现的串，重渲染，看它出不出现）比猜选择器可靠得多，
这次就是靠哨兵一次定案的。

**㉓ 混进别的字母表的字，肉眼防不住（v71）**
卡霍基亚的「南阿**palачi**山」里混了西里尔的 `а`(U+430) 与 `ч`(U+447)，
在源码里和真的「巴拉契亚」几乎看不出区别，却会原样显示给孩子。
**已做成 audit 规则 34**；注意不能一刀切禁西里尔——辽的卡片要讲「俄语 Китай 源自契丹」，
那是合法的，所以规则只查**拉丁与西里尔贴在同一个词里**。

**㉔ audit 通过 ≠ 页面上能用（v74，最值得记的一条）**
`"零"与十进制` 这个键在数据里完全正确，规则 31 判它匹配 `f['成就']` 原文，audit exit=0——
但卡片**永远点不开**。原因在渲染层：键要插进 HTML 属性，而键里带 ASCII 双引号，
属性被提前闭合成 `data-achv="" 零"与十进制"=""`，属性值变成空串，**页面还不报任何错**。
**教训：audit 查的是数据一致性，不是可渲染性。**任何会被插进 HTML 属性的数据都要转义
（已加 `attr()`，覆盖 `data-achv`/`data-person`/`data-goto`/`data-pgo`/`aria-label`），
而新增一类内容之后仍要在浏览器里逐个点一遍——这次就是靠浏览器全量点击才发现的。

**㉕ SVG 里用 `~` 兄弟选择器要看清 DOM 顺序（v73）**
给 GL 做悬停高亮，第一版写 `.gl-hit:hover ~ .gl-bar`，但命中区排在色条**之后**，
`~` 选的是后续兄弟，于是高亮跑到了下一行。**包一层 `<g>` 让悬停与点击匹配落在同一层**，
顺带解决了「点色条时 `closest('.gl-hit')` 匹配不到」的问题。

**㉖ 有些检查做不成规则，要及时收手并记下来（v77）**
核查员建议加一条「中英数字必须一致」的 audit 规则，想法很对（圣索菲亚「四十几扇窗 vs forty」、
印加「三万 vs 40,000」都是从这条缝漏的）。我做了三版，全站命中 **418 → 347 → 308 → 158**，
剩下的**仍然全是噪声**——中文写「1090年代」「三万」，英文写 the 1090s / 30,000，
**书写习惯的差异与真正写错了的形状完全相同**，正则分不出来。
**已撤掉，没留半成品**：命中几百条的规则只会被学会忽略，比没有更糟。
`tools/audit.js` 里留了一段注释说明为什么不做。**下次遇到「这个能不能做成规则」，
先估一下噪声与信号是不是同一种形状；是的话直接放弃，交给核查员。**

**㉗ 摘要字段会把 hedge 挤掉（v79，这轮最值钱的一条）**
长字段（六问）里写得都相当克制，而错误几乎全部出现在被压缩的 `b` / `f` / `d` / `CHRONO` 上：
波罗卡 `b` 的「把佛法送进西藏」与站内吐蕃卡差两个半世纪，而同一张卡的 `legacy` 写得很准；
蒲甘的让步只打在人物卡上，文明卡 `born` 用「由此」把因果焊死；
哈布斯堡的「匈牙利**残部**」在六问里有、在 `CHRONO` 与英文里没了。
**压缩时第一个被砍掉的就是限定词。**
👉 **校对优先扫 `b` / `f` / `d` / `CHRONO` 这四个短字段，长字段反而更安全。**

**㉘ 改一句话之前先 grep 它有几份副本（v79）**
v64 把美国宪法的「世界第一部成文国家宪法」改成了「今天仍在施行的…最早的一部」，
但**只改了六问、没扫大事记**——`CHRONO` 里那句「成文宪法首创」原封不动留到了 v79。
同类的还有奥斯曼 1529「扩张的高水位线」与 `PEAK` 的 `ay:1683` 打架。
👉 **同一个说法在 `q` / `d` / `CHRONO` / `CHRONO_X` / `GL` / `EN.*` 里往往有好几份副本，
改之前先 grep 全。**

**㉙ 我自己写的 audit 规则也会有洞（v79b）**
规则 34（混字母表）第一版只查「拉丁 ↔ 西里尔」相邻，**没查「汉字 ↔ 西里尔」**——
而这个站的正文主体是中文。我手滑打出「佛法второ入藏」时它一声不吭。
👉 **写规则前先问「这个站的数据主要长什么样」，别照着最先想到的那一类写。**

**㉚ 上一轮的坑仍然有效**：`color-mix` 陷阱（v48，全站已无，别再引入）· 改渲染必须量化验证不能看截图 · 测颜色用 canvas 取像素不用 `getComputedStyle` · 模板字符串里别写含 `${}` 的正则 · 拆分色带前先算宽度 · 手工 grep 清点引用会漏，用 `tools/refs.js`

## 独立核查的方法论（v93→v108 这一轮共派 20 个核查员，16 份出了报告）

**本轮（v93 → v108，20 个核查员）最值钱的三条：**

> **① 站内互斥是这轮的主要错误类型，而且大多是我自己一批内容内部打架。**
> 特奥蒂瓦坎「550 年焚毁 + 一千年后阿兹特克人来」——两个数都是我写的，**错的是它们的和**
> （550+1000=1550，比阿兹特克被灭还晚）。德黑兰那批我写「两地两百年后完全对调」，
> 而同一批我自己写的另外三张卡逐条否掉它。**给核查员的判定依据里要明确要求：
> 把同一座城的所有卡连着读一遍，再把新卡与站内旧卡比一遍。**
>
> **② 自查只能查出「我知道该怀疑的形状」。** v107 因为核查员全部额度中断，
> 我自己过了一遍最后 112 组、改了 10 处——全是「最」字句和拿不到来源的量词这类
> 我本来就警惕的东西。v108 独立核查回来抓到的绝大多数（假共识、记载搬错年代、
> 因果讲反、整段前提错），**我一处都想不到**。自查是补丁，不是替代。
>
> **③ 让核查员核「我改过的地方」。** 自查最容易犯的错是「改完觉得对了」。
> 本轮把三处自查修改点名交给核查员复核，其中非斯那处的改法被确认成立、
> 另两处也确认改对了——但同一句里**新暴露出一个我没注意的错**（「罗马建了瓦卢比利斯」，
> 实际它先是柏柏尔聚落、后由客王营建）。

**这一轮固化下来的派单模板**（直接照抄即可）：

1. 给**核查对象的确切抽取命令**（`grep -n "^'城市|"`，注意条目缩进不统一，别用固定空格）
2. 给**判定依据文件的绝对路径**（`scratchpad/lore-criteria.md`，六条标准）
3. **按我最不放心的顺序列出重点查项**——主动交底，省下自己回头再查的功夫
4. 明确要求做**第 5 条（站内互斥）**，并点名去 grep 哪些既有卡片
5. **绝不给自己的完成报告**——否则两个独立上下文瞬间退化成一个
6. 一次派 4 个、按地理分工；每个覆盖 25–50 组是合适的负载

**核查员会误判，必须逐条核实。** 本轮反驳的典型：智慧宫的传统框架（站内 8 处都这么写，
改一处反而制造新矛盾）；蒲甘「1995 年首次提名」（我自查时弱化过头，核查员确认原文本来站得住）。

（以下为更早几轮的经验，仍然有效）


**v79 那一轮（3 个核查员）最值钱的一条：**

> **错误集中在短字段，不在长字段。** 六问写得克制，而 `b`/`f`/`d`/`CHRONO` 里
> hedge 被压缩挤掉了——见坑㉗。给核查员的判定依据里应当明确点出这四个字段。

另外这轮验证了两件已知有效的做法：**主动交底**（我自报的 6 处疑点，核查员对其中 3 处
给了「核过，无异议」的明确裁决，其中一处还告诉我「比你想的更严重」）；
**留一个专做跨卡一致性**（1683 双认领只有把两张卡放在一起读才发现得了）。

**v76 那一轮（4 个核查员，按泳道分工 + 第四个做跨卡一致性）的结论，仍然有效：**

> **新内容会把旧内容的错照出来。** 前几轮都是新内容有错、旧内容对；v76 有 **12 处要改的是既有内容**——
> 贵霜 7 处「最早的佛陀造像 / 诞生地不在印度腹地」、吴哥「湮没丛林四百年」、印加驿道
> 中文三万而英文四万、拜占庭穹顶「近千年无人超越」、英文大事记「The first university」。
> **每加一批新内容，实际上是在给既有内容做一次交叉审计**——所以给核查员的判定依据里
> 一定要写「新旧两处对同一件事的说法必须一致」，而不只是「查新内容对不对」。

另外两条：
- **给核查员主动交底有用**。我把自己知道有风险的地方明着列进 prompt（「请特别判断这几处」），
  四个核查员里有三个对其中一部分给了「核过，无异议，不必改」的明确裁决——**省下了我自己回头再查的功夫**，
  也让它们更容易驳回我。
- **第四个专做跨卡一致性，仍然是设计对的地方**（v71 已验证一次）。三张位值制卡的计数互相打架
  （「两个文明」vs「第三次」），是只看单张卡的核查员结构上不可能发现的。

**v71 那一轮（3 个核查员）的结论，仍然有效：**

- **抓到的 11 处硬错里，两条最狠的都是「站内自己打自己脸」**——929 年把法蒂玛写成
  「开罗的」而站里法蒂玛卡自己写着 969 年才筑开罗；莫切的 GL 写「跨谷渠灌」而站里
  奇穆卡的成就栏就是「跨谷灌溉渠」。**这类错单靠外部查证抓不到，必须让核查员读站内既有卡片**
  ——所以给核查员的判定依据里一定要点名「去读哪几条相邻的既有条目」。
- **第三个核查员专做交叉一致性（不重复核前两条的内部史实），是这轮设计对的地方。**
  它抓到的跨谷渠灌冲突，是两个只看单条的核查员结构上不可能发现的。
- **核查员报的可以更保守**：版图那条它说也门在界外、厄立特里亚在界内，我自己写射线法复测，
  发现阿勒颇与麦地那也在界外。**几何、算术、计数这类能硬验的，一律自己再跑一遍。**
- 这轮**反驳/未采纳 3 条**：影响力 .55 不降（与站内标定一致，且 961 年的对比有教育价值）·
  不改色带名（牵连所有按名索引的表）· 版图顶点不逐点贴海岸线（既有卡同样如此，
  DATA.md 写明是示意版图）。

（以下为更早几轮的经验，仍然有效）

**拆两个核查员比一个好。** 本轮几次都是拆两组，**它们各自补上了对方的盲区**——一个说「另外 4 张没人核」，实际是另一个在核；单个 agent 摊在 8 条上会明显变薄。

**只给成品和判定依据，绝不给自己的完成报告**——否则两个独立上下文瞬间退化成一个。

**核查员会误判，必须逐条核实。本轮反驳了 3 条：**
1. 「audit 不校验 co」——实为规则 24–27，反向注错实测 exit=1
2. 「给 audit 加终年超母带尾规则」——会误报布哈拉/希瓦 1920（保护国名义存续，卡片 `q.fall` 有据）
3. 「加规则禁止 `q` 里带 hedge 的事实在 `d`/CHRONO 里裸断言」——原型实测命中 **40/130（31%）**，抽查两条全是误报（西周的 hedge 讲井田而 `d` 根本没提井田；匈奴的 `d` 其实有 hedge 只是关键词表没覆盖）。**这类需要语义理解，正是核查员该做的事，不是正则能做的。**

**核查抓到的最有价值的一类**：站内自己打自己脸。本轮三处——「非洲唯一未被殖民」而站里「欧洲殖民非洲」卡自己写着「仅埃塞俄比亚与利比里亚幸免」；「加纳率先独立」而站里恩克鲁玛人物卡本来就写对了「撒南非洲第一个」；阿散蒂「五次战争」与「四次英阿战争」并存。

## 内容质量的硬教训（累积；v93→v108 这一轮核出的硬错在 300 处量级）

- **「最早/第一/唯一」是最高危的一类**，本轮栽了五次：「印度本土政权最后一次到达阿托克」（1813 锡克帝国推翻）· 「德索托是最后的文字记录」（纳奇兹人到 18 世纪仍在造丘）· 「美国宪法是世界第一部成文国家宪法」（科西嘉 1755 更早）· 「巴西是唯一走过帝制的」（墨西哥两个帝国、海地两个）· 「汉字是唯一从上古用到今天的文字」（希腊字母是反例）
- **一字之差全错**：「1969 年的登月至今是人类**唯一一次**踏上另一个天体」——阿波罗共**六次**登月、12 人登上月面。原意是「月球是唯一一个」
- **两处出处拼成一句**：伊本·赫勒敦「从蒙古兵锋下逃来」（他 1382 年自突尼斯赴开罗，差八十余年）
- **伪引文**：「朕即国家」无同时代史料
- **热门知识已被推翻**：卡霍基亚「砍伐林地导致水患」（Rankin et al. 2021 实测证伪）· 长白山大喷发测定在 946 年、晚于渤海亡国 20 年（是被排除不是证据不足）
- **串卡**：卡霍基亚的「黑曜石」是霍普韦尔文化的标志物
- **叙事需要它成立，史料不支持它成立**：奇穆「印加不攻城、切断水渠即不战自屈」——实为打了数年、昌昌坚守约半年。核查员那句提醒值得记：*它与同卡「奇穆靠控制水与路而非野战」形成太漂亮的对仗，漂亮到应该警觉*
- **一个字段写对、另一个写反**：维查耶纳伽尔的 `born`/`fall` 都写「北方/德干诸苏丹国」，只有 `b` 写成「德里苏丹国」。**改一处内容要顺带看同卡其它字段**
- **译名不是风格问题是功能问题**：`f['人物']` 会渲染成维基搜索链接，编造的译名 = 坏链
- **底图数据的缺口只有在有人盯着看时才暴露(v93)**：Ray 一句「台湾省没有轮廓」，查出 `LAND` 这份简化海岸线漏了台湾、海南、西西里、**克里特**、塞浦路斯、西兰岛六个岛。克里特那条最要紧——米诺斯文明的中心就在岛上，它的版图此前一直浮在空海上，而站里跑了几十轮 audit 没有一条规则查得出来。**几何/底图类的问题，机器规则查不到，只能靠人看图。**
- **盘点前先跑 `tools/coverage.js`，别凭印象挑城(v98)**：写 lore 的顺序应该由缺口决定，不是由「我想起哪座城」决定。这个脚本还踩过一次自己的坑——PLACE_LORE 的条目缩进不统一（有的顶格有的两空格），固定两空格的正则抽出 0 个键、盘点结果全是「零覆盖」，差点据此重写一批已有内容。**任何抽数据的正则，先验证抽出来的条数对不对。**
- **手写内容和自动生成的句子会打架，而且只有人看渲染结果才发现(v97)**：「距核心区 800 公里，属核心地带」这种自相矛盾的单句在库里躺了很久，audit 查不出来（数据本身没错，错在两处用了不同的尺子），是核查员读渲染后的整段才抓到。**新增任何自动拼接的句子时，把它和同一屏上的手写内容一起读一遍。**
- **同一份文本翻译过去可能凭空长出一个断言(v97)**：「启拓封疆」译成 "opening the frontier"，机器规则就以为这里在说边疆。**做双语文本的机器规则，默认要求中英两侧都命中**，单侧命中基本都是翻译副作用。
- **改 GEO 的南岸/边界，必须连着「岛有没有被切开」一起验(v101)**：为了兜住海南岛多加一个顶点，就把河内圈进了中华人民共和国；把顶点拿掉，海南又变成十个王朝地图上的空洞或半块。**边界线穿过一座画出来的岛，视觉上就是渲染故障**。验证脚本要同时查三件事：城市覆盖有没有变（规则 36）、边界线有没有穿过岛的外接框、岛上取样点是不是被完整覆盖。
- **批量改多边形时，一次只动一件事(v101)**：我在一个脚本里同时改南岸走向 + 想补岛多边形，结果补岛那步漏了，还顺手切掉了汉唐的越南部分——而那正是我刚写的四条内容的依据。audit 一次报出七个文明丢覆盖 + 五条 lore 变成死内容才拦住。**先改走向、跑一次；再补岛、再跑一次。**
- **一次写完一座城的六到十条之后，把这几条连着读一遍(v104)**：德黑兰那批里我写「两个地方两百年后完全对调」，而同一批我自己写的另外三张卡（伊尔汗、帖木儿、萨法维）逐条否掉它。**同一批新内容内部的矛盾，比新旧内容之间的矛盾更难发现**，因为写的时候它们在我脑子里是连贯的。
- **一条记载可以被搬错年代，而读起来毫无破绽(v104)**：「当时的旅行者提到地下住处」——那是十三世纪雅古特写 1220 年雷伊城破的事，我挂到了 1404 年克拉维霍名下，还顺势编了个因果。**引用一条记载前，先确认它的作者和它描述的事件各属哪一年**，这是两个数字不是一个。
- **同一批新内容内部的算术要单独验一遍(v108)**：特奥蒂瓦坎那条写「550 年焚毁 + 一千年后阿兹特克人来到废墟」——550+1000=1550，比阿兹特克被灭（1521）还晚。**两个数都是我自己写的，错的是它们的和**。凡是一张卡里同时出现「某年」和「多少年后」，落笔前把它加一遍。
- **政治敏感段落的失败模式是「事实缺位」不是「措辞过软」(v108)**：台北三张卡加起来没出现「1949 年后由谁管辖」，拉萨的现代卡把 1951—1965 压成一个错标签。核查员那句话值得抄下来——**中立是把各方都承认的事实说全后不下判断，不是把有争议的事实略去**。
- **自查能查出「我知道该怀疑的」，查不出「我不知道该怀疑的」(v107)**：核查员全部中断后我自己过了一遍最后 112 组，改掉 10 处——但那 10 处全是我本来就知道属于高危形状的断言（「最」字句、拿不到来源的量词）。真正伤人的两类错（v100 的「假共识」、v104 的「记载搬错两百年」）**在自查里一处都不会浮出来**，因为写的时候我就相信它们是对的。**自查是补丁不是替代，未经独立核查的批次要在文档里明确标出来。**
- **别给结论盖「一般认为」的章，除非真的查过共识(v100)**：我写「大马士革锻钢一般认为就是被帖木儿掳走工匠断的根」——主流叙述里这门工艺活到 1900 年前后、四个假说里没有帖木儿。**错误结论加上共识背书，比裸的错误结论更难被后人推翻**，因为下一个读者会停止追问。「一般认为 / generally taken to be / 学界普遍」这类词，只在真的核过多个来源时才准用。
- **引了史料名，就必须读原文(v100)**：仓慈那条挂着「据《三国志》记载」，而原文是两拨人两件事（吏民图画其形 / 西域诸胡发哀立祠），我并成了一拨。**一旦引了出处，读者就不会再追问口径**——所以引出处的门槛要比不引更高，不是更低。
- **拿不到来源就改写，不要赌一个说法(v100)**：隋代莫高窟窟数、榆林窟取经图的窟号、六字真言碑今天是否仍立在窟前——三处都改成不带具体数字/窟号的写法。**一个查不到来源的精确数字，价值低于一句不精确但站得住的话。**
- **别新造「第一次 / 最早」的断言，先查站内已有的口径(v96)**：我在辽卡写「北京第一次成为陪都」、金卡写「第一次成为正式国都」，而站内战国卡写着「燕国的都城蓟」——三处互斥。站内自己的说法「北京建都之始」本来就是对的、也够用。**新写一张卡时先 grep 站内怎么说这件事，再决定要不要另起炉灶。**
- **深度补城时，一座城的相邻朝代卡要连读(v96)**：金卡那句「此后八百多年除了明初的几十年一直是政治中心」同时打脸自家民国卡（1928 改北平）和蒙古卡（荒废近半个世纪）。这类矛盾**逐条读读不出来，只有把一座城的十几张卡连着读一遍才暴露**。
- **带单位的精确表述最有欺骗性(v92)**：「一贯铁钱重到要用车拉」——一贯约 25 斤，是背得动的；而站内成就卡自己写的是「买一匹布要背上百斤钱」。**「一贯」这种带单位的说法读起来像查过书**，所以没人回去查。写数量前先确认单位和量级。
- **会误报正确文本的规则，比没有规则更糟(v92 一轮废了两条)**：维也纳解围哨兵报了波兰卡的正确写法；「手写层年份必须落在文明存续期内」两版分别误报了「城市前史句」和「后世影响句」。**合法句与真错在形式上同形时，就该停手交给核查员**——布拉格挂错文明那次，正是核查员读内容读出来的，机器查不出来。理由要留在代码里，否则下一个人会再写一遍。
- **给版图加「小环」时，矩形会悄悄圈进邻国大城市(v91)**：给大英帝国补香港时用了个矩形，北界 22.58 把**深圳罗湖、福田、南山、盐田全圈了进去**——点深圳市中心会看到「大英帝国的版图覆盖过这里」。加小环必须按实际边界画折线，并拿邻近城市逐个复测。
- **画面感强的错句比错年份更危险(v91)**：「1868 年俄军兵临城下」——决战其实在泽拉布拉克、条约签于撒马尔罕。错年份容易被下一个人查出来，**细节丰富的错句会被当成「可靠」而没人回去查**。写具体场景前先确认这个场景本身有出处。
- **别写站内自己警告过的说法(v91)**：加奥写「弓箭对火枪一战定局」，而站内桑海卡明文写着「不是『火枪一战定输赢』这么简单」「不宜当作单一解释」。**站内既有的 hedge 是资产，新卡不能把它拆掉**——开罗的阿音扎鲁特也栽在同一条上（站内两处都有「西亚方向」限定与争议标注，新卡两样都没写）。
- **改 GEO 会静默改变别的城市的查询结果，必须跑全城市 diff(v90)**：地理视角对全部城市点做点在多边形内判定，改任何一块版图都可能让不相干的城市多出/少掉一条文明。v90 改印度河版图，德里从 11 条变 12 条——这是该自觉接受还是该回退，必须是有意识的判断。**流程：改完 GEO 跑 `tools/audit.js` 的基线重算，看 diff 列表，逐条确认。**
- **「表面精确的数字」在单边与面积之间偷换(v90)**：卡霍基亚僧侣丘「底面比胡夫金字塔还宽」——按含坡道的单边长度勉强成立，按底面积是「差不多大」。这类比较句要先确认比的是哪个量。
- **主语写错比事实写错更难发现(v89)**：「这座城连着挡住了三次蒙古大军」——每个字都能在史料里找到对应，只有主语错了：挡住蒙古的是大越这个国，而升龙城三次全部失守。**站内其余五处主语都是国，只有新写的这条是城**。写「某地在某朝如何」时，先确认这句话的主语到底是城还是国。
- **中英漂移是这个站最稳定的失败模式，已上机器约束(v89)**：三轮核查里反复出现的不是事实错，而是「中英讲的不是同一件事」（开罗英文多出 continuously、北京元英文多出 ruined、蒲甘中文上万座英文只写 thousands、平壤中文两百多年英文写 two centuries、雅加达中英两个城名）。年份是其中唯一能机器查的部分，已做成 audit 规则 39；**其余仍只能靠独立核查员**。
- **「表面精确的限定词」会让人停止追问(v88)**：「印度最高的**砖砌**宣礼塔」「**塔基**的石料取自拆庙」——两个限定词听起来越具体，越没人回去查；而实际上塔是红砂岩、拆庙的碑铭说的是塔旁边的清真寺。**加限定词之前先确认这个限定词本身有出处**，否则它只是让错误显得可信。
- **写「第一个/最早」时必须限定地理范围(v88)**：「印度第一个长久立住的穆斯林政权」——伽色尼王朝据拉合尔统治旁遮普 156 年，711 年起还有阿拉伯信德。站内 `CHRONO_X` 自己用的是「统治**北印**」这个正确口径。
- **给具名人物安引语,是本站出现过的最严重的一类错(v87)**：「马可·波罗说汗八里的壮丽远超欧洲的城」——《游记》该章通篇没有与欧洲的比较，而「世界上最美最壮丽的城市」那句他给的是**杭州**。这类错比数字错更难被发现（读着像真的、也确实有个真人说过类似的话），且一旦被对照原文就是硬伤。**规则：写「某某说」之前必须找到原文出处，找不到就改写成不带引述的客观陈述。**
- **站内已经写对的地方，是最好的核查基准（v87 三次命中）**：西墙（希律卡写对、城市卡写错）、圣索菲亚穹顶（三处写「室内空间/帆拱穹顶」、新卡写「最大的穹顶」）、爱资哈尔（`:960` 写「被压制了近百年」、新卡英文写 `continuously running`）。**写新内容前先 grep 站内同题材的既有表述**，比事后查外部来源便宜得多。
- **「示意图」不是「随便画」的许可证（v82）**：DATA.md 一直写着 GEO 是示意多边形，于是「顶点少、海岸线简化」被当成了默认可以接受的状态——**但简化不等于可以用一条直线代替一整段海岸**。实测下来，从秦到共和国 17 条色带都拿一条 `[121,30]→[113,22]` 的斜线当东南沿海，把福建与粤东整片切在版图外：泉州是宋元第一大港、宁波是明清口岸，这不是边角。同样的直线切法还切掉了美国西海岸与佛罗里达、加勒比全部岛屿、尤卡坦半岛、朝鲜半岛东南角。**判据是「这条直线抹掉的地方，有没有这个文明的重要城市」**，不是「顶点够不够多」
- **数据自检要选对探针（v81/v82 连着两轮验证）**：GEO 已经有规则 30（外接框）却什么也没查出来，因为**外接框对「边界画歪」结构性无感**——版图被切掉一整块，外接框往往一点没变。v81 换成「核心点在不在自己多边形内」抓出 6 条，v82 换成「230 座真实城市各命中几条文明」又抓出 30 条。**同一份数据换个探针就掉出一批新问题，说明「审计通过」只等于「这几条规则通过」**，不等于数据对

## Ray 的口径决定（本轮新增，累积生效）

- **图例「草原游牧」→「草原与内亚」**：只改标签不动分类，辽金元西夏吐蕃渤海的归属维持原样
- **一条色带包上千年时，拆信息不拆色带**：用 `ct`+`co` 分期，不改条目名（改名会牵连所有按名索引的表）
- **在世文明第⑤问 =「最近一次大的转折」**，只写已经发生、有公认时间点的，不写正在进行、结论未定的；区块标题也换成「这个国家的六问」（「兴衰」二字本身就先把结论说了）
- **成就卡与 GL 各管各的**：GL 讲「这个文明什么领域什么时候最好」，成就卡讲「这一样东西本身是什么、怎么来的、后来影响了谁」
- **版图数字只在偏离主序列时标出处**（未标的即出自 Taagepera 序列）
- **敏感内容六条（v65）**：日本补侵略只写年份不带评价 · 共和国补计划生育只陈述 · 韩国补三八线由来但不加朝鲜色带 · 东南亚补红色高棉 · **中东与美国两边都不提战争**（原本美国写了「两场长期战争」而中东只字未提，取最低成本的拉平）· 美国与法兰西两处时间框写准
- **站内标准要一致**：这是本轮反复出现的判据。现有标准是——美国写了奴隶制与种族隔离与原住民、俄罗斯写了集体化大饥荒、英属印度写了孟加拉饥荒、现代非洲写了卢旺达八十万、德意志写了大屠杀与二战死亡、日本写了侵略年份。**再加新条目时按这条线对齐。**

## 关键上下文

**验证流程（每次改完必跑，顺序固定）**
```
sed -n '/<script>/,/<\/script>/p' index.html | sed '1d;$d' > /tmp/c.js && node --check /tmp/c.js
node tools/audit.js          # 43 条规则;新增色带时它会逐条报还缺哪张附表——这是最省事的一招
node tools/coverage.js       # 地点小段覆盖率;写新一批 lore 前必跑,别凭印象挑城
node tools/coverage.js 洛阳  # 单城:还缺哪几个朝代
node tools/refs.js <文明名>   # 拆分/改名前必跑
python3 -m http.server 8777 --bind 127.0.0.1 --directory "<项目路径>"   # 后台任务方式起
# 浏览器全量扫描(claude-in-chrome;本机无 Playwright,file:// 被拒):
#   140 张文明卡 × 中英 = 280 张,查 undefined/NaN/[object/null/空括号
#   地点小段:101 城 × 全部命中文明 × 中英 = 1274 次 gvDetail,同时查「兜底句剩余几格」
#   (脚本见 CHANGELOG v106 那条;关键是 !PLACE_LORE[城市+'|'+文明] 的计数必须为 0)
#   co 卡还要在 375/360px 宽度下查 .co-i 溢出(记得扣 17px 滚动条)
#   验证脚本每次显式设定语言 + 重新取 rect;查内容优先直接读 CIVS 而不是 innerText
```

**数据结构速查**
- `q` 六问：六键必须齐全；在世文明的第⑤问标签与区块标题由 `qHTML` 按 `c.k` 末帧是否 >=2025 自动切换，**不动 QT 常量、不动数据结构**
- `co`/`ct`：规范在 `docs/DATA.md`。要点——先后接力必须换 `ct`；**组里只有一个成员时不要给 `m`**；**接力关系里 `m` 要给赢家不给最大的那个**（南诏那次错在给了被灭掉的前一个，朝鲜半岛正确地给了新罗而不是高句丽）
- `PEAK`：新增 `as` 字段 = 面积出处，只在偏离 Taagepera 序列时标
- `ACHV`：成就卡，键必须与 `f['成就']` 原文整词一致（audit 规则 31 现在会查）；`AGLYPH`/`AGNAME` 四类 inst/obj/tech/idea。**渲染进 `#pcard .pc-box` 不是 `panel`**；声明位置在 `TRACES` 之后，audit 靠单独抽一段 `const AGLYPH`→`\n};` 才拿得到，**再往后加新表要记得同样处理**
- 曲线圆点：`.sp-dot` 是视觉、`.sp-hit`（r=11 透明）是命中区，点击跳到 `.tl-item[data-evy]` 并高亮
- 缩放：标签位置由渲染器取影响力最大点（`index.html` 里 `let mi = a; for...` 那段），**标签重叠是数据决定的**；全景横轴非线性、已缩放后线性，跨这两种状态换算必踩坑

**新色带的影响力标定参照**
唐 1.0 · 阿拉伯哈里发 1.0 · 奥斯曼 .9 · 莫卧儿 .9 · 北宋 .75 · 突厥 .6 · 马里 .6 · 印加 .6 · 吐蕃 .55 · 马拉塔 .55 · 马穆鲁克 .55 · 玛雅 .55 · 伊尔汗国 .5 · 桑海 .5 · 法蒂玛 .45 · 维查耶纳伽尔 .45 · 辽 .4 · 埃塞俄比亚 .4 · 回鹘 .35 · 阿散蒂 .35 · 奇穆 .35 · 渤海国 .3 · 南诏·大理 .3 · 卡霍基亚 .3 · 西夏 .25

**加一条新色带的成本**：`CIVS`（含六问双语 6 键，主要工作量）+ `EN.civ` + `CHRONO`/`EN.chrono` + `GEO` + `PLACE` + `GL_X`。照西夏抄结构，**加完先跑 audit，它会逐条告诉你还缺哪张附表**。

**Ray 的沟通偏好（累积确认）**
- 汇报只说「哪个功能、走到几成、有没有卡住他」——中间的核查细节自己消化
- 要他拍板的事**必须用大白话写清楚**：现在什么样 / 问题在哪 / 几个选项 / 我的建议，让他回「1C 2A」这种就能定
- spec 他不细看，评审点放在「做出来的实际卡片」上
- 数据量大的批次必然有错，主动核查并说出来，比假装没问题有用
- 拿不准写区间，查不到就留空，**不编**
