# Handoff — 文明长河（17-civilization-river）
更新时间：2026-08-19 22:02

## 这是什么

给 7 岁的 Jasper（在上海用 iPad）做的世界文明兴衰可视化，**单文件 PWA**：全部代码、数据、海岸线都在 `index.html` 里，无构建、无后端。三个视角——**按时间看**（泳道图）、**按地方看**（世界地图 + 城市面板）、**按列表看**（172 行的表）。

## 这个 session 干了什么（2026-08-19）

上一版 HANDOFF 留的唯一未闭环项是「v272–v275.2 那 60 段鼎盛区间没过独立核查员」。这个 session 把它闭掉了，顺带补了 6 张成就卡。

1. **派出 adversarial-verifier**（只给 commit 区间 + 判定依据 + 高风险位置清单，**没给我的完成报告**）→ 判「需要修改」，9 必修 18 建议
2. **9 条必修全部修完**（v277）——我逐条独立复核外部来源，没有一条站不住
3. **建议档事实/hedge 类 12 条改完、3 条驳回**（v278）
4. **中档无成就卡补齐第 1 批 6 张**（v276）

## 当前状态（接手先 `node tools/check.js` + `git log origin/main..HEAD` 核实）

- `HEAD = ce8f22b`，工作区只有 `HANDOFF.md` 未 commit（就是这份）
- `node tools/check.js` **全通过**（exit 0；audit 16 条 warn、smoke 5 条 warn 都是既有基线）
- **9 泳道 · 172 文明 · 33 轨迹 · 342 张人物卡 · 84 张成就卡 · 391 段鼎盛区间 · 127 城 · 30 张数据表 · audit 规则至 78**
- **3 个 commit 未 push**（v276/v277/v278）。上一版 HANDOFF 说的那 22 个 v262 起的 commit **已经推上去了**（`git ls-remote` 实测 remote main 与本地同步），这次只剩今天新增的三个
- depth 三项绝对缺口：鼎盛不足 **0**、无人物 **0**、大档无成就卡 **0**；中档无成就卡 43→**37**，小档 77 未动

## 已完成 · 核查闭环（v277 / v278）

### 9 条必修（每条我都独立复核过外部来源）

| 位置 | 问题 |
|---|---|
| 商·甲骨 | **v275.2 自核把对的改成了错的**：原稿「十几万片」正确，被改成「有刻辞的约一万三千件」差一个数量级，且「刻辞条数」这个口径不存在。已改回「各家统计 13 万—15 万片，科学发掘 3.5 万余片」 |
| 商 | 两段 `thought` 同区间（-1250~-1046）同讲甲骨占卜，**重复上架** → 合并。GL 392→391，规则 75 如期拦下，按指示 `rm tools/.audit-stats.json` 重置基线 |
| 希腊化·犍陀罗 | 段区间 -330~-150 早于所有证据（佛像出自公元 1–3 世纪贵霜时代），且与站内贵霜卡的 100~250 矛盾、抹掉了贵霜卡已拍板的「犍陀罗与秣菟罗谁先谁后没定论」→ 改标题「留在东方的希腊石匠」、区间 -250~-30 |
| 萨法维·阿德比尔地毯 | 「三十四平方米」是把 **34 英尺**当成了平方米（实为 1044×535.5 cm ≈ 56 m²、约 2600 万结） |
| 德里苏丹 | 「为养常备军**又不加税**」是反的——他把 kharaj 提到产出的一半，另设 charai/ghari 两项教法外新税 |
| 西葡殖民美洲 | 围城是 1521-05-26~08-13；1520 年是「悲痛之夜」逃出城，天花在城内是 1520 年 10 月底—12 月初 |
| 现代东南亚 | 英文有 "between its members" 中文没有，照字面读与本卡 `q.fall`（红色高棉）正面冲突；另柬埔寨并未实施空袭 |
| 古巴比伦·《吉尔伽美什》 | 标题「定本」与加喜特卡的 GL 段标题**逐字相同**；「1853 年…当年轰动」错 19 年（轰动的是 1872 年史密斯宣读洪水残篇） |
| 帖木儿 | 雷吉斯坦三座经学院建于 1417–1660，帖木儿 1405 年已死 → 换成古尔-埃米尔陵（1403 起建 1404 落成） |

另修一条盲区（不在本批范围但同卡可见）：澳新岛国「人口一万多的国家管辖海域比印度还大」——图瓦卢 EEZ 约 75 万 km²，印度约 230 万，不到三分之一。

### 建议档改的 12 条

赫梯「十六年」→十五年（卡叠什前 1274、和约前 1259 缔结 1258 批准，站内 `q.legacy` 本就写 1259）；迦太基 220 艘归属改「阿庇安记」而非「考古估计」；孔雀石柱「还立着」→「还留存」（权威是 survive）；蒙古《史集》「第一次」→「已知最早」与伊尔汗国同步；贵霜佉卢文限定到早期金币；汉诺《航海记》补「希腊文转本 + 航线有争」；满者伯夷「唯一的清单」→「几乎唯一」；俄治中亚 a:1860→1890；罗马近东 a:100→-50（死海古卷成书于公元前后）；匈人「最可靠」→「最直接的目击」；赛克斯-皮科「按…分」→「以…为起点…与 1916 年那张图并不重合」；YBC 7289「精确到六位」→「约六位」。

### 驳回的 3 条（不要被后来的人当成「漏改」补回去）

1. **贵霜币重「口径分叉」** — GL 不提某事 ≠ 与 `q` 矛盾，那是遗漏不是冲突；且维基 Kushan coinage 只给金币 7.8–7.9 g，**没有**任何与罗马奥里斯的比较，两个方向都核不实。`q.money` 与 GL 均不动。
2. **隋「605 年设进士科」要加争议 hedge** — 中文维基科举条目明写 605 年且**未标注争议**，核查员称的 605/606 分歧与「由 587 年改名而来」找不到出处支持。
3. **迦太基船坞「Hurst 只复原出约 170 个」** — 这个具体数检索未能证实，只改 220 的归属，不写 170。

### v276 · 6 张成就卡（中档第 1 批）

亚述『尼尼微图书馆』/ 日本『明治维新』/ 澳大利亚原住民『布吉毕姆养鳗水渠』/ 德意志『现代大学』/ 新巴比伦『伊什塔尔门』/ 西周『天命观』。

**选题刻意避开 v272–v275.2 涉及的文明**——43 条中档缺卡里有 27 条属于那一批，当时尚未过核查，在其上叠卡会同时踩「成就卡抄鼎盛段」（v267.1 秦的教训）与「叠在未验文本上」两个坑。

## 进行中 / 下一步

### 1. 第二批成就卡（我停在这里，没落笔）

干净条目（不属于已核查那批，可直接写）还剩 **10 条**，我已跑过 `context.js` 的是前 3 条：

后倭马亚·安达卢斯（建议键：科尔多瓦大清真寺）· 马穆鲁克（**只能选「驿传制度」**——「开罗的清真寺与经学院」已是 GL 段且在规则 78 warn 名单里，「挡住蒙古」已被拜巴尔人物卡占掉）· 马拉塔（山地要塞体系）· 吐蕃（唐蕃会盟碑）· 南宋（市舶司海贸）· 阿尤布（经学院网络 / 开罗城堡，注意开罗城堡与 `place_lore` 开罗|阿尤布 重合）· 朱罗（青铜舞王像）· 战国（变法运动）· 西晋（**键是「再统一」**，站内原文带括号，`stripParen` 后才匹配）· 特奥蒂瓦坎（网格都市）

流程：`node tools/context.js <带名>` → 写 → `node tools/check.js` → commit → `lint-content <sha>~1` 逐条销账 → 派核查员。

### 2. 等 Ray 定的两个体例问题（我有意没动）

- **帕提亚「安息回马箭」归 `k:'art'`** —— 一项骑射战术被归进「艺术」域，条上直接显示「艺术」两个字给孩子看。是为凑齐四域，还是改 `tech`？
- **四处「GL 逐句复述 q」**：赫梯两段、帖木儿、德里苏丹、古巴比伦数学段。要重写而不是改一句，且牵涉「GL 到底该讲什么」的口径。

### 3. 规则线（仍押后，需要「下一条真实新带」当反例）

- `depth` 接进 `check.js`（新内容低于同组地板 → warn），难点是「新写」怎么识别
- `newband.js` 改造：把开工前自查表与 `context.js` 输出打进预检；让它自己拦「锚点取条目中间」「反例幅度不够」这两条复发过 2 次的问题。动手前先读透它（79 行）

### 4. 核查员点出、目前无人管的两个结构性缺口

- **跨色带一致性没有任何工具**。这次最硬的两条（犍陀罗 vs 贵霜、《吉尔伽美什》定本 vs 加喜特）都是**相邻色带互相拆台**，而 audit 的 GL 校验只在单条带内查区间与 `k` 值
- **同带同域同区间可以重复上架**（商那两段）——结构层不拦

### Ray 手上的

- push v276/v277/v278 三个 commit
- iPad 实机验收（人物卡与成就卡现在能点开，值得点几张：秦、罗马共和国、清，以及新加的尼尼微图书馆、伊什塔尔门）
- 两处待统一的既有数字（上一版就在他手上，未动）：清 乾隆卡「约一千三百万平方公里」vs `q.power`「约 1470 万」；蒙古 成吉思汗卡「3300 万」vs 成就卡「2400 万」
- `docs/TANG-EXPERIMENT.md`、IDEAS 001/003、`whviz-sq` 搜索记录导出、视频候选 32 条待人工核 BV 号

## 试过但失败的路

1. **「跑了工具」不等于「这一层没复述」。** 规则 78 用的是**最长公共子串 ≥14 字**（`tools/audit.js` 1802–1806 行，判据是 `best.length >= 14`；源码里没有「最长公共子串」这几个字，grep 那个词找不到，要 grep `复述母条目`），而 v272–v275.2 那批的复述全是**同义改写**——一条都没触发，warn 名单里 8 条全是旧卡。上一版 CHANGELOG 却把「扩了规则 78」当成了兜底。**同义改写的复述只能人读。**

2. **自核会把对的改成错的。** 商甲骨那条是铁证：v275.2 因为核查员四次被 API 529 打断而改用自核，结果把正确的「十几万片」改成了错的「一万三千件」，还在 CHANGELOG 里写成「修正」。**自核能抓工具能抓的，抓不到自己思路里的盲点——它不是独立核查的替代品，只是拖延。**

3. **hedge 只改正文、不改标题，等于没改。** 蒙古《史集》那段我自己就犯了：正文改成「已知最早」，英文标题 `the first world history` 忘了跟。这和核查员批评 v275.1 的伊尔汗国是同一个毛病——**标题才是显眼的那行**。是 `lint-content` 抓出来的。

4. **英文串里的撇号必须转义。** `soldiers' pay` / `ASEAN's` / `Tuvalu's` 三处直接写进单引号 JS 串，语法步当场报红。写英文正文时留意 `\'`。

5. **批量替换脚本的 all-or-nothing 是对的，但要看 `git diff --stat` 确认哪个文件真改了。** 这次 `civs.js` 因为原文是 `Noah\'s`（带转义）没命中，整个文件回滚未写，而同一批的 `gl.js` 已经写盘了——**逐文件 all-or-nothing，不是全局事务**。

6. **改数据前先确认它住在哪个文件。** 鼎盛段散在 `data/gl.js` / `data/gl_x.js` / `data/civs.js` 三处，我按核查员报告里的行号直接写脚本，蒙古《史集》和匈人两段其实在 `gl_x.js`，第一次跑整批断言失败。先 `grep -ln "<原文片段>" data/*.js`。

7. **规则 75「统计缩水」会拦有意的删减，这是设计如此。** 合并商那两段导致 GL 392→391，audit 直接红。按它自己的提示 `rm tools/.audit-stats.json` 重跑即可，但**要在 CHANGELOG 里写明为什么少了一段**，否则下一个人会以为是加载器漏了。

8. **派核查员时不要给它我的完成报告。** 这次给的是：commit 区间 + 判定依据文件路径 + 「以下位置刚被改过，我不告诉你改动理由，你只看现在的文本独立判断」。它因此逮到了自核改错的那一条——如果我把 CHANGELOG 的自核说明转给它，大概率就被带跑了。

## 关键上下文

### 改完必跑
```
node tools/build.js      # 改 data/*.js 之后必跑
node tools/check.js      # = build 一致性 → audit → 语法 → lint-content → smoke
node tools/lint-content.js <本批第一个 commit>~1   # 批量写完之后,逐条销账
```
⚠ `node tools/audit.js | tail` 会让 `$?` 变成 tail 的退出码——要看真实退出码就别接管道。

### 写新内容的固定流程
`node tools/context.js <带名|城市名>` → 写（中国带以中文一手为准；**西方带先用英文材料打底再译中文**，Ray 2026-08-18 定）→ `node tools/check.js` → commit → **派 adversarial-verifier**（给它 commit 号 + 判定依据 + 存疑位置清单；**不给完成报告、不给改动理由**）→ 逐条裁决（站不住的要驳回，这次驳了 3 条）→ 修正 commit → `lint-content <sha>~1` 销账

### 加成就卡的两个硬约束
- **键必须与该文明 `f['成就']` 里的原文整词一致**，渲染端做 `NSPLIT` 拆分 + `stripParen` 去括号后**精确匹配**，对不上就是纯文本、点不开而且不报错
- 自查可达性：照渲染端做一遍 `split(/[、,，;；]/)` + 去括号，再比对 ACHV 的键

### 核查员反复挑到的模式（下一批写之前先看）
1. 中英 hedge 不齐（含标题与正文不齐）
2. 卡片/鼎盛段抄母条目——规则 78 只查逐字，同义改写查不出
3. 同一件事在同卡讲第三遍
4. **跨色带互相拆台**（同一件事在两条带上年代/口径不一致）——目前无工具
5. 最高级要么给出处要么写「之一」「已知最早」
6. 现在时会过期（「至今还」「再没有」）

### 本机环境的坑
- **`git push` 被权限拦**（Ray 手动推）；`curl` 出网被拦；`timeout` 不存在。**Python `urllib` 与 WebFetch 出网可以**。
- smoke 需要 `playwright-core`（仓库根 `npm install`，只装这一个 devDependency，用本机 Chrome）。
- 派 adversarial-verifier 连续遇到 API 529 时别硬重试——但**也不要拿自核当替代品交付**（见「试过但失败的路」第 2 条）。这次一次就跑通了，耗时约 21 分钟。

### 关键文件
| 文件 | 要点 |
|---|---|
| `docs/CHECKS.md` | 规则体系设计文档（缺陷分层、A/B/C/D 自动化分级） |
| `docs/CHANGELOG.md` | 每版详录，**判断依据都在这里**；v277/v278 有本轮核查的逐条裁决与驳回理由 |
| `docs/DATA.md` | 数据模型；ACHV 的键匹配规则、`y` 是「这东西起作用的年代」 |
| `docs/DESIGN.md` | 「卡片是博物馆，不是词条」写在「信息层级」一节 |
| `tools/audit.js` | 78 条规则；规则 78 的判据在 1802–1806 行（`best.length >= 14`），grep `复述母条目` 定位 |
| `data/gl.js` / `gl_x.js` / `civs.js` 内联 `gl:[` | **鼎盛段住在三个地方**，改之前先 `grep -ln` 确认在哪 |
| `data/achv.js` | 84 张成就卡 |
