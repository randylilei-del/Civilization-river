# 数据模型与增改指南

全部数据以常量表形式存在。**已迁移到 `data/` 的表**(名单见下面「表的性质与住处」),正本是 `data/<表名>.js`,改完跑 `node tools/build.js` 注入回 index.html——**直接改 index.html 里的数据区间是错的**,audit 规则 45 会拦;**未迁移的表**照旧直接改 index.html 的 `<script>` 段。改动后务必:语法检查 → `node tools/audit.js` → 渲染验证 → 双语同步(完整工作流见 CLAUDE.md,迁移设计见 ARCH.md)。

## 表的性质与住处(2026-08-09 起,设计原则见 ARCH.md)

四类性质。**这是「One History, Many Lenses」原则的全部落地形式**——不做 lens 机制,只守两条纪律:**新增 fact 类字段不得掺叙事语言;新增叙事内容不得混入 fact 表。**

| 性质 | 含义 | 表 |
|---|---|---|
| **fact** | 受众无关的历史事实,未来任何受众/页面直接复用 | `CHRONO`(年份+事件;v121 起原 CHRONO_X 已并入) · `PEAK`(版图人口,`as`/`an` 记出处与口径) · `GEO` `GEO_CITY` `GC_ALIAS` `PLACE`(地理) · `LAND` `BORDERS` `RIVERS` `RANGES` `DESERTS`(底图) · `CIVS.k`(影响力曲线,半主观但口径统一) |
| **interpretation** | 经拍板的历史理解(结论受众无关,语言已偏儿童) | 六问(`CIVS.q`) · `GL` `GL_X`(领域鼎盛判断) · `EVENTS` `TRACES`(文明间关系) · `co`/`ct`(并存政权判断) · audit 的 `SETTLED` 表(已裁决说法) |
| **presentation** | 当前 age-7 版本的具体表达 | `PLACE_LORE`(地点小段叙事) · `PEOPLE` `ACHV`(卡片文案) · `CIVS.b/.d/.f` 的措辞 · `WIKI_NAME` `CIVS.e.w`(消歧) · `VIDEO` · 各条目内联的英文(翻译) |
| **config** | 渲染参数,不是历史数据,**永远不迁出** | `LANES` `SPHERES` `ERAS` `EV_GLYPH` `EV_NAME` `PGLYPH` `PGNAME` `AGLYPH` `AGNAME` `GL_KIND` `QT` 及 `INNER_W`/`BP`/`UNIT`/`STEP`/`ZOOM_MIN_SPAN`/`AY_GAP`/`NSPLIT`/`PB`/`GV*` 等标量 |

**Phase 2 已完成(v117)**:23 张内容表全部迁到 `data/`,`CIVS` `EN` 于 v117 收口。config 类(`PGLYPH` `PGNAME` `AGLYPH` `AGNAME` `LANES` `SPHERES` `ERAS` 及渲染标量)按设计留在 index.html。**Phase 3 已完成(v118)**:audit / coverage / peakgap / refs 四个工具统一走 `tools/load.js`(数据表按目录枚举 data/、config 表按名抽取),「从 LANES 切到 TRACES」的位置切片已删除。**注意 load.js 镜像了 index.html 的 GL_X 合并逻辑**——index.html 那边改合并,load.js 要跟(两处都有注释互指)。

`tools/audit.js` 自包含、无依赖,直接从 index.html 抽表,查下面这些约定是否被破坏:大事记越界/排序/双语完整性/同年重复、GL 区间越界与 k 值合法性、按名索引表的孤儿键、泳道 id 有效性等 40 余条。

**GEO 三条几何规则的分工**(容易只记住前两条):30 查外接框是否离谱、35 查核心点是否落在自己版图内、36 拿内联的 230 城基线表查「命中数是否跌破」。36 只报跌破、不报上涨(新增文明本来就会让命中数上升);基线 0 的六座城是站里确实没有对应文明(悉尼/奥克兰/火奴鲁鲁/复活节岛无大洋洲泳道,奥斯陆/哥本哈根无维京·北欧带)。**有意收缩某条版图时**,把该城基线改小即可——但改之前先确认那片地方真的该空着。

## 表清单

| 表 | 结构 | 说明 |
|---|---|---|
| `LANES` | {id, name} | 泳道,按经度自东向西;新泳道按经度插位 |
| `SPHERES` | {name, v(CSS变量名)} | 文明圈;新增需配色 + 色盲校验 |
| `CIVS` | {l, s, v, n, k, b, f, d, e, gl?} | 文明主表。l=泳道 s=文明圈 v=色深0/1/2 k=[[年,影响力]]关键帧 b=一句话 f=速览{鼎盛/中心/人物/成就} d=叙述 **e=英文块{n,w?,b,d?,f}(v122 起内联,EN.civ 退役;audit 查缺失与人物条数对齐)** gl=鼎盛区间(可选) |
| `q` / `qh` 子表 | q:{born,rule,money,power,fall,legacy} 各 [zh,en] / qh 同键,各 [zh,en] | 六问。`q` 是正文,`qh` 是**钩子句**(v134 起,可选)——渲染成正文上方单起一行的粗体,让一段里有三级层次(问题 / 一句话答案 / 正文),否则六段等密度散文没有落点、读完记不住。四条纪律:①**从 q 正文里提取,不新增事实**(新事实要走「写→核查→改」全流程)②中文 ≤20 字(audit 硬上限 30)、英文 ≤12 词——写长了层次就又抹平了 ③钩子取自正文首句时,**正文里要把那句删掉**,不删就是同一句连写两遍(唐的 power 犯过)④正文不重复速览已有的数字:PEAK 的版图/人口/占比在上面已用阿拉伯数字显示过,正文再用汉字重说一遍(「约一百万平方公里…百分之八左右」)既占字数又最难扫,直接删(古埃及 power 犯过)。audit 规则 49 查孤儿键/双语/长度。**目前只有唐、古埃及两张样板**,其余 138 个照旧只出正文——这是加法不是替换,没有半成品状态 |
| `GL_X` | 同 gl 子表 | v30 的追加块,在 TRACES 之前合并进 GL / c.gl,并**全站按起始年重排**。加新区间走这里,不必改写既有字面量;注意 gl 可能写在 CIVS 条目里,只往 GL 塞会被 `GL[c.n] \|\| c.gl` 短路吞掉 |
| `gl` 子表 | {a, b, k, t:[zh,en], d:[zh,en]} | k∈econ/art/tech/thought;t/d 自带双语 |
| `co` / `ct` | co:[{n,y?,c?,g?,m?,x?}] / ct:[zh,en] | 母条目仍是一条色带,co 把「里面有哪几个」摊开。**n/c/g/x 全部自带双语**(不进 EN 字典);`y=[起,止]`,`y[0]` 可为 null(老国不建于本时期,渲染成「亡于 X」),终年不可省;`c` 原设计是都城,城邦类条目改填**所在地区**(城邦的都城就是它自己),**同一张卡不要混用两种语义**;`g` 分组名,**保持数组原序**(五代的顺序本身就是历史);`m:1` 该组最强/最后胜出者,渲染成下划线并触发一行说明——**组里只有一个成员时不要给 m**,那行说明会自相矛盾;`x` 是**整组共用**的补充,渲染在该组末尾(同组写两个不同值 audit 会报错)。`ct` 覆盖区块标题,缺省是「同时存在的政权」——**先后接力而非并存时必须换**(古埃及用「先后的王国时期」、玛雅用「各时期的主要城邦」),不换就是教错。audit 规则 24—27 覆盖:双语完整性、年代与母条目重叠、同组 x 一致、每组最多一个 m |
| `EVENTS` | {y, ls, t, n:[zh,en], d:[zh,en]} | 交流事件;ls=涉及泳道 t∈war/trade/culture/tech/plague/migration。**v119 起 n/d 双语成对**,EN.events 已退役 |
| `TRACES` | {n:[zh,en], stops:[{y,l,t:[..],d:[..],p?}]} | 传播轨迹;stops 按年排序,p=上游节点索引(默认前一个) |
| `GEO` | {'文明名': {p:[[[lon,lat]..]..], c:[lon,lat]}} | 鼎盛期示意版图(可多多边形)+ 核心点;键=CIVS.n。**是示意图不是地图**,顶点少、海岸线简化,但「简化」不等于可以用一条直线代替一整段海岸:v82 就是因为从秦到共和国 17 条色带都拿 `[121,30]→[113,22]` 一条斜线当东南沿海,把福建与粤东整片切在版图外。画完必须过 audit 规则 30(外接框)、35(核心点在自己多边形内)、**36(城市覆盖基线)**——前两条查不出「边界画歪、漏掉一整片」,只有 36 能 |
| `CHRONO` | {'文明名': [[年,[中标题,英标题],[中一句话,英一句话]?]..]} | 大事记,**单表已按年排序**。v120 双语成对(EN.chrono 退役);**v121 原 CHRONO_X 追加块并入本表**——数据进 data/ 后「追加不改既有字面量」的理由消失。标题双非空,描述对允许单边空串(双空则省略第三元),同年重复 audit 查 |
| `VIDEO` | {'文明名': {b?:BV号, y?:YouTube ID, t:[zh,en]}} | 纪录片,**人工策展**。b/y 至少给一个;中文界面优先 B 站、英文优先 YouTube。**绝不可凭印象生成 ID**——编出来的要么死链、要么指向无关内容,而用户是小孩。没条目的文明只显示搜索按钮 |
| `RIVERS` | {n:[zh,en], p:[[lon,lat]..]} | 河流折线(不闭合),约 35 条。手写粗略坐标 ±1~2° |
| `RANGES` | {n:[zh,en], p:[[lon,lat]..]} | 山脉闭合多边形,约 20 条 |
| `DESERTS` | {n:[zh,en], p:[[lon,lat]..]} | 沙漠闭合多边形,约 8 条 |
| `PEOPLE` | {'中文人名': {g,y,yk?,ca?,r?,c,n,t,a,s,w?}} | 人物卡 281 条,**人工策展**,已覆盖全部「人物」字段。g=类别 y=[生,卒] c=所属文明 n/t/s=双语 a=双语「三件事」。**yk**:生卒不详时 y 的真实含义——`'r'`=在位、`'a'`=活跃(与 r 互斥,audit 查);**ca**=年代为约值。非君主一律用 `'a'` 而不是 `'r'`,否则会渲染成「在位 X—Y」(伊姆霍特普、抄写员被标成在位过一次)。**在世人物**:schema 要求 `y[0]<y[1]`,直接写 `[生年,当年]` 等于宣告此人已故 —— 改用 `yk:'a'` + 活跃年区间,生年写进「三件事」。全站目前只有屠呦呦一位在世者(`y:[1969,2025] yk:'a'`,1969 是她加入 523 任务之年)。键=中文人名,英文界面靠**位置**换回中文键 |
| `ACHV` | {'成就名': {g, c, y?, yk?, ca?, n, t, a, s}} | 成就弹出卡,**人工策展**,目前 4 张试点。g=类别(见 `AGLYPH`/`AGNAME`) c=所属文明 n/t/s=双语 a=双语「三件事」。**键必须与该文明 `f['成就']` 里的原文整词一致**——渲染端把 `f['成就']` 按 `NSPLIT` 拆开、`stripParen` 去括号后做**精确匹配**,对不上就只是纯文本、卡片永远点不开而页面不报任何错(v62 四张里中了两张:写了「科举」而站里是「科举制」、写了「青铜礼器」而站里是「司母戊鼎」)。audit 规则 31/32 现在查这一条 + 年份一致性。**卡片渲染进 `#pcard .pc-box`,不是 `panel`**(与人物卡同一层) |
| `ACHV.y` | [起,讫] 或 [年] | **y = 这东西起作用的年代**,不是所属文明的存续期(所以罗马法可以是「前450—533」而它挂在罗马帝国下)。只考订得出一个时点时给**单值**,配 `yk:'m'`(铸造/制成)与 `ca:1`(约值),渲染成「铸于 约前1200」/「cast c. 1200 BCE」。**别把断代的不确定范围写成区间**——那会被读成「这东西只存在了六十年」,司母戊鼎原先写 `[-1250,-1190]` 就是这个错(v68 改掉)。yk/ca 与 `PEOPLE` 同一套词汇 |
| `AGLYPH` / `AGNAME` | {类别: 字形} / {类别: [zh,en]} | 四类:inst 制度 ⚖ / obj 器物 🏺 / tech 技术 ⚙ / idea 观念 💡。config 表,留在 index.html;Phase 3 起 audit 经 `tools/load.js` 按名抽取,**位置不再敏感**(旧的「TRACES 之后要单独补一段」陷阱已随位置切片一起退役) |
| `PGLYPH` / `PGNAME` | {类别: 字形} / {类别: [zh,en]} | 八类:king 帝王/state 政治/war 征战/mind 思想/sci 科学/art 艺术/faith 宗教/sail 航海。「思想」只给真正的思想家,治国者归「政治」 |
| `PEAK` | {'文明名': {a?, ay?, an?, p?, py?, pc?, w?}} | 鼎盛期硬数字。a=版图(万 km²) **ay**=该面积对应年份 **an**=面积口径说明(双语,如民国的「法理疆域」);p=人口(万,**拿不准写成 [下限,上限] 区间,不要取单一高值**) **py**=人口对应年份 **pc**=1 表示官方户口数(缺省为学界估算);w=占当时世界人口(0~1,渲染成百分比)。**版图取历史最大疆域、人口取历史峰值,二者通常不在同一年**,卡片上会把 ay/py 显示出来。**as**=面积数字的出处(双语,只在偏离主序列时标,如「非 Taagepera 序列」「Turchin 数据」——未标的即出自主序列)。口径一致比单个数字精确更重要:版图统一用 Taagepera 序列,w 的分母按 py 年取 McEvedy & Jones 的世界人口 |
| `BORDERS` | [[[lon,lat]..]..] | 今日陆上国界折线(Natural Earth 110m 简化,333 段 1374 点约 17KB)。**只画在地理视角的世界地图上**——卡片版图小地图不画,免得被当成那个年代的边界。与 LAND 一样是压缩底图(数据在 `data/borders.js`) |
| `GEO_CITY` | [[zh, en, lon, lat]..] | 地理视角的 91 个可点城市。坐标必须命中 ≥1 条 GEO 版图(audit 规则 37);挑选按历史重要性。纬度显示范围 [-56,78],出界画在画布外 |
| `GC_ALIAS` | {'城市名': [古称..]} | 城市古称别名,供地理视角从大事记筛「发生在这里的事」。只收同一城址(或紧邻如咸阳之于西安);歧义名不收(「东京」不给开封);civ 名含别名时该别名对该 civ 失效 |
| `PLACE_LORE` | {'城市\|文明': [zh,en]} | 地点小段手写层:「那个朝代这个地方是什么样」,两三句硬事实,双语。城市与文明都必须真实存在且城市落在该文明版图内(audit 规则 38);年代拿不准用「前后/传为」hedge |
| `PLACE` | {'古地名': [国zh, en] 或 [国zh, en, 今名zh, en]} | 古都今属何处。键=`f['中心']`里的中文原文(精确匹配,含括号);**中英共用这一套中文键**,不会两边跑偏。四项=今天仍有对应城市,两项=遗址或泛指区域 |
| `WIKI_NAME` / `CIVS.e.w` | 消歧义映射 | 中文歧义名(明→明朝)/英文歧义名(Ming→Ming dynasty)。**v123 起 EN 字典已整个退役**:英文全部内联在各条目里(EVENTS/CHRONO 成对、CIVS 的 e 块、config 表的 en 字段) |
| `LAND` | 压缩海岸线环 | 生成自 world-atlas land-110m,勿手改 |

## 常见操作

- **k 关键帧的首尾约定**:已消亡的文明首尾帧影响力都写 0(色带两端收窄成尖);**存续至今的文明末帧保持非 0**(如共和国 .9、美国 .95),色带在 2025 右边缘齐平截断——这是刻意的,别"修正"成 0。
- **大事记年份必须落在 k 的首末年之间**,否则曲线上画不出锚点(`sparkSVG` 直接跳过越界节点),只在列表里出现。历史年份早于色带起点时,应前移色带起点而不是改年份。
- **加纪录片**:先自己看过、确认视频确实在讲这个文明、画质时长过得去,再往 `VIDEO` 加一行。B 站 BV 号取自视频链接 `bilibili.com/video/BV…` 那一段。**iframe 是点击后才插入的**,所以不加视频、或用户不点,页面对外零请求——离线缓存与"不引入外部资源"的铁律都不受影响。
- **加地理要素**:往 `RIVERS`/`RANGES`/`DESERTS` 加一条即可,地图会自动画(只画与视野相交的)。**河名的标注条件是"至少两个点落在本文明版图多边形内"**——加了新河却不见名字,先查是不是版图多边形没覆盖到,而不是去放宽判定;放宽会让埃及标上幼发拉底河。
- **找泳道的断流**:按年扫一遍某泳道在场的文明(取 k 插值 >0.02),空场时段就是内容缺口。v15 靠这招发现草原·中亚有四段共 775 年完全空白。**别靠看图估**——压缩的古代时间轴会让空档显得比实际短。
- **色深 v 的分配规则**:只在「同泳道 + 同文明圈 + 时间重叠」三者同时成立时才需要错开;跨圈不必管(颜色本来就不同)。加色带前先列出时间上与它重叠的同圈色带,再挑没被占用的 v。
- **标签会被抑制**:色带厚度 <13px 或横向宽度 <52px 时不画名字(`buildChart` 里的规则,防重叠)。古代段时间轴压缩厉害,一个 120 年的色带只有约 47px,拿不到标签——这是有意的,不是 bug;该色带仍可悬停/点击。
- **每个文明至少要有一个人物**(v28 定的标准),**「真的没有名字可写」才留空**——无同时代文字也无考古实证个体时,编人名比空着糟得多(二里头不写禹、三星堆不写蚕丛鱼凫:千年后追记的传说不算人)。**有意留空的名单在 audit 规则 48 的 `FIGURES_NA` 里**(v130 时点 10 条,双向校验,别再重判)。特奥蒂瓦坎不属于这一档:统治者名字见于玛雅碑文(与加纳帝国只见于阿拉伯地理学家同理);莫切的西潘王(考古实证个体)同理,是否补卡待 Ray 拍板。**给文明加「人物」字段要中英两侧同时插入且逐个对齐**,注意 `f:{'中心':'…'}` 可能是唯一字段、末尾无逗号,以及 `Siyaj K'ak'` 这类英文名里的撇号必须转义。
- **加人物卡**:`PEOPLE` 加一条即可,人名会自动从维基外链变成站内弹卡。写作格式固定为**「一句话身份 + 他做了三件事 + 意义」**——三件事对小孩最好读;第三段写**客观成就与地位**(世界之最、后世沿用多久、是否列入世界遗产这类可核查的事实),**不要写成因果推论**。v26 最初按「所以」的因果口径写,Ray 判定"太深刻",v26.1 全部重写过一遍。**键用中文人名,英文界面靠位置换回中文键**,所以 `EN.civ[n].f.Figures` 必须与中文「人物」逐个对齐(audit 查这一条;隋原本写成 "Emperors Wen and Yang" 合并了两个人,v26 拆开了)。没配 PEOPLE 的人名保持维基外链行为,两者视觉上靠「浅底片 vs ↗箭头」区分。
- **加成就卡**:先去该文明 `f['成就']` 里把原文一字不差抄出来当键(**别按概念自己命名**),再往 `ACHV` 加一条,那个词就自动变成可点。写作格式固定为**「这东西是什么 → 怎么来的 → 后来影响了谁」**(`t` 一句话 + `a` 三件事 + `s` 影响),与人物卡的「三件事」同构。`y` 填**这东西起作用的年代**而不是文明的存续期;只有一个时点就给单值 + `yk:'m'` + `ca:1`。**加完必跑 `node tools/audit.js`**——规则 31 会告诉你这张卡点不点得出来(现有整词会一并列出),规则 32 会比对大事记里同名事件的年份。成就卡与 GL 各管各的:**GL 讲「这个文明什么领域什么时候最好」,成就卡讲「这一样东西本身是什么、怎么来的、后来影响了谁」**(Ray 定的口径)。
- **加古都的"今属何处"**:在 `f['中心']` 里写古地名,再去 `PLACE` 补一条。渲染时按今属国家归并(`todayHTML`),所以「长安、洛阳」出来是「今 中国:西安、洛阳」而不是重复两遍国名。**遗址类条目只写两项**(没有对得上的今日城市);但要注意:同一文明里若一个遗址与一个有今名的城市落在同一国家,遗址会被归并吞掉——这时给遗址补一个「XX近郊」当今名。`node tools/audit.js` 会查 PLACE 缺条目、孤儿条目、中英"中心"地点数不等。
- **加一个文明**:CIVS 加条目(影响力峰值参考同泳道对比;**e 块英文与中文一起写**)→ GEO 加版图 → CHRONO 加大事记(双语成对)→ 歧义名进 WIKI_NAME
- **加传播轨迹**:TRACES 加条目即可(选择器自动出现),节点 t/d 双语内联
- **调影响力曲线**:只改 k;注意同泳道同期堆叠总和(超限会自动压缩,峰值别全 1.0)
- **拆分色带**(参考 v10 中国拆分):新条目替换旧条目;GEO/CHRONO 同步换键(先跑 `node tools/refs.js`);旧键残留无害但建议清理
