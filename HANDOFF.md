# Handoff — 文明长河（17-civilization-river）
更新时间：2026-08-15 11:53

> ## ⚠ 最重要的一条：这个项目现在有**两个 session 在跑**
>
> 本文件由「A 线」写（做的是 v134–v148 与 v165），而 **v149–v164 是另一个 session（B 线）做的**。
> 两边都在改同一个 `index.html`（2.4MB 单文件），**并发写会互相覆盖**。
>
> **接手前必做三步**：
> 1. `git fetch` —— 本文引用的 `origin/main` 是本地 ref，可能已过期
> 2. `git status` + `git log --oneline -10` —— 以现实为准，不信本文
> 3. **动手前先问 Ray：另一条 session 还在跑吗？** 若在，约定各改哪一块，
>    或让其中一条先收工。**不要两边同时编辑 `index.html`。**

---

## 任务目标

给 7 岁的 Jasper（在上海用 iPad）做的世界文明兴衰可视化**单文件 PWA**。
数据在 `data/*.js`，`tools/build.js` 注入回 `index.html`。

---

## 当前状态（2026-08-15 11:53 实测）

- `HEAD = origin/main = 99fb52a`，**工作区干净、已 push**
- 规模：**140 文明 · 大事记 1000 · GL 265 段(140/140) · 事件 38（v167 扩容 16 条前 1500 年交流事件）· 轨迹 10 · 共同体 3 · 搜索别名 10**
- `data/` 共 **23 张表**；`tools/audit.js` 现有规则编号到 **52**
- 验证：`node tools/audit.js` 退出码 0

### 本轮（A 线）做完的

| 版本 | 内容 |
|---|---|
| v134 | 六问标题改口语问句 + 钩子句样板（唐、古埃及）+ audit 规则 49 |
| v135 | 「国家还在但色带结束了」排查，4 条改名（荷兰共和国／埃塞俄比亚帝国／西班牙·葡萄牙帝国／朝鲜半岛诸王朝） |
| v136–v142 | **六问钩子句铺完 140 个文明（840 条）** + audit 规则 50 |
| v143–v144 | **共同体层**：欧盟／东盟／非盟，新表 `data/unions.js` + audit 规则 51 |
| v145 | 色带标签不再被画布边切掉 |
| v146 | 台湾表述统一口径（按语法角色分类，非全局替换） |
| v147 | 地图白色长条（跨日界线）+ 标签跟随横向滚动 |
| v148 | 按实测重写 `docs/DEPLOY-CN.md` |
| **v165** | **卡内圈色 + 「同时代的世界」+ 交流事件挪进主行** |

### B 线继续做的（2026-08-15 下午，Ray 直接指挥）

| 版本 | 内容 |
|---|---|
| v166 | 传播追踪升主行按钮化（选中反色）+「发生了什么」放宽为全球同期按年排序 |
| v167 | **EVENTS 22→38**：补 16 条前 1500 年交流事件（战车/字母/御道/季风航路/犍陀罗/大秦使者/法显/蚕种/玄奘/遣唐使/印度数字/智慧宫/维京抵美/马可·波罗/伊本·白图泰/南海航路），口径纪律见 CHANGELOG |

### B 线做的（v149–v164，我没有它们的上下文）

细色带命中补偿(v150) · 开场条(v151) · **搜索上线=IDEAS 001 层一 + `data/search_alias.js` + audit 52**(v152) ·
对抗核查修正(v153) · 曲线与鼎盛条合并单 SVG(v156) · 鼎盛条上写字(v157) · 鼎盛条压淡(v158) ·
**UI 评审批 v159–v164**（对比度、字号 em 化、触控命中加大、**鼎盛条退成中性衬底(v163 Ray 裁决)**、首屏控件两级分层(v164)）。

**接手时读 `docs/CHANGELOG.md` 的 v149–v164 条目**，那里有 B 线的判断依据。

---

## 进行中 / 下一步

### Ray 手上的

1. **微信发 `index.html` 给上海** —— 已实测单文件自包含（空目录下功能完整）。**不用等备案，今天就能用。**
   六个故事页 `tang/qin/han/song/ming/qing.html` 同理。
2. **提腾讯云工单**：「全球可用区（含中国大陆）项目，使用平台默认 `.edgeone.cool` 域名，
   中国大陆访问返回 401，是否必须绑定已备案域名？」→ 答案回来改 `docs/DEPLOY-CN.md` 第三节。
3. **开压缩**：EdgeOne **站点加速 → 智能压缩**（不在 Pages 项目里）。实测传输 2362KB vs 本地 2365KB，**没开**。
4. **看 24 条 B 站候选**（`docs/VIDEO-CANDIDATES.md`），报「文明 + BV + 标题」入 `data/video.js`。
   ⚠ 铁律：**绝不可凭印象生成 BV 号**。
5. **细看 840 条六问钩子句的语气**，发现别扭的报编号。
6. **看 v165 的效果**：`open index.html` → 点唐 → 滚到底看「同时代的世界」→ **点里面的名字会跳转**。

### 代码侧（等 Ray 拍板）

- **维基链接国内打不开**：A. 中文改百度百科（`wiki()` 一个函数、一行）／B. 不改／C. 去掉外链。**建议 A。**
- **东帝汶入盟待核**：`data/unions.js` 东盟按 10 国计，`nt` 已写明「待核」。
- **故事页离线**：`sw.js` 缓存清单只有 `index.html`。**有意押后**（见 `docs/TANG-EXPERIMENT.md`）。
- **`docs/IDEAS.md` 001 的层二层三**（故事级颗粒）：层一（搜索）已由 B 线 v152 上线，
  层二三仍等唐朝实验结论。

---

## 试过但失败的路

1. **EdgeOne 排查连续改口三次**（区域→备案→环境绑定→又回到备案）。根因是**先下结论再找证据**。
   最后靠一条不对称定案：**干净 URL 国内 401(123ms) ／ 海外同一 URL 不带任何凭证 200(2362KB)**——
   同时否掉「墙」「区域选错」「预览未发布」三种解释。**教训：先量再写。**
2. **卡内想直接用圈色当文字色**（v165）：实测 140 张里 **104 张对比度低于 4.5:1、最差 1.64**。
   圈色是给大色块的填充色。改用 `color-mix(圈色 55%, var(--ink))` 派生，55% 是扫出来的拐点。
3. **「同时代」重叠门槛写死 30 年**：秦只存在 14 年，一个都算不出来（曾是全站唯一没有该区块的）。
   改 `min(30, 自身跨度/2)`。
4. **想全局替换「台湾→中国台湾」**：会造病句。改按语法角色分类（归属语境 3 处改，历史叙述 7 处保留）。
5. **想用「横跨色带的括号」画欧盟**：全景下只有 45px 放不下标签；且四条色带里只有法德是成员，**四条错两条**。
6. **对地图白条的第一次推断错了**：以为压缩掉顶点，实为**跨日界线**（两条横扫全图的边，缝的上下边界正好是 65/69）。
7. **选择器写错造成两次假警报**：`data-fig`（实为 `data-person`）、`panel.querySelector('svg')`（拿到火花线）。
   **报「找不到」时先证明选择器是对的。**
8. **反向注入自己出错**：bash 传了字面 `\n`，而我只 grep 关键词把真实报错滤掉了。
   **注入测试要看清报的是哪一种错。**
9. **`curl` 出网被本机权限拦**；`timeout` 命令不存在。改用
   `node -e "https.request({host:<IP>, servername:<域名>, headers:{Host:...}})"` 直连边缘节点。
10. **用 `getComputedStyle` 读 `color-mix` 的结果会解析失败**（返回 `oklab()` 等），
    算出「对比度 5063」这种不可能的数。**改用 1×1 canvas 取真实像素。**

---

## 阻塞项 / 等用户决定

- **并发 session**（见文首）——这是当前最大的风险。
- **维基链接 A/B/C** —— 一行改动，只差拍板。
- **备案** —— 买域名 + 个人备案 1–3 周，Claude 不碰（涉及身份证）。
- **840 条钩子句语气 review**。

---

## 关键上下文

### 改完必跑

```
node tools/build.js      # 改 data/*.js 之后必跑,否则 audit 规则 45 会报漂移
node tools/audit.js      # 退出码非 0 即有问题
sed -n '/<script>/,/<\/script>/p' index.html | sed '1d;$d' > /tmp/c.js && node --check /tmp/c.js
```

浏览器验证：`python3 -m http.server 8777 --bind 127.0.0.1 --directory <项目路径>`
（**用后台任务方式起，`nohup &` 会被权限拦**），再用 claude-in-chrome；`file://` 会被扩展拒绝。

### 本机环境的坑

- **Shadowrocket 以 TUN 方式在跑**（系统代理显示 Off，但 DNS 返回 fake-ip `198.18.x.x`）。
  **浏览器测不出「手机不挂梯子」的情况**；测国内可达性必须 `dig @223.5.5.5` 拿真实 IP + node 直连并带 SNI。
- `curl` 出网被权限拦；`timeout` 不存在；`rm -rf` 会触发确认。

### 关键文件

| 文件 | 作用 |
|---|---|
| `data/unions.js` | 共同体表（v143 新增），字段与纪律见 `docs/DATA.md` |
| `data/search_alias.js` | 搜索别名（B 线 v152 新增） |
| `tools/audit.js` | 规则编号到 52；49/50/51 是 A 线加的，52 是 B 线加的 |
| `docs/DEPLOY-CN.md` | **已按实测重写**；第七节记了「这份文档改过什么」，**加内容前先读** |
| `docs/DATA.md` | 「台湾相关表述的口径」一节 + `q`/`qh` 子表 + `UNIONS` |
| `docs/DESIGN.md` | 标签定位(v145/v147)、共同体层、跨日界线、**卡内强调色与「同时代的世界」(v165)** |
| `docs/IDEAS.md` | 001 搜索/故事颗粒（**层一已上线**）；002 共同体（**已收口**） |
| `docs/TANG-EXPERIMENT.md` | 六页实验的判据与边界——**结果出来前不加功能** |

### 长期约定（不在本文）

- 仓库 `CLAUDE.md`：数据改动工作流、铁律（双语／颜色只承载文明圈语义／不引入外部资源／VIDEO 人工核实）
- `docs/ARCH.md`：数据迁出架构（Phase 1–4 已完成）
